# 概率论与贝叶斯推断复习指南（codebuddy 版）

> “没有数据，你只是又一个有意见的人。”——W. Edwards Deming
>
> 这句话的另一面是：**有了数据，你还得会量化“不确定”**，否则意见换了个包装还是意见。
> 这份指南把本仓库 `统计学基础/` 里的 STA238 讲义（A. Gibbs & A. Stringer，基于 MIPS:
> Dekking et al.）重新讲成**一个带因果逻辑的故事**。公式不再是重点，重点是你读完能回答：
> “这一步为什么会出现？它和下一步有什么关系？”
>
> 我采用和优秀教程一样的写法：**先抛现实问题 → 给数学定义 → 用讲义里的真实数字示例建立直觉 → 最后用代码验证**。
> 故事的最后会落回本仓库的其他书——`from inverse problem to diffusion/`、`贝叶斯变分推断/`、
> `IP_and_Im_Lectures-master/` 等，让你看到同一套概率语言如何贯穿整个仓库。

**怎么读**：不用从头背公式。每节开头的“我们为什么需要它”才是主线；公式只是把那一节的直觉说精确。遇到例子，先自己猜个数字，再看答案反差——那一下反差，就是你会记住的东西。

---

## 0. 先说清楚：我们到底在解决什么问题

你手里的数据永远是**带噪声的**。老忠实泉（Old Faithful）两次喷发之间的等待时间，平均 209 秒，但
有的等 40 秒、有的等 90 秒。同一座泉，为什么每次不一样？因为它不是一个确定值，而是一个**分布**。

统计学基础这门课，从头到尾只做一件事：**从带噪声的数据里，反过来猜出那个生成数据的分布（以及它的参数）**。
而本仓库里那些看起来高深的书——图像反问题、扩散模型、变分推断——其实都在做同一件事，只是“数据”变成了图像、“分布”变成了像素空间里复杂到写不出来的那个东西。

记住这条主线，后面所有概念都是它的注脚：

> **分布 →（噪声叠加成高斯）→ 似然（数据怎么来的）→ 贝叶斯（把数据和信念合起来）→ 后验 →〔能直接算就直接算，算不出就用变分或扩散近似〕**

---

## 1. 我们为什么需要“分布”这个概念

因为单次观测说明不了任何事。老忠实泉等了 80 秒，你什么也推断不出来；但如果你记了 272 次等待时间，
就敢说“它大概是双峰的，多数在 50 秒或 80 秒附近”。

怎么把 272 个数字变成“分布”？讲义给了三件工具：

- **经验累积分布 (ECDF)**：直接数“有多少次 ≤ 某个值”。最老实，不假设任何形状。
- **直方图 / 核密度估计 (KDE)**：把数据“抹平”成一条连续的密度曲线 $f(x)$。KDE 估的就是 $f$ 本身。
- **箱线图、样本矩**：用中位数、IQR、MAD 等描述“中心在哪、散得多开”，而且特意要用稳健的量——
  因为均值和标准差会被一两个离群点带偏。

这里有个你后面会反复用到的关系：**CDF 就是 pdf 的积分**，$F(x)=\int_{-\infty}^x f(t)\,dt$。它看起来简单，
却是连接“分布”和“概率”的桥。

> **和本仓库的关系**：KDE 在估的，正是某个未知密度 $p(x)$。而扩散模型（第 8 节）学的是
> $\nabla_x\log p(x)$——也就是“密度往哪边走更大”。KDE 是密度估计的朴素版，扩散模型是它用
> 神经网络撑到高维图像的版本。本质同一件事。

---

## 2. 当我们多收集一些数据时，会发生什么

你大概听过一个朴素的期望：**样本越多，均值越准**。这叫大数定律（WLLN）：只要每次抽取独立同分布，
$\bar X_n$ 就会依概率收敛到真实期望 $\mu$。讲义用 Gamma 样本和抛硬币比例都演示了这一点。

**但这里有个坑，而且这个坑很重要。** 不是所有分布都有“期望”。Cauchy 分布就没有——
它的两个长尾把平均值永远拉扯，所以即使你取一百万个数的平均，直方图还是那副重尾模样，**既不收敛也不变尖**。
结论是：大数定律成立的前提，是那个分布的矩得存在。

那“变尖”这件事本身叫什么？叫**中心极限定理 (CLT)**。它的真正含义是：哪怕原始分布奇形怪状，
只要它有有限方差，把样本均值标准化之后
$$\frac{\sqrt n\,(\bar X_n-\mu)}{\sigma}\ \xrightarrow{d}\ \mathrm N(0,1)$$
就会稳稳收敛到标准正态分布。注意那个 $n^{1/2}$ 的缩放——这是频率派几乎所有正态近似的来源，
也是为什么“样本均值的标准误是 $\sigma/\sqrt n$”。

> **和本仓库的关系（关键）**：CLT 解释了为什么“噪声”在本仓库里几乎永远是**高斯**的。
> 反问题里观测写成 $y=A(x)+\eta,\ \eta\sim\mathrm N(0,\sigma^2 I)$；扩散前向过程写成
> $x_t=\sqrt{\bar\alpha_t}x_0+\sqrt{1-\bar\alpha_t}\,\epsilon$。背后都是同一句话：
> 大量独立小扰动叠在一起，分布就趋向高斯。score-based SDE 能成立，也是因为每一步的边际分布都是高斯的。

---

## 3. 什么是“统计推断”：我们其实在猜一个看不见的数

到了这一步，问题升级了。我们不满足于描述数据，我们想**猜出那个生成数据的、看不见的量**。
讲义给了一个干净的定义：统计模型，就是“以未知**参数**为条件的随机变量的概率分布”；推断，就是用数据去估这个参数。

这里要建立一个很重要、但很多人一开始转不过来的弯：**参数是固定的，只是你不知道；数据是随机的，因为你抽到了这一份。**
比如：

- 一种新药和安慰剂的胆固醇差异，是一个固定的数，你用病人的随机样本去估它；
- 银河系的质量，是固定的，你用恒星位置和速度去反推；
- Janka 木材硬度回归里，$\beta_0,\beta_1$ 是固定的，你用每根木头的测量去拟合。

> **和本仓库的关系**：在 `IP_and_Im_Lectures` 和 `from inverse problem to diffusion` 里，
> **那个“看不见的参数”就是你想重建的图像 $x$ 本身**。观测 $y$ 是数据，前向算子 $A$ 把参数映射成观测：
> $y=A(x)+\eta$。所谓“重建图像”，在数学上就是第 3 节说的“估计统计模型里的参数”。一句话把你和那些
> 大部头拉到了同一块地基上。

---

## 4. 怎么判断一个估计量“好不好”

既然要估，自然要问：哪个估计量更值得信？讲义把标准拆成两半：

- **偏差 (Bias)**：它平均来看偏不偏？无偏意味着 $E(\hat\theta)=\theta$。
- **方差 (Variance)**：它稳不稳？每次抽样结果跳不跳？

两者合起来就是**均方误差** $\mathrm{MSE}=E[(\hat\theta-\theta)^2]=\mathrm{Var}+\mathrm{Bias}^2$。
所以“又无偏又低方差”当然最好，但现实常常逼你二选一。于是有了**效率**——在同样无偏的估计量里，方差小的更高效；
以及 **Cramér–Rao 下界**：任何无偏估计量的方差都不可能小于 $1/I(\theta)$，其中 $I(\theta)$ 是 Fisher 信息。
换句话说，精度是有天花板的。

**德国坦克问题**把这个权衡演得很明白。你从编号 $1..\theta$ 里随机抽到 $n$ 个数，想估最大的 $\theta$。
用样本均值 $\bar X$ 是无偏的，但太飘；用最大值 $\max X_i$ 虽然有偏，方差却小得多。
讲义给的无偏修正是 $\max X_i+\frac{\max X_i-n}{n+1}$——但即便不修正，那个有偏的最大值在很多情况下
**总误差（MSE）反而更小**。这就是偏差–方差权衡活生生的例子。

> **和本仓库的关系**：成像重建里的任何一个估计量，都同样撞上 Cramér–Rao 天花板。
> 第 5、8 节的 **MAP 估计就是“有偏但高效”的典型**——它用先验把方差压下来，代价是引入一点偏差，
> 逻辑和德国坦克里选最大值完全一致。

---

## 5. 最大似然：我们到底该选哪个参数

现在进入核心方法。给定一个参数 $\theta$，数据出现的概率（似然）是 $L(\theta;x)=p(X=x\mid\theta)$。
Fisher 在 1922 年敲了所有人一下桌子：**似然不是概率**。概率是“$\theta$ 固定，问数据长这样有多大可能”；
似然是“数据已经看到了，拿 $\theta$ 当变量，比一比哪个 $\theta$ 让眼前这份数据最说得通”。

操作上我们取对数（把乘积变求和，好优化），然后找让对数似然最大的 $\hat\theta$——这就是**最大似然估计 (MLE)**。

两硬币的例子最直观：单次 Bernoulli 的似然是 $p^x(1-p)^{1-x}$，对 $n$ 次独立观测求最大，答案就是样本比例 $\hat p=\bar X$。
就这么简单，却是一切现代估计的起点。

> **和本仓库的关系（最重要的桥）**：反问题假设 $y=A(x)+\eta,\ \eta\sim\mathrm N(0,\sigma^2 I)$，
> 那它的似然就是高斯
> $$p(y\mid x)=\mathcal N(A(x),\sigma^2 I).$$
> 取负对数，常数项扔掉，得到
> $$-\log p(y\mid x)=\frac{1}{2\sigma^2}\|y-A(x)\|^2+\text{const}.$$
> 看到了吗？**MLE 就是最小二乘的数据保真项**。你在 `from inverse problem to diffusion` 里看到的
> “data-fidelity / likelihood”，精确来源就是这一节的似然。下一节会把它和“信念”合起来。

---

## 6. 我们怎么知道估计“有多不确定”

算出一个点估计还不够，你总得知道它靠不靠谱。讲义给了两条互补的路：

- **Bootstrap**：既然不知道数据的真实分布，就从已有样本里**有放回地重抽样**很多次，每次重算一遍估计量，
  用这一大堆结果近似出估计量的抽样分布。它妙在**完全不需要公式**——Old Faithful 的均值、software 数据的 KS 距离，
  都能这么估出不确定性。
- **置信区间**：如果噪声方差 $\sigma$ 已知，区间就是 $\bar X\pm q_{1-\alpha/2}\sigma/\sqrt n$；
  如果 $\sigma$ 未知，用样本标准差 $s_n$ 替上，并把正态分位换成 **$t$ 分布**分位。
  讲义特意用模拟提醒：直接拿 $s_n$ 当真值会让区间偏窄、覆盖率不够，加宽成 $t$ 区间才修得回来。

这里有个容易踩的语义坑：**置信区间说的是“这个随机区间在重复抽样下有 $1-\alpha$ 概率盖住真值”**，
不是说“真值有 $1-\alpha$ 概率落在这个具体区间里”。下一节贝叶斯会给你一种能这么说的方式。

> **和本仓库的关系**：现代深度成像常用 dropout、ensemble、扩散随机采样来近似后验不确定性。
> 它们和 Bootstrap 是同一个念头——“用重采样/多次采样代替算不出的抽样分布”。

---

## 7. 贝叶斯：当我们愿意说出“我相信什么”

频率派把 $\theta$ 当成固定未知的数，只谈“用数据能证明什么”。贝叶斯派换了个姿态：**把 $\theta$ 也看成随机的，
先给一个“先验信念” $p(\theta)$，再用数据更新它。** 更新的规则就是贝叶斯定理：

$$p(\theta\mid y)=\frac{p(y\mid\theta)\,p(\theta)}{p(y)}\ \propto\ p(y\mid\theta)\,p(\theta).$$

左边是**后验**——看完数据之后你对 $\theta$ 的信念；右边两项，一个是第 5 节的似然，一个是你事先的信念。
**数据越多，似然越锋利，先验的影响就越小；数据越少，先验越能兜住你。**

讲义里最漂亮的例子是世界人口模型。把各国人口看成 Poisson 计数 $y_i\sim\mathrm{Poisson}(\lambda)$，
先验选一个弱信息的 Gamma 分布 $p(\lambda)=\mathrm{Gamma}(\alpha_0,\beta_0)$。
因为 Gamma 和 Poisson 是**共轭**的，后验还是 Gamma，而且一眼能写出来：

$$\lambda\mid y\ \sim\ \mathrm{Gamma}\!\left(\alpha_0+\sum y_i,\ \beta_0+n\right).$$

于是你直接拿到 $\lambda$ 的完整后验：可以取众数/均值/中位数当点估计，取 $2.5\%$ 和 $97.5\%$ 分位当
**95% 可信区间**。注意这里的说法和频率派不同——可信区间可以正经地说“$\lambda$ 有 95% 概率落在这里”。
讲义还强调**先验敏感性**：弱信息先验下结论主要由数据决定；换成强先验，结论会跟着信念走。
所以做贝叶斯，必须先交代你用了什么先验。

> **和本仓库的关系**：这正是反问题与扩散的入口。把上面公式里的 $\theta$ 换成图像 $x$，就是
> $p(x\mid y)\propto p(y\mid x)\,p(x)$。能像世界人口那样共轭、直接算后验，是幸运的少数；
> 图像这种高维、分布写不出来的情况，才是本仓库要解决的硬骨头——见下一节。

**先自己猜一个数字**（这是建立贝叶斯直觉最快的方式）。设想一种疾病，试剂 95% 灵敏、90% 特异，
但人群中真实患病率只有 1%。一个人检测呈阳性，你觉得他“真的患病”的概率有多大？
很多人会脱口而出“90% 以上”。实际算一下：先验 $P(\text{病})=0.01$，似然比
$\frac{P(+|\text{病})}{P(+|\text{无病})}=\frac{0.95}{0.10}=9.5$，于是后验
$$P(\text{病}\mid +)=\frac{0.01\times0.95}{0.01\times0.95+0.99\times0.10}\approx 8.76\%.$$
**反差就在这里**：证据把信念从 1% 拉到了 8.76%，但远没到“确诊”的程度。原因只有一个——**先验太弱（患病率太低）**。
这正好解释了讲义里反复强调的“先验敏感性”：数据再犀利，也撼不动一个极端的先验；反之先验一变，结论跟着变。

**再亲手感受一次“信念更新”**，用讲义的世界人口模型。弱信息先验 $\mathrm{Gamma}(2,1)$ 的均值是 $2/1=2$。
假如 50 个国家的人口计数均值约为 7（总和 350），后验立刻变成 $\mathrm{Gamma}(2+350,\ 1+50)$，
均值是 $(352)/(51)\approx 6.90$。**看，先验均值 2 被数据一把拉到了 6.9 附近**——数据越多，先验的话语权越小，
这正是贝叶斯“随证据进化”的核心。代码见第 10 节，跑一遍你就能看见这条曲线怎么从先验“长”成后验。

---

## 8. 后验收不到时怎么办：什么时候、为什么、怎么做

先用一句话点破本章的动机：**频率派 / 传统 MLE 只给你一个“点”，贝叶斯给你一整个“分布”**。
点好用、好优化；但只有分布能让你诚实地说出“我有多不确定”。可麻烦在于——这个分布常常**算不出来**。
下面按“什么时候 / 为什么 / 怎么破”三层来讲，这正是优秀教程的 when–why–how 套路。

### 8.1 什么时候需要去“近似”后验
- **数据很少**、先验得替你补位时；
- 你真正关心**不确定性**、而不只是想要一个答案时；
- 你要做**生成建模**（比如从噪声里画出一张图）时。
这三类，恰好就是 `贝叶斯变分推断` 那篇教程里列举的变分推断用武之地。

### 8.2 为什么算不出来
世界人口那个后验能“一眼写出来”，**完全是因为共轭**。一旦先验和似然不配套，后验
$p(x\mid y)\propto p(y\mid x)p(x)$ 就既没闭式、也无法直接采样——分母那个 $p(y)$ 要在整个高维空间积分，根本算不了。
图像这种情形尤其惨：先验 $p(x)$ 是像素空间里复杂到写不出的分布。

### 8.3 怎么破局：本仓库的两套解法
它们用的还是第 7 节那两个老零件（似然 + 先验），只是换了“交付方式”。

**(a) 用扩散模型当“学出来的先验” —— `from inverse problem to diffusion`**

Gutha 等人（WACV 2025）把反问题整件事翻译成了贝叶斯语言：

1. 观测 $y=A(x)+\eta,\ \eta\sim\mathrm N(0,\sigma^2 I)$；
2. 后验 $p(x\mid y)\propto p(y\mid x)\,p(x)$（就是第 7 节的公式，$\theta$ 换成 $x$）；
3. 似然 $p(y\mid x)=\mathcal N(A(x),\sigma^2 I)$——第 5 节那个高斯似然，对应数据保真项；
4. 先验 $p(x)$ 太复杂写不出，于是**用一个预训练好的无条件扩散模型来代表它**；
5. 扩散模型真正提供的是 **score** $\nabla_x\log p_t(x)$，通过 score matching 学出来，替代算不出的 $\nabla\log p(x)$；
6. 要做点估计，就取 **MAP**：$x_{\mathrm{MAP}}=\arg\max_x[\log p(y\mid x)+\log p(x)]$，等价于“数据保真 + 先验正则”的优化；
7. 要采样，就跑反向 SDE $dx=[f(x,t)-\tfrac12 g(t)^2\nabla_x\log p_t(x)]dt+g(t)d\bar w$，其中可解性全靠第 5 步的 score。

一句话总结这一支：**贝叶斯定理里“似然×先验=后验”没变，只是先验从手写分布换成了扩散模型。**

**(b) 用变分推断去“近似”整个后验 —— `贝叶斯变分推断`**

另一条路更直接：既然后验算不出，就别硬算，找个简单的分布族 $q_\phi(\theta)$ 去**模仿**它，挑一个最像的。
“像不像”用 KL 散度衡量，最大化下面这个 **ELBO**（证据下界）即可：

$$\log p(y)\ge \mathrm{ELBO}=E_{q_\phi}[\log p(y\mid\theta)]-\mathrm{KL}\!\big(q_\phi(\theta)\,\|\,p(\theta)\big).$$

看清楚它的两部分：**第一项是期望似然（数据保真，对应第 5 节），第二项是近似后验与先验的 KL（正则，对应第 7 节的先验）**。
所以变分推断不是新哲学，而是把“似然 + 先验”重新打包成了一个优化问题——和 (a) 的 MAP 是同一家族，只是目标从“取最大”变成“找最像的分布”。

这里面有个工程上的小坎，值得提前知道：ELBO 的期望是对 $q_\phi$ 本身取的，直接对 $\phi$ 求导会因期望依赖 $q$ 而方差爆炸，
所以实际要用**重参数化技巧**（reparameterisation）把随机性挪到固定噪声上，再用自动微分优化——
正是 `贝叶斯变分推断` 那篇教程里“从零实现”时处理的点。而当你运气好、碰到共轭情形（比如世界人口），
学出来的 $q_\phi$ 会和真实后验**严丝合缝地重合**——就像教程里那个“看哪，完美匹配！”的玩具例子，
这一步信任，是后面敢用近似后验的底气。

> 到这里，全仓库的逻辑闭环了：能共轭就直接算（世界人口），算不出就用扩散当先验做 MAP/采样（反问题），
> 或用变分近似整个后验（贝叶斯变分推断）。三者在第 7 节那个乘积公式下统一。

---

## 9. 一句话串起全部

从老忠实泉的抖动开始，我们走了这样一条路：

**数据有噪声 → 用分布描述它（§1）→ 多采样后平均趋近高斯（§2 的 CLT）→ 我们想反推看不见的参数（§3）→
用 MSE/效率评判估计（§4），用最大似然挑最好的参数（§5）→ 还要量化不确定性（§6）→ 贝叶斯把“数据”和“信念”合成后验（§7）→
后验算不出时，要么用扩散模型当学出来的先验做 MAP/采样，要么用变分推断去近似它（§8）。**

口诀：**分布打底，高斯成噪；似然保真，贝叶合成；共轭直解，难则变分或扩散。**

---

## 10. 动手感受一下（Python，对应 `examples/`、`diffusion/` 的 numpy 风格）

读到这里，公式都该“活”起来了。下面几段代码用最普通的 `numpy`/`scipy` 复现讲义里的关键现象，
跑一遍比看十遍都清楚：

```python
import numpy as np
from scipy import stats

rng = np.random.default_rng(0)

# §2 中心极限定理：正态样本均值会收敛成尖峰，Cauchy 永远散着
n = 1000
normal_means = [rng.standard_normal(n).mean() for _ in range(2000)]
cauchy_means = [rng.standard_cauchy(n).mean() for _ in range(2000)]
# normal_means 近似 N(0, 1/sqrt(n))；cauchy_means 仍是重尾、不收窄

# §4 德国坦克：无偏的均值 vs 有偏但更低方差的最大值
def german_tank(theta, m=10, B=5000):
    X = rng.integers(1, theta + 1, size=(B, m))
    T1 = X.mean(axis=1)
    T3 = X.max(axis=1)
    T3_unb = T3 + (T3 - m) / (m + 1)
    return (np.mean(T1) - theta, np.mean(T3_unb) - theta,
            np.var(T1), np.var(T3_unb))

# §7 Poisson–Gamma 共轭：看完数据，先验如何变成后验
alpha0, beta0 = 2.0, 1.0
y = rng.poisson(lam=7.0, size=50)
post = stats.gamma(alpha0 + y.sum(), scale=1.0 / (beta0 + len(y)))
print("后验众数:", (post.args[0] - 1) / post.scale,
      "95% 可信区间:", post.ppf([0.025, 0.975]))
```

> 把最后这个 `post` 想成“扩散 score + 高斯似然”的雏形，你就摸到了 §8(a) 反问题后验采样的门把手。

---

## 11. 考前一页：概念回顾（不是公式表，是因果回顾）

- **为什么有分布**：单次观测不可信，只能用分布刻画不确定性。→ ECDF / KDE / 箱线图是“从数据还原分布”的工具。
- **为什么有 CLT**：独立同分布样本均值必趋高斯（要有有限方差）。→ 这解释了全仓库的噪声为何是高斯的。
- **为什么要推断参数**：我们关心的那个数看不见，只能从数据反推。→ 反问题里这个“参数”就是图像 $x$。
- **怎么评估计**：偏差和方差合成 MSE；Cramér–Rao 给出无偏估计的方差下限。→ MAP 是“有偏但高效”。
- **怎么选参数**：似然把数据当固定、参数当变量，取最大即 MLE。→ 高斯似然的负对数就是数据保真项。
- **怎么量化不确定**：Bootstrap 免公式，置信区间要分已知/未知 $\sigma$（后者用 $t$）。
- **为什么贝叶斯**：把先验信念和数据合成后验，可信区间能直接说“概率”。→ 共轭才能直接算。
- **后验收不到怎么办**：扩散模型当学出来的先验做 MAP/采样；或变分推断用 ELBO 近似后验。两者都还是“似然+先验”。

---

## 12. 收尾：为什么是“概率思维”，而不只是公式

走到这里，你已经把整条链走完了。但我想用最后一句话把格局拉高——这也是那篇概率教程结尾的用意：

**数据科学真正稀缺的不是算出一个答案，而是诚实地知道自己有多不确定。**

- 分布（§1）让你承认单次观测不可信；
- 大数定律与 CLT（§2）告诉你“多采样”和“噪声为什么是高斯的”这两条底层规律；
- 推断与 MLE（§3–§5）给你从数据反推参数的工具；
- 置信/可信区间与 Bootstrap（§6）让你把“不确定”量化出来；
- 贝叶斯（§7–§8）更进一步：把你的**先验信念**和**数据证据**合成一个会随证据进化的后验，而不是只抛出一个孤零零的点。

本仓库那些大部头——反问题、扩散、变分推断——没有发明新数学，它们只是在高维图像上，把这套“量化不确定、随证据更新”的思维反复使用。你一旦用概率的眼光看它们，厚厚的书就会塌缩成一句话：

> **似然是数据怎么来的，先验是你相信什么，后验是两者合一；算得出就直接算，算不出就用扩散或变分去近似。**

记住这个，比背下任何一条公式都值钱。祝你复习顺利。
