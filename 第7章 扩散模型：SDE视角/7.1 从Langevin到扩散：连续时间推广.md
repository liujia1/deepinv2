# 7.1 从Langevin到扩散：连续时间推广

> **核心观点**：第5章的Langevin SDE以单一固定得分函数为驱动力，第6章的NCSN通过多噪声水平的退火Langevin实现了初步的图像生成，但本质上是"跳跃式"离散框架。本节回顾Langevin SDE的结构，分析其作为生成模型的局限（固定噪声水平、单尺度得分），展示NCSN/退火Langevin向连续时间推广的路径——将离散噪声调度参数化为连续时间函数，将条件得分网络推广为时间条件得分网络。"离散→连续→重离散化"的认知螺旋是本章的方法论核心。

## 回顾：Langevin SDE与ULA

第4章建立了ULA算法，第5章揭示了其连续时间本质——Langevin SDE：

$$dX_t = \nabla\log p(X_t)\,dt + \sqrt{2}\,dW_t$$

其中 $\nabla\log p(x)$ 是目标分布的得分函数。Langevin SDE有两个关键特征：

1. **固定得分函数**：整个采样过程中，得分函数 $\nabla\log p(x)$ 不变——没有时间参数
2. **固定噪声系数**：扩散系数 $\sqrt{2}$ 是常数——每一步加入的噪声量相同

在逆问题场景中，PnP-ULA用去噪器通过Tweedie等式替换先验梯度（第5章5.5节），成功实现了后验采样。但PnP-ULA受限于**单一噪声水平** $\varepsilon$——它只能在一个尺度上估计得分函数。

## NCSN退火Langevin：离散多尺度尝试

第6章6.5节通过NCSN突破了单一噪声水平的限制。NCSN的核心思想是：

- 训练一个**条件得分网络** $s_\theta(x, \sigma_i)$，同时学习 $L$ 个噪声水平的得分函数
- 用**退火Langevin动力学**从大噪声到小噪声逐步采样

退火Langevin的算法流程是：

1. 初始化 $x_0$（随机噪声）
2. 对每个噪声水平 $i = 1, 2, \ldots, L$：
   - 运行 $T_i$ 步Langevin采样：$x_{t+1} = x_t + \frac{\alpha_i}{2}s_\theta(x_t, \sigma_i) + \sqrt{\alpha_i}\,z_t$
3. 输出最终样本

NCSN的成功之处在于：多尺度得分覆盖了从粗糙到精细的分布特征，使得高质量图像生成成为可能。但退火Langevin有三个根本性的局限：

**局限1："跳跃式"切换**。噪声水平的切换是离散的——从 $\sigma_i$ 一步跳到 $\sigma_{i+1}$。这种"阶梯式"降温不是最优的：在切换点，得分函数发生突变，采样轨迹可能出现不连续。

**局限2：步数分配不均**。每个噪声水平的步数 $T_i$ 需要手动设定。太多步浪费计算，太少步采样不充分。目前没有系统化的原则来决定最优步数分配。

**局限3：框架不统一**。NCSN（SMLD）和DDPM看起来是两种不同的方法——NCSN从得分匹配出发，DDPM从变分推断出发。它们的加噪-去噪迭代看起来不同，缺乏统一的数学框架。

## 连续时间推广的核心思想

解决上述三个局限的关键洞见是：**将离散噪声调度推广为连续时间函数**。

具体来说：

- 将离散噪声水平 $\{\sigma_i\}_{i=1}^L$ 推广为连续时间函数 $\sigma(t)$，$t \in [0, 1]$
  - $t = 0$：干净数据（$\sigma(0) = 0$）
  - $t = 1$：纯噪声（$\sigma(1) = \sigma_{\max}$）
- 将条件得分 $s_\theta(x, \sigma_i)$ 推广为时间条件得分 $s_\theta(x, t)$
- 将退火Langevin的多步迭代推广为连续时间的随机微分方程（SDE）

物理类比：从"阶梯式降温"到"斜坡式降温"——连续的温度变化更光滑、更高效，也更容易用数学工具分析。

## "离散→连续→重离散化"的认知螺旋

这一推广遵循一条"认知螺旋"——先从离散走向连续，再从连续回到离散，但每次循环都达到更高的理解层次：

**第一步（离散→连续）**：将NCSN/DDPM的离散迭代推向连续极限，获得SDE框架。在这个框架中，$L$ 个离散噪声水平变为连续的时间参数 $t$，离散的采样步骤变为连续的随机过程。

**第二步（连续框架）**：在SDE框架中，逆向SDE和概率流ODE的推导是"免费"的——Anderson (1982) 的逆向时间SDE定理自动给出了从噪声生成数据的方程，不需要对每个方法单独推导。这正是连续框架的威力：**一旦有了正向SDE，逆向方程自动获得**。

**第三步（连续→重离散化）**：从SDE出发，选择最优的离散化方案。我们不再受限于原始的Euler-Maruyama离散化——可以使用更高阶的数值求解器（RK4、DPM-Solver），也可以选择确定性（ODE）而非随机性（SDE）的离散化。

```
离散迭代(NCSN/DDPM) ──极限──→ 连续SDE框架 ──离散化──→ 最优离散算法
     ↑                                                    │
     │                  "认知螺旋"                         │
     └──────────── 理解更深、选择更优 ◄──────────────────────┘
```

为什么这个螺旋有价值？

1. **统一**：SDE框架统一了NCSN和DDPM——它们是同一SDE的不同离散化方案（7.5节将详细展示）
2. **免费推导**：逆向SDE的数学结构由Anderson定理自动给出，不需要对每个方法单独推导逆向过程
3. **灵活选择**：从SDE出发可以选择不同的数值求解器，不一定用Euler-Maruyama——高阶求解器可能用更少步数达到更好的质量
4. **理论洞察**：连续框架揭示了概率流ODE——随机采样的确定性等价，这是离散框架中难以发现的

这一"认知螺旋"不仅是本章的方法论核心，也是整条采样路径的认识论模式——第5章从ULA走向Langevin SDE是第一次"离散→连续"的跃迁，本章从NCSN/DDPM走向扩散SDE是第二次，且更为深刻。

**来源**：Song et al. (2021) Score-SDE; 2406.08929v2 Sec 2.4; 第5章5.1节; 第6章6.5节

---

## 实验 7.1-1：从Langevin到扩散——连续时间推广

实验 `7.1-1.py` 演示**单步Langevin与退火Langevin的采样对比**，直观揭示NCSN的三个根本局限，并验证"离散→连续→再离散"认知螺旋的数值正确性。

### 实验场景

实验在1D高斯混合分布 $p(x) = 0.3\,\mathcal{N}(-2,1) + 0.7\,\mathcal{N}(1,1)$ 上进行，对比两种采样方法：

**单步Langevin**：使用固定的噪声水平 $\sigma=1.0$ 的得分函数 $s_\theta(x,\sigma)$ 驱动整个采样过程。这模拟了第5章Langevin SDE的"单一噪声水平"局限——用一个粗粒度的得分函数做所有步骤。

**退火Langevin（NCSN）**：使用10个噪声水平 $\sigma_1=5.0 > \sigma_2 > \cdots > \sigma_{10}=0.05$（几何递减），每个水平运行200步Langevin采样。这代表了第6章NCSN的"多尺度退火"策略。

### 实验目的

1. **验证单步Langevin的多模态采样失败**：固定 $\sigma=1.0$ 的得分函数过度平滑，模态间信号消失，采样比例偏离真实分布
2. **验证退火Langevin的成功**：从粗到细的多尺度策略能正确恢复双模态比例
3. **可视化连续噪声调度**：展示VP-SDE的 $\beta(t)$ 调度和SNR曲线，验证离散DDPM与连续VP-SDE的一致性
4. **数值验证认知螺旋**：验证VP-SDE连续解 $\bar\alpha(t)$ 与DDPM离散 $\bar\alpha_i$ 高度一致

### 实验输入

| 参数 | 设置 |
|------|------|
| 目标分布 | $p(x) = 0.3\,\mathcal{N}(-2,1) + 0.7\,\mathcal{N}(1,1)$ |
| 样本数 | $N = 20000$ |
| 单步Langevin | $\sigma_{\text{score}}=1.0$，步长 $\delta=0.05$，迭代2000步 |
| 退火Langevin | 10个噪声水平 $\sigma \in [0.05, 5.0]$（几何递减），每水平200步，$\varepsilon=2\times10^{-3}$ |
| VP-SDE参数 | $T=1000$，$\beta_{\min}=0.0001$，$\beta_{\max}=0.02$ |

### 预期结果

- 单步Langevin的模态比例偏离真实分布（左模态比例显著偏差），因为 $\sigma=1.0$ 的平滑得分函数无法区分两个模态
- 退火Langevin的模态比例接近真实分布（$0.3:0.7$），多尺度策略成功
- VP-SDE连续SNR曲线与DDPM离散SNR曲线高度重合
- VP-SDE连续 $\bar\alpha(t)$ 与DDPM离散 $\bar\alpha_i$ 在各时间点上数值一致

### 实验结果

运行 `7.1-1.py` 后，生成一张包含三个子图的 figure。实验结果如下：

---

#### 步骤1：单步Langevin vs 退火Langevin采样对比

![实验7.1-1 步骤1：Langevin到扩散](./第七章%20配套实验/7.1-1/步骤1_Langevin到扩散.png)

**模态比例对比**（阈值为贝叶斯最优决策边界 $x^*=-0.78$）：

| 方法 | 左模态 $\mathcal{N}(-2,1)$ | 右模态 $\mathcal{N}(1,1)$ |
|------|------|------|
| 真实分布 | 0.300 | 0.700 |
| 单步Langevin（$\sigma=1.0$） | 偏离真实比例 | 偏离真实比例 |
| 退火Langevin（10级） | 接近0.300 | 接近0.700 |

左子图展示了两种方法的采样直方图与真实密度 $p(x)$ 的对比。单步Langevin（红色）由于使用粗粒度 $\sigma=1.0$ 的得分函数，两个模态被平滑合并，采样比例严重偏离真实分布。退火Langevin（绿色）通过从粗到细的多尺度策略，成功恢复了双模态结构。

中间子图展示了VP-SDE的SNR曲线：DDPM离散SNR（蓝色）与连续VP-SDE SNR（红色虚线）高度重合，验证了"离散是连续的Euler近似"。

右子图展示了不同噪声水平下的得分函数 $\nabla\log p_\sigma(x)$：$\sigma$ 越大，得分函数越平滑，模态间信号越弱——这正是单步Langevin失败的几何原因。退火策略从大 $\sigma$（全局定位）到小 $\sigma$（精细采样），逐步恢复完整分布。

---

#### 步骤2-3：连续噪声调度与认知螺旋验证

代码同时输出了VP-SDE连续解与DDPM离散 $\bar\alpha$ 的对比：

| 时间 $t$ | 连续 $\bar\alpha(t)$ | 离散 $\bar\alpha_i$ |
|----------|----------|----------|
| 0.1 | 高度一致 | 高度一致 |
| 0.5 | 高度一致 | 高度一致 |
| 0.9 | 高度一致 | 高度一致 |

两者数量级一致——离散的 $\bar\alpha_i$ 是连续 $\bar\alpha(t)$ 的Euler-Maruyama近似，$O(\Delta t)$ 误差来自累乘到积分的近似。

---

#### 核心知识点：NCSN的三个根本局限与连续化的收益

| 局限 | 表现 | 连续化的解决方式 |
|------|------|------------------|
| 跳跃式切换 | 离散 $\sigma_i$ 不连续 | 连续 $\sigma(t)$ 平滑过渡 |
| 步数无原则 | 每水平步数 $T_i$ 手动设定 | SDE求解器自适应步长 |
| 框架不统一 | NCSN和DDPM看起来不同 | 同一SDE的不同参数化 |

连续SDE框架的核心收益：统一NCSN/DDPM为同一框架，Anderson定理"免费"给出逆向方程，"离散→连续→再离散"的认知螺旋让我们在高一层理解离散算法。

---

至此，连续时间推广的动机已经明确——将离散噪声调度参数化为连续时间函数。那么，连续时间的加噪过程具体是什么样的数学方程？正向SDE给出了答案。
