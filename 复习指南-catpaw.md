# 概率论与贝叶斯推断：从直觉到实践的学习指南

> **写给谁看的**：这份指南是为仓库里那些想理解"逆问题求解"和"扩散模型"背后的贝叶斯思想的人写的。我们不从零推导数学，而是讲清楚**为什么**需要这些概念，它们之间是什么关系。

---

## 引言：这本书到底在讲什么

先花一分钟理解这本书的核心主张。

**逆问题**（比如从模糊照片恢复清晰图像）和**图像生成**（比如AI画图）看起来完全不相关。但这本书说：它们是同一个问题。

为什么？因为两者都在求解同一个东西——**后验分布** $p(\mathbf{x}|\mathbf{y})$。逆问题关心的是这个分布的均值或众数（最可能的图像），生成问题关心的是从这个分布里采样（画出可能的图像）。

这个统一的视角就是**贝叶斯框架**。我们这份复习指南要做的，就是把贝叶斯的核心思想讲清楚。

---

## 第一章：概率思维——世界是不确定的

### 1.1 为什么需要概率

想象你有一张模糊的照片，你想恢复它。你只有一个模糊的观测 $\mathbf{y}$，但可能的原始图像 $\mathbf{x}$ 有无数种。怎么选？

这里的关键认识是：**不确定性是固有的**。我们不能得到一个"确定"的答案，但可以描述"哪些答案更可能"。

概率就是描述不确定性的数学工具。

### 1.2 两种随机变量

**离散随机变量**：结果可以数出来。比如抛硬币（正面/反面），掷骰子（1-6）。

**连续随机变量**：结果在一个区间内连续取值。比如图像灰度值（0到1之间任何实数）。

### 1.3 常见的"可能性分布"

自然界和机器学习中，有几类经常出现：

- **高斯分布（正态分布）**：最常见的"钟形曲线"。为什么常见？因为大量独立小扰动叠加后，结果总是趋于高斯（中心极限定理，后面会讲）。图像噪声几乎都是高斯的。

- **伯努利分布**：只有两种结果（0或1）的试验。抛硬币、二分类问题都是这个。

- **泊松分布**：描述"单位时间内事件发生次数"。比如放射性物质衰变的次数、网站的点击量。

- **Gamma分布**：描述"等待时间"。比如等待下一次地震的时间。

- **Beta分布**：描述"概率的概率"。当你不知道一个概率是多少时，用Beta分布来描述你对这个概率的不确定性。

**直觉理解**：每种分布对应一种"世界是怎么运行的"假设。选哪个分布，就是你对问题背后的随机机制做的一个建模选择。

### 1.4 两个数字：期望和方差

**期望**：分布的平均值，长期最可能的结果。

**方差**：结果的"分散程度"。方差大意味着不确定性高。

在逆问题中，如果我们给出一个重建结果，方差大意味着"我们对这个结果不太确定"。这就是**不确定性量化**的核心。

---

## 第二章：从数据到推断——统计的逻辑

### 2.1 什么是统计模型

统计模型的核心思想很简单：**我们有一些数据，想从中推断出背后的规律**。

具体来说：我们假设数据是从某个**概率分布**中产生的，但这个分布的**参数**是未知的。我们的任务就是用数据来估计这些参数。

**例子**：假设图像噪声是高斯分布 $N(0, \sigma^2)$，但我们不知道 $\sigma^2$ 是多少。通过观测多幅含噪图像，我们可以估计 $\sigma^2$。

### 2.2 大数定律——为什么样本均值有用

如果你想知道一个分布的均值，最自然的方法是：抽很多样本，取平均。

**大数定律**告诉你：当样本量足够大时，样本均值会**趋近于真实均值**。

**直觉**：抛一次硬币，结果随机。抛1000次，正面的比例会非常接近0.5。

**局限性**：大数定律要求分布的方差有限。有些分布（比如柯西分布）方差无限，样本均值永远不会收敛。

### 2.3 中心极限定理——为什么高斯无处不在

**中心极限定理**说：大量独立随机变量之和，其分布趋近于高斯分布。

**为什么重要**：
- 图像噪声 = 大量微小扰动的叠加 → 高斯
- 测量误差 = 多个独立误差源的叠加 → 高斯

**这是逆问题中使用高斯噪声假设的理论基础**。

### 2.4 最大似然估计——最"合理"的参数

现在有个问题：我们有一堆数据，怎么估计参数？

**最大似然估计（MLE）**的思路很直接：**选择让观测到的数据出现概率最大的那个参数值**。

**例子**：抛硬币10次，7次正面。哪个正面概率 $p$ 最"合理"？
- 如果 $p=0.5$，出现7次正面的概率是 $0.5^{10} \times \binom{10}{7} \approx 0.117$
- 如果 $p=0.7$，概率是 $0.7^7 \times 0.3^3 \times \binom{10}{7} \approx 0.267$

$p=0.7$ 让观测数据更"可能"，所以MLE选择 $\hat{p}=0.7$。

**推广**：对于逆问题 $\mathbf{y} = \mathbf{A}\mathbf{x} + \boldsymbol{\varepsilon}$，假设噪声 $\boldsymbol{\varepsilon} \sim N(0, \sigma^2 I)$，那么：

$$p(\mathbf{y}|\mathbf{x}) \propto \exp\left(-\frac{\|\mathbf{y} - \mathbf{A}\mathbf{x}\|^2}{2\sigma^2}\right)$$

取对数，最大化似然 = 最小化 $\|\mathbf{y} - \mathbf{A}\mathbf{x}\|^2$（最小二乘）。

**关键洞察**：逆问题中的"数据保真项" $\frac{1}{2\sigma^2}\|\mathbf{y} - \mathbf{A}\mathbf{x}\|^2$，本质上就是负对数似然。它衡量的是"在当前参数 $\mathbf{x}$ 下，观测到数据 $\mathbf{y}$ 有多大的概率"。

---

## 第三章：贝叶斯革命——把不确定性显式表达

### 3.1 频率派的局限

MLE只给出一个点估计（"最可能的值"），但不告诉你"有多不确定"。

比如，MLE告诉你图像某个像素灰度应该是0.5，但它不会告诉你"这个估计靠谱吗？是0.5±0.01（很确定）还是0.5±0.3（很不确定）"。

### 3.2 贝叶斯定理——更新你的信念

**贝叶斯定理**的形式很简单：

$$p(\theta|\text{数据}) \propto p(\text{数据}|\theta) \cdot p(\theta)$$

翻译成人话：

- **先验 $p(\theta)$**：在看数据之前，你对参数 $\theta$ 有什么信念？
- **似然 $p(\text{数据}|\theta)$**：如果参数是 $\theta$，看到这些数据有多大概率？
- **后验 $p(\theta|\text{数据})$**：看了数据之后，你对 $\theta$ 的修正信念。

**直觉理解**：贝叶斯推断就是"用新证据更新旧信念"的过程。

### 3.3 共轭先验——数学上的便利

计算后验时，积分 $p(\text{数据}) = \int p(\text{数据}|\theta)p(\theta)d\theta$ 通常很难算。

**共轭先骚**是一类特殊的先验，它让后验和先验属于同一分布族，积分可以解析算出来。

**常见组合**：
- 伯努利似然 + Beta先验 → Beta后验
- 泊松似然 + Gamma先验 → Gamma后验
- 正态似然 + 正态先验 → 正态后验

**为什么重要**：共轭先验让贝叶斯推断从理论变成可计算。

### 3.4 Beta-Bernoulli模型——直觉的例子

假设你在估计一个硬币正面朝上的概率 $\theta$。

**先验**：你觉得硬币大概公平，但不确定。用Beta(5,5)表示"均值0.5，有一定不确定性"。

**数据**：抛了10次，7次正面。

**后验**：根据贝叶斯定理，后验是Beta(5+7, 5+3) = Beta(12, 8)。

- 后验均值 = 12/(12+8) = 0.6
- 这比你直接MLE估计的0.7要"保守"一些，因为你先验认为0.5更可能

**关键洞察**：先验就像"正则化"，它防止你被少量数据"带偏"。数据越多，后验越倾向于数据；数据越少，后验越倾向于先验。

### 3.5 可信区间 vs 置信区间

这是频率派和贝叶斯派最容易混淆的地方。

**频率派置信区间**："重复实验100次，大约有95次算出的区间会包含真实参数。"（关于区间的陈述）

**贝叶斯可信区间**："参数有95%的概率落在这个区间内。"（关于参数的陈述）

**哪个更有用**？贝叶斯的解释更直觉。当我们说"重建图像的像素值是0.5±0.1，置信度95%"时，我们其实想说"我们认为像素值有99%概率在[0.4, 0.6]之间"——这是可信区间的解释。

---

## 第四章：从贝叶斯到正则化——先验就是约束

### 4.1 Tikhonov正则化的贝叶斯解释

逆问题 $\mathbf{y} = \mathbf{A}\mathbf{x} + \boldsymbol{\varepsilon}$ 通常是**不适定的**——解不唯一，且对噪声敏感。

**Tikhonov正则化**通过加入先验约束来解决这个问题：

$$\hat{\mathbf{x}} = \arg\min_{\mathbf{x}} \|\mathbf{y} - \mathbf{A}\mathbf{x}\|_2^2 + \lambda \|\mathbf{x}\|_2^2$$

**贝叶斯视角**：这个优化问题的解，恰好是以下概率模型的**MAP估计**：

- 似然：$\mathbf{y}|\mathbf{x} \sim N(\mathbf{A}\mathbf{x}, \sigma^2 I)$
- 先验：$\mathbf{x} \sim N(0, \sigma_x^2 I)$
- 正则化参数：$\lambda = \sigma^2 / \sigma_x^2$

**直觉**：先验 $\mathbf{x} \sim N(0, \sigma_x^2 I)$ 表示"我们认为图像像素值倾向于接近0（或小幅度）"。$\sigma_x^2$ 越小，先验越"强"，正则化参数 $\lambda$ 越大，解的平滑性越强。

**关键结论**：正则化参数 $\lambda$ 的比值 $\sigma^2/\sigma_x^2$ 有明确的贝叶斯意义——噪声方差与先验方差的比值。

### 4.2 不同的先验 = 不同的正则化

- **高斯先验** → L2正则化（Tikhonov）→ 解平滑
- **拉普拉斯先验** → L1正则化（LASSO）→ 解稀疏
- **全变差先验** → TV正则化 → 保持边缘

**直觉**：先验编码了"什么样的图像是合理的"的信念。在逆问题中，这种信念就是**正则化**。

### 4.3 后验不确定性

贝叶斯不仅给出点估计（后验均值或众数），还给出**后验协方差**，表示每个像素的不确定度。

**高斯后验**的情况下：

$$\boldsymbol{\Sigma}_{\text{post}} = \sigma^2 (\mathbf{A}^T\mathbf{A} + \lambda \mathbf{I})^{-1}$$

对角线元素就是每个像素的后验方差。方差大的地方，重建结果不确定；方差小的地方，重建结果可靠。

**这是现代逆问题方法（如扩散模型）的核心目标之一**。

---

## 第五章：当后验不可解析——变分推断

### 5.1 共轭的局限

共轭先验虽好，但现实中大多数模型没有共轭形式。比如扩散模型、深度神经网络。

当后验 $p(\mathbf{x}|\mathbf{y})$ 无法解析计算时怎么办？

### 5.2 变分推断的思想

**变分推断（VI）**的思路是：用一个简单的分布 $q_\phi(\mathbf{x})$ 去**近似**真实后验 $p(\mathbf{x}|\mathbf{y})$。

怎么衡量"近似得好"？最小化两者的KL散度：$\text{KL}(q_\phi \| p)$。

直接算这个KL散度需要知道后验（后验正是我们要求的），这形成了鸡生蛋蛋生鸡的问题。

### 5.3 ELBO——变分的目标函数

通过数学变换，最大化 $\text{KL}(q_\phi \| p)$ 等价于最大化**证据下界（ELBO）**：

$$\text{ELBO} = E_{q_\phi}[\ln p(\mathbf{y}|\mathbf{x})] - \text{KL}(q_\phi(\mathbf{x}) \| p(\mathbf{x}))$$

**两项的含义**：
- **第一项**：近似后验下，数据的期望似然（数据保真）
- **第二项**：近似后验与先验的KL散度（正则化）

**直觉理解**：ELBO的两项和正则化逆问题的形式完全一致——数据保真 + 先验约束。

### 5.4 VAE——变分的神经网络实现

**变分自编码器（VAE）**用神经网络来实现变分推断：

- **编码器网络**：输入数据 $\mathbf{x}$，输出变分分布的参数（均值和方差）
- **解码器网络**：从变分分布采样，重构数据
- **重参数化技巧**：让梯度可以反向传播通过随机采样节点

**训练目标**：最大化ELBO，等价于最小化重构误差 + KL正则化。

### 5.5 从VAE到扩散模型

关键洞察：当VAE的层数 $L \to \infty$，编码器退化为高斯加噪过程时，VAE就变成了**扩散模型**。

$$\text{VAE} \xrightarrow{L \to \infty} \text{扩散模型}$$

扩散模型的训练目标（VLB，变分下界）就是VAE的ELBO在高维极限下的形式。

---

## 第六章：采样——从后验中抽取样本

### 6.1 为什么需要采样

贝叶斯推断的目标是整个后验分布，不仅仅是点估计。如果能从后验中采样，我们就能：
- 计算后验均值、方差
- 估计可信区间
- 生成多个可能的解（反映不确定性）

### 6.2 MCMC——用随机游走进后验

**马尔可夫链蒙特卡洛（MCMC）**的基本思想：构造一条随机游走，其稳态分布恰好是目标后验。

**Metropolis-Hastings算法**：
1. 从当前位置 $\mathbf{x}$ 跳到候选位置 $\mathbf{x}'$
2. 根据后验概率比决定是否接受这个跳跃
3. 重复足够多次，访问过的位置就近似后验样本

**问题**：MCMC太慢，每次跳跃需要计算整个数据集上的似然。

### 6.3 Langevin动力学——用梯度加速

**Langevin动力学**的核心思想：后验概率高的地方，得分函数（后验密度的梯度）指向概率更高的方向。

**ULA（Unadjusted Langevin Algorithm）**的迭代公式：

$$\mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} + \delta \nabla_{\mathbf{x}} \ln p(\mathbf{x}^{(k)}|\mathbf{y}) + \sqrt{2\delta} \mathbf{z}^{(k)}$$

其中 $\mathbf{z}^{(k)} \sim N(0, I)$ 是随机噪声。

**直觉**：每一步都沿着后验概率增大的方向走（梯度项），加上一些随机扰动（噪声项）来探索整个分布。

### 6.4 得分函数——概率密度的梯度

**得分函数** $\mathbf{s}(\mathbf{x}) = \nabla_{\mathbf{x}} \ln p(\mathbf{x})$ 指向概率密度增长最快的方向。

**问题**：真实数据分布的得分函数无法直接计算（需要知道归一化常数 $Z$）。

**解决方案——得分匹配**：通过训练一个神经网络 $\mathbf{s}_\theta(\mathbf{x})$ 来近似得分函数，目标函数是：

$$E[\|\mathbf{s}_\theta(\mathbf{x}) - \nabla_{\mathbf{x}} \ln p(\mathbf{x})\|^2]$$

### 6.5 Tweedie等式——去噪 = 学习得分

**Tweedie等式**给出了一个惊人联系：

$$\nabla_{\mathbf{x}} \ln p_\varepsilon(\mathbf{x}) = \frac{\mathbf{D}_\varepsilon(\mathbf{x}) - \mathbf{x}}{\varepsilon}$$

其中 $\mathbf{D}_\varepsilon$ 是一个**去噪器**——输入带噪图像 $\mathbf{x}$，输出去噪后的图像。

**直觉**：去噪器做的事情，恰好就是估计得分函数。去噪器的输出与输入的差，就是概率密度梯度的方向。

### 6.6 扩散模型——多步Langevin

扩散模型可以理解为**多步的Langevin动力学**：

- **前向过程**：逐步加噪，把复杂分布变成简单的高斯分布
- **反向过程**：逐步去噪，从高斯噪声恢复复杂分布（图像）

**反向SDE**：

$$d\mathbf{x} = [f(\mathbf{x}, t) - g^2(t)\nabla_{\mathbf{x}} \ln p_t(\mathbf{x})]dt + g(t)d\bar{\mathbf{W}}$$

**关键**：反向过程需要知道每一步的得分函数 $\nabla_{\mathbf{x}} \ln p_t(\mathbf{x})$，而这正是我们可以用去噪器学到的。

---

## 第七章：回到逆问题——贝叶斯框架下的图像恢复

### 7.1 逆问题的贝叶斯表述

逆问题 $\mathbf{y} = \mathbf{A}\mathbf{x} + \boldsymbol{\varepsilon}$ 的贝叶斯解：

$$p(\mathbf{x}|\mathbf{y}) \propto p(\mathbf{y}|\mathbf{x}) \cdot p(\mathbf{x})$$

- **似然** $p(\mathbf{y}|\mathbf{x})$：噪声模型（通常高斯）
- **先验** $p(\mathbf{x})$：图像的先验知识（可以用扩散模型学习）

### 7.2 MAP估计——点估计

**最大后验估计**：

$$\hat{\mathbf{x}}_{\text{MAP}} = \arg\max_{\mathbf{x}} \ln p(\mathbf{y}|\mathbf{x}) + \ln p(\mathbf{x})$$

- 第一项：数据保真（由噪声模型决定）
- 第二项：先验约束（由图像先验决定）

### 7.3 条件扩散模型——从后验采样

现代方法（如DDRM、DPS）用预训练的扩散模型作为先验，通过修改反向过程的条件来从 $p(\mathbf{x}|\mathbf{y})$ 采样。

**关键**：扩散模型学习的得分函数 $\nabla_{\mathbf{x}} \ln p_t(\mathbf{x})$，恰好就是先验的梯度。加上似然的梯度，就能采样后验。

### 7.4 完整闭环

现在我们可以理解这本书的完整叙事：

1. **贝叶斯定理**统一了逆问题和生成问题
2. **先验 = 正则化**，扩散模型 = 强大的先验学习器
3. **采样路径**（Langevin→扩散）和**变分路径**（VAE→扩散）殊途同归
4. **条件扩散模型** = 用扩散先验求解逆问题

---

## 附录A：常见分布速查

| 分布 | 场景 | 均值 | 方差 |
|------|------|------|------|
| Bernoulli(p) | 二分类 | p | p(1-p) |
| Poisson(λ) | 计数数据 | λ | λ |
| $N(\mu,\sigma^2)$ | 噪声、自然现象 | μ | σ² |
| Exp(λ) | 等待时间 | 1/λ | 1/λ² |
| Gamma(α,β) | 正等待时间 | α/β | α/β² |
| Beta(a,b) | 概率的不确定性 | a/(a+b) | ab/[(a+b)²(a+b+1)] |

---

## 附录B：关键公式速查

**贝叶斯定理**：
$$p(\theta|X) \propto p(X|\theta) \cdot p(\theta)$$

**MLE**：
$$\hat{\theta}_{\text{MLE}} = \arg\max_\theta p(X|\theta)$$

**MAP**：
$$\hat{\theta}_{\text{MAP}} = \arg\max_\theta p(X|\theta) \cdot p(\theta)$$

**Tikhonov正则化（贝叶斯解释）**：
$$\hat{\mathbf{x}} = (\mathbf{A}^T\mathbf{A} + \lambda\mathbf{I})^{-1}\mathbf{A}^T\mathbf{y}, \quad \lambda = \sigma^2/\sigma_x^2$$

**ELBO**：
$$\text{ELBO} = E_{q_\phi}[\ln p(\mathbf{y}|\mathbf{x})] - \text{KL}(q_\phi \| p)$$

**ULA迭代**：
$$\mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} + \delta \nabla \ln p(\mathbf{x}^{(k)}) + \sqrt{2\delta}\mathbf{z}^{(k)}$$

**得分函数**：
$$\mathbf{s}(\mathbf{x}) = \nabla_{\mathbf{x}} \ln p(\mathbf{x})$$

**Tweedie等式**：
$$\nabla_{\mathbf{x}} \ln p_\varepsilon(\mathbf{x}) = \frac{\mathbf{D}_\varepsilon(\mathbf{x}) - \mathbf{x}}{\varepsilon}$$

---

## 附录C：与仓库章节的对应

| 概念 | 仓库位置 |
|------|---------|
| 贝叶斯定理 | 第1章、绪论 |
| MLE与逆问题 | 第1.5-1节实验 |
| 先验与正则化 | 第2章 |
| MCMC/ULA | 第4章 |
| Langevin/得分 | 第5章 |
| 扩散模型(SDE) | 第7章 |
| 变分推断/ELBO | 第8章 |
| VAE | 第9章 |
| 层级VAE | 第10章 |
| 扩散模型(变分) | 第11章 |
| Score≡ELBO | 第12章 |
| 条件生成 | 第13章 |

---

## 学习路径建议

1. **先理解直觉**：这一章在讲什么问题？为什么需要这个工具？
2. **再看数学**：公式是直觉的形式化，不是反过来
3. **动手实验**：跑仓库里的代码，看结果
4. **建立联系**：把新概念和已知概念连接起来

**记忆口诀**：分布打底，高斯成噪；似然保真，贝叶合成；共轭直解，难则变分或扩散。

---

*这份指南是为理解仓库核心内容服务的，强调概念之间的联系而非严格的数学推导。*
