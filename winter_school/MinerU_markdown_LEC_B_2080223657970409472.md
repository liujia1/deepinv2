# PhD Winter School 2023: ADVANCED METHODS for MATHEMATICAL IMAGE ANALYSIS

mail: alessandro.lanza2@unibo.it 

## Alternating Direction Method of Multipliers (ADMM)

(two- and three-blocks) 

From: Boyd et al., Distributed Optimization and Statistical Learning via the Alternating Direction Method of Multipliers, Foundations and Trends in Machine Learning, 2010 

(file "01_ADMM_GENERAL.pdf" in "PAPERS_REPOSITORY" folder) 

ADMM was originally proposed in the mid-1970s by Glowinski and Marrocco [86] and Gabay and Mercier [82]. There are a number of other important papers analyzing the properties of the algorithm, including [76, 81, 75, 87, 157, 80, 65, 33]. In particular, the convergence of ADMM has been explored by many authors, including Gabay [81] and Eckstein and Bertsekas [63]. 

ADMM has also been applied to a number of statistical problems, such as constrained sparse regression [18], sparse signal recovery [70], image restoration and denoising [72, 154, 134], trace norm regularized least squares minimization [174], sparse inverse covariance selection [178], the Dantzig selector [116], and support vector machines [74], among others. For examples in signal processing, see [42, 40, 41, 150, 149] and the references therein. 

From: Boyd et al., Distributed Optimization and Statistical Learning via the Alternating Direction Method of Multipliers, Foundations and Trends in Machine Learning, 2010 

(file "01_ADMM_GENERAL.pdf" in "PAPERS_REPOSITORY" folder) 

Many papers analyzing ADMM do so from the perspective of maximal monotone operators [23, 141, 142, 63, 144]. Briefly, a wide variety of problems can be posed as finding a zero of a maximal monotone operator; for example, if $f$ is closed, proper, and convex, then the subdifferential operator $\partial f$ is maximal monotone, and finding a zero of $\partial f$ is simply minimization of $f$ ; such a minimization may implicitly contain constraints if $f$ is allowed to take the value $+\infty$ . Rockafellar's proximal point algorithm [142] is a general method for finding a zero of a maximal monotone operator, and a wide variety of algorithms have been shown to be special cases, including proximal minimization (see §4.1), the method of multipliers, and ADMM. For a more detailed review of the older literature, see [57, §2]. 

From: Boyd et al., Distributed Optimization and Statistical Learning via the Alternating Direction Method of Multipliers, Foundations and Trends in Machine Learning, 2010 

(file "01_ADMM_GENERAL.pdf" in "PAPERS_REPOSITORY" folder) 

The method of multipliers was shown to be a special case of the proximal point algorithm by Rockafellar [141]. Gabay [81] showed that ADMM is a special case of a method called Douglas-Rachford splitting for monotone operators [53, 112], and Eckstein and Bertsekas [63] showed in turn that Douglas-Rachford splitting is a special case of the proximal point algorithm. (The variant of ADMM that performs an extra $y$ -update between the $x$ - and $z$ -updates is equivalent to Peaceman-Rachford splitting [137, 112] instead, as shown by Glowinski and Le Tallec [87].) 

Prototypical “two-blocks” optimization problem solved by ADMM 

minimize $f(x) + g(y)$ 

subject to $Bx + Cy = c$ 

$$
x \in \mathbb {R} ^ {n}, y \in \mathbb {R} ^ {m}, B \in \mathbb {R} ^ {q \times n}, C \in \mathbb {R} ^ {q \times m}, c \in \mathbb {R} ^ {q}
$$

Prototypical “two-blocks” optimization problem solved by ADMM 

minimize 

$$
f (x) + g (y)
$$

subject to 

$$
B x + C y = c
$$

$$
x \in \mathbb {R} ^ {n}, y \in \mathbb {R} ^ {m}, B \in \mathbb {R} ^ {q \times n}, C \in \mathbb {R} ^ {q \times m}, c \in \mathbb {R} ^ {q}
$$

Main assumption: 

$$
f: \mathbb {R} ^ {n} \to \mathbb {R} \cup \{+ \infty \}, g: \mathbb {R} ^ {m} \to \mathbb {R} \cup \{+ \infty \}
$$

closed, proper and convex 

## Prototypical “two-blocks” optimization problem solved by ADMM

minimize 

$$
f (x) + g (y)
$$

subject to 

$$
B x + C y = c
$$

$$
x \in \mathbb {R} ^ {n}, y \in \mathbb {R} ^ {m}, B \in \mathbb {R} ^ {q \times n}, C \in \mathbb {R} ^ {q \times m}, c \in \mathbb {R} ^ {q}
$$

Main assumption: 

$f:\mathbb{R}^n\to \mathbb{R}\cup \{+\infty \} ,g:\mathbb{R}^m\to \mathbb{R}\cup \{+\infty \} \text{ closed, proper and convex}$ 

- For non-convex problems, convergence is yet an open issue 

- Convex constraints can be dealt with (indicator functions of closed convex sets) 

## Prototypical “two-blocks” optimization problem solved by ADMM

minimize 

$$
f (x) + g (y)
$$

subject to 

$$
B x + C y = c
$$

$$
x \in \mathbb {R} ^ {n}, y \in \mathbb {R} ^ {m}, B \in \mathbb {R} ^ {q \times n}, C \in \mathbb {R} ^ {q \times m}, c \in \mathbb {R} ^ {q}
$$

Main assumption: 

$f:\mathbb{R}^n\to \mathbb{R}\cup \{+\infty \} ,g:\mathbb{R}^m\to \mathbb{R}\cup \{+\infty \} \text{ closed, proper and convex}$ 

- For non-convex problems, convergence is yet an open issue 

- Convex constraints can be dealt with (indicator functions of closed convex sets) 

- Multi-blocks problems can also be dealt with (convergence guaranteed under more restrictive assumptions) 

"three-blocks" optimization problem solved by ADMM 

minimize $f(x) + g(y) + h(z)$ subject to $Bx + Cy + Ez = c$ 

$x \in \mathbb{R}^n$ , $y \in \mathbb{R}^m$ , $z \in \mathbb{R}^p$ , $B \in \mathbb{R}^{q \times n}$ , $C \in \mathbb{R}^{q \times m}$ , $E \in \mathbb{R}^{q \times p}$ , $c \in \mathbb{R}^q$ 

"two-blocks" problem solved by ADMM 

minimize 

subject to 

$$
f (x) + g (y)
$$

$$
B x + C y = c
$$

$$
x \in \mathbb {R} ^ {n}, y \in \mathbb {R} ^ {m}, c \in \mathbb {R} ^ {q}
$$

$$
B \in \mathbb {R} ^ {q \times n}, C \in \mathbb {R} ^ {q \times m}
$$

"two-blocks" problem solved by ADMM 

minimize 

subject to 

$$
f (x) + g (y)
$$

$$
B x + C y = c
$$

$$
x \in \mathbb {R} ^ {n}, y \in \mathbb {R} ^ {m}, c \in \mathbb {R} ^ {q}
$$

$$
B \in \mathbb {R} ^ {q \times n}, C \in \mathbb {R} ^ {q \times m}
$$

The associated augmented Lagrangian function: 

$$
L _ {\beta} (x, y; \lambda) = f (x) + g (y) + \left\langle \lambda , B x + C y - c \right\rangle + \frac {\beta}{2} \left\| B x + C y - c \right\| _ {2} ^ {2}
$$

vector of Lagrange multipliers $\lambda \in R^{q}$ 

penalty parameter $\beta \in R_{++}$ 

"two-blocks" problem solved by ADMM 

minimize 

$$
f (x) + g (y)
$$

subject to 

$$
B x + C y = c
$$

$$
x \in \mathbb {R} ^ {n}, y \in \mathbb {R} ^ {m}, c \in \mathbb {R} ^ {q}
$$

$$
B \in \mathbb {R} ^ {q \times n}, C \in \mathbb {R} ^ {q \times m}
$$

The associated augmented Lagrangian function: 

$$
L _ {\beta} (x, y; \lambda) = f (x) + g (y) + \left\langle \lambda , B x + C y - c \right\rangle + \frac {\beta}{2} \left\| B x + C y - c \right\| _ {2} ^ {2}
$$

Solution of the original minimization problem by seeking for saddle points of $L_{\beta}$ : 

find 

$$
\left\{x ^ {*}, y ^ {*}, \lambda^ {*} \right\}
$$

s.t. 

$$
L _ {\beta} \left(x ^ {*}, y ^ {*}, \lambda\right) \leq L _ {\beta} \left(x ^ {*}, y ^ {*}, \lambda^ {*}\right) \leq L _ {\beta} \left(x, y, \lambda^ {*}\right)
$$

$$
\forall \left\{x ^ {*}, y ^ {*}, \lambda^ {*} \right\} \in \mathbb {R} ^ {n} \times \mathbb {R} ^ {m} \times \mathbb {R} ^ {q}
$$

"two-blocks" problem solved by ADMM 

minimize 

subject to 

$$
f (x) + g (y)
$$

$$
B x + C y = c
$$

$$
x \in \mathbb {R} ^ {n}, y \in \mathbb {R} ^ {m}, c \in \mathbb {R} ^ {q}
$$

$$
B \in \mathbb {R} ^ {q \times n}, C \in \mathbb {R} ^ {q \times m}
$$

The associated augmented Lagrangian function: 

$$
L _ {\beta} (x, y; \lambda) = f (x) + g (y) + \left\langle \lambda , B x + C y - c \right\rangle + \frac {\beta}{2} \left\| B x + C y - c \right\| _ {2} ^ {2}
$$

ADMM iterative algorithm (compute a saddle-point of $L_{\beta}$ ): 

Primal
descent 

(alternating) 

Dual
ascent 

$$
\begin{array}{r l} \boxed {x ^ {(k + 1)}} = & \underset {x \in \mathbb {R} ^ {n}} {\arg \min} L _ {\beta} (x, y ^ {(k)}; \lambda^ {(k)}) \\ y ^ {(k + 1)} = & \underset {y \in \mathbb {R} ^ {m}} {\arg \min} L _ {\beta} (x ^ {(k + 1)}, y; \lambda^ {(k)}) \\ \boxed {\lambda^ {(k + 1)}} = & \lambda^ {(k)} + \beta (B x ^ {(k + 1)} + C y ^ {(k + 1)} - c) \end{array}
$$

"two-blocks" problem solved by ADMM 

minimize 

subject to 

$$
f (x) + g (y)
$$

$$
B x + C y = c
$$

$$
x \in \mathbb {R} ^ {n}, y \in \mathbb {R} ^ {m}, c \in \mathbb {R} ^ {q}
$$

$$
B \in \mathbb {R} ^ {q \times n}, C \in \mathbb {R} ^ {q \times m}
$$

The associated augmented Lagrangian function: 

$$
L _ {\beta} (x, y; \lambda) = f (x) + g (y) - \left\langle \lambda , c - B x - C y) \right\rangle + \frac {\beta}{2} \left\| c - B x - C y \right\| _ {2} ^ {2}
$$

ADMM iterative algorithm (compute a saddle-point of $L_{\beta}$ ): 

Primal
descent
(alternating) 

Dual
ascent 

$$
\begin{array}{r l} {x ^ {(k + 1)}} & {= \underset {x \in \mathbb {R} ^ {n}} {\arg \min} L _ {\beta} (x, y ^ {(k)}; \lambda^ {(k)})} \\ {y ^ {(k + 1)}} & {= \underset {y \in \mathbb {R} ^ {m}} {\arg \min} L _ {\beta} (x ^ {(k + 1)}, y; \lambda^ {(k)})} \\ {\lambda^ {(k + 1)}} & {= \lambda^ {(k)} - \beta (c - B x ^ {(k + 1)} - C y ^ {(k + 1)})} \end{array}
$$

EQUIVALENT 

(we use this) 

"three-blocks" problem solved by ADMM 

subject to 

$$
f (x) + g (y) + h (z)
$$

$$
B x + C y + E z = c
$$

$$
x \in \mathbb {R} ^ {n}, y \in \mathbb {R} ^ {m}, z \in \mathbb {R} ^ {p}, c \in \mathbb {R} ^ {q}
$$

$$
B \in \mathbb {R} ^ {q \times n}, C \in \mathbb {R} ^ {q \times m}, E \in \mathbb {R} ^ {q \times p}
$$

"three-blocks" problem solved by ADMM 

subject to 

$$
f (x) + g (y) + h (z)
$$

$$
B x + C y + E z = c
$$

$$
x \in \mathbb {R} ^ {n}, y \in \mathbb {R} ^ {m}, z \in \mathbb {R} ^ {p}, c \in \mathbb {R} ^ {q}
$$

$$
B \in \mathbb {R} ^ {q \times n}, C \in \mathbb {R} ^ {q \times m}, E \in \mathbb {R} ^ {q \times p}
$$

The associated augmented Lagrangian function: 

$$
L _ {\beta} (x, y, z; \lambda) = f (x) + g (y) - \left\langle \lambda , c - B x - C y - E z \right\rangle + \frac {\beta}{2} \left\| c - B x - C y - E z \right\| _ {2} ^ {2}
$$

"three-blocks" problem solved by ADMM 

$$
f (x) + g (y) + h (z)
$$

subject to 

$$
B x + C y + E z = c
$$

$$
x \in \mathbb {R} ^ {n}, y \in \mathbb {R} ^ {m}, z \in \mathbb {R} ^ {p}, c \in \mathbb {R} ^ {q}
$$

$$
B \in \mathbb {R} ^ {q \times n}, C \in \mathbb {R} ^ {q \times m}, E \in \mathbb {R} ^ {q \times p}
$$

The associated augmented Lagrangian function: 

$$
L _ {\beta} (x, y, z; \lambda) = f (x) + g (y) - \left\langle \lambda , c - B x - C y - E z \right\rangle + \frac {\beta}{2} \left\| c - B x - C y - E z \right\| _ {2} ^ {2}
$$

Solution of the original minimization problem by seeking for saddle points of $L_{\beta}$ : 

find 

$$
\left\{x ^ {*}, y ^ {*}, z ^ {*}, \lambda^ {*} \right\}
$$

s.t. 

$$
L _ {\beta} \left(x ^ {*}, y ^ {*}, z ^ {*}, \lambda\right) \leq L _ {\beta} \left(x ^ {*}, y ^ {*}, z ^ {*}, \lambda^ {*}\right) \leq L _ {\beta} \left(x, y, z, \lambda^ {*}\right)
$$

$$
\forall \left\{x ^ {*}, y ^ {*}, z ^ {*}, \lambda^ {*} \right\} \in \mathbb {R} ^ {n} \times \mathbb {R} ^ {m} \times \mathbb {R} ^ {p} \times \mathbb {R} ^ {q}
$$

"three-blocks" problem solved by ADMM 

subject to 

$$
f (x) + g (y) + h (z)
$$

$$
B x + C y + E z = c
$$

$$
x \in \mathbb {R} ^ {n}, y \in \mathbb {R} ^ {m}, z \in \mathbb {R} ^ {p}, c \in \mathbb {R} ^ {q}
$$

$$
\boldsymbol {B} \in \mathbb {R} ^ {q \times n}, \boldsymbol {C} \in \mathbb {R} ^ {q \times m}, \boldsymbol {E} \in \mathbb {R} ^ {q \times p}
$$

The associated augmented Lagrangian function: 

$$
L _ {\beta} (x, y, z; \lambda) = f (x) + g (y) - \left\langle \lambda , c - B x - C y - E z \right\rangle + \frac {\beta}{2} \left\| c - B x - C y - E z \right\| _ {2} ^ {2}
$$

ADMM iterative algorithm (compute a saddle-point of $L_{\beta}$ ): 

Primal
descent
(alternating) 

Dual
ascent 

$$
\begin{array}{r l} \boxed {x ^ {(k + 1)}} = & \underset {x \in \mathbb {R} ^ {n}} {\arg \min} L _ {\beta} (x, y ^ {(k)}, z ^ {(k)}; \lambda^ {(k)}) \\ y ^ {(k + 1)} = & \underset {y \in \mathbb {R} ^ {m}} {\arg \min} L _ {\beta} (x ^ {(k + 1)}, y, z ^ {(k)}; \lambda^ {(k)}) \\ z ^ {(k + 1)} = & \underset {z \in \mathbb {R} ^ {p}} {\arg \min} L _ {\beta} (x ^ {(k + 1)}, y ^ {(k + 1)}, z; \lambda^ {(k)}) \\ \boxed {\lambda^ {(k + 1)}} = & \lambda^ {(k)} - \beta (c - B x ^ {(k + 1)} - C y ^ {(k + 1)} - E z ^ {(k + 1)}) \end{array}
$$

- Convergence of ADMM in the two- and three-blocks cases has been studied in different manners 

- For the two-blocks case, you can refer to the standard proof given in 

Boyd et al., Distributed Optimization and Statistical Learning via the Alternating Direction Method of Multipliers, Foundations and Trends in Machine Learning, 2010 

(file "01_ADMM_GENERAL.pdf" in "PAPERS_REPOSITORY" folder) 

- For the three-blocks case (for a special situation, with a generalization on the usage of different penalty parameters $\beta$ for different constraints), refer to the proof in 

C. Wu et al., Augmented Lagrangian Method for Total Variation Restoration with Non-quadratic Fidelity, Inverse Problems and Imaging, 2011 

(file "02_TV_NQ_FIDELITY_ADMM.pdf" in "PAPERS_REPOSITORY" folder) 

# Alternating Direction Method of Multipliers (ADMM)

# for the numerical solution of the

(unconstrained) 

TV- $L_{2}$ , TIK- $L_{1}$ , TV- $L_{1}$ models 

## ADMM (two-blocks)

# for the numerical solution of the (unconstrained)

TV-L₂ model 

The unconstrained convex non-smooth model: 

$$
u ^ {*} = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{J (u) = \frac {\mu}{2} \| A u - b \| _ {2} ^ {2} + \sum_ {i = 1} ^ {d} \sqrt {\left(D _ {h} u\right) _ {i} ^ {2} + \left(D _ {v} u\right) _ {i} ^ {2}} \right\}
$$

Variables splitting: 

$$
t = D u \iff \binom{t _ {h}}{t _ {v}} = \binom{D _ {h} u}{D _ {v} u}, t _ {i} \triangleq \binom{t _ {h, i}}{t _ {v, i}} = \binom{\left(D _ {h} u\right) _ {i}}{\left(D _ {v} u\right) _ {i}} \in \mathbb {R} ^ {2}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*} \right\} = \underset {u \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t) = \frac {\mu}{2} \left\| A u - b \right\| _ {2} ^ {2} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\}
$$

subject to (s.t.) t = Du 

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*} \right\} = \underset {u \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t) = \frac {\mu}{2} \big \| A u - b \big \| _ {2} ^ {2} + \sum_ {i = 1} ^ {d} \big \| t _ {i} \big \| _ {2} \right\}\tag{s.t.}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*} \right\} = \underset {u \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t) = \frac {\mu}{2} \left\| A u - b \right\| _ {2} ^ {2} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \mathrm{s.t.} t = D u
$$

It can be equivalently rewritten as: 

$$
\begin{array}{l} \left\{u ^ {*}, t ^ {*} \right\} = \underset {u \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t) = f (u) + g (t) \right\} \text {s.t.} D u + (- I _ {2 d} t) = 0 _ {2 d} \\ \text {with} f (u) = \frac {\mu}{2} \left\| A u - b \right\| _ {2} ^ {2}, g (t) = \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2}, \text {both closed, proper, convex} \end{array}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*} \right\} = \underset {u \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t) = \frac {\mu}{2} \left\| A u - b \right\| _ {2} ^ {2} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \text {s.t.} t = D u
$$

It can be equivalently rewritten as: 

$$
\begin{array}{r l} & {\left\{u ^ {*}, t ^ {*} \right\} = \underset {u \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t) = f (u) + g (t) \right\} \text {s.t.} D u + (- I _ {2 d} t) = 0 _ {2 d}} \\ & {\qquad \text {with} f (u) = \frac {\mu}{2} \big \| A u - b \big \| _ {2} ^ {2}, g (t) = \sum_ {i = 1} ^ {d} \big \| t _ {i} \big \| _ {2}, \text {both closed, proper, convex}} \end{array}
$$

Is it a standard "two-blocks" problem? 

minimize $f(x) + g(y)$ subject to $Bx + Cy = c$ 

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*} \right\} = \underset {u \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t) = \frac {\mu}{2} \left\| A u - b \right\| _ {2} ^ {2} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \mathrm{s.t.} t = D u
$$

It can be equivalently rewritten as: 

$$
\begin{array}{r l} & {\left\{u ^ {*}, t ^ {*} \right\} = \underset {u \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t) = f (u) + g (t) \right\} \mathrm{s.t.} D u + (- I _ {2 d} t) = 0 _ {2 d}} \\ & {\qquad \mathrm{with} \quad f (u) = \frac {\mu}{2} \big \| A u - b \big \| _ {2} ^ {2}, \quad g (t) = \sum_ {i = 1} ^ {d} \big \| t _ {i} \big \| _ {2}, \quad \mathrm{bothclosed,proper,convex}} \end{array}
$$

Is it a standard "two-blocks" problem? 

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/ecb10ee3c008f5446403a0cfa9201888c15027eebb9f1f7aeb1784120c271dcc.jpg)


$$
\begin{array}{l} \text {minimize} f (x) + g (y) \\ \text {subject to} B x + C y = c \end{array} \quad \Longleftrightarrow \quad \boxed {x = u, y = t, B = D, C = - I _ {2 d}, c = 0 _ {2 d}}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*} \right\} = \underset {u \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t) = \frac {\mu}{2} \left\| A u - b \right\| _ {2} ^ {2} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\}
$$

s.t. 

$$
t = D u
$$

The augmented Lagrangian function: 

$$
\lambda = \binom{\lambda_ {h}}{\lambda_ {v}} \in \mathbb {R} ^ {2 d}
$$

vector of Lagrange 

multipliers 

$$
L (u, t; \lambda) = \frac {\mu}{2} \left\| A u - b \right\| _ {2} ^ {2} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , t - D u \right\rangle + \frac {\beta}{2} \left\| t - D u \right\| _ {2} ^ {2}
$$

penalty parameter $\beta > 0$ 

Solution image as the saddle point of the above function, that is: 

find 

$$
\left\{u ^ {*}, t ^ {*}, \lambda^ {*} \right\}
$$

s.t. 

$$
L \left(u ^ {*}, t ^ {*}, \lambda\right) \leq L \left(u ^ {*}, t ^ {*}, \lambda^ {*}\right) \leq L \left(u, t, \lambda^ {*}\right)
$$

$$
\forall (u, t, \lambda) \in \mathbb {R} ^ {d} \times \mathbb {R} ^ {2 d} \times \mathbb {R} ^ {2 d}
$$

Augmented Lagrangian function: 

$$
L (u, t; \lambda) = \frac {\mu}{2} \left\| A u - b \right\| _ {2} ^ {2} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , t - D u \right\rangle + \frac {\beta}{2} \left\| t - D u \right\| _ {2} ^ {2}
$$

saddle point ?? 

ADMM-based iterative algorithm (split optimization sub-problems): 

$$
\begin{array}{c c c c c} \text {Primal} & t ^ {(k + 1)} = & \underset {t \in R ^ {2 d}} {\arg \min} L (u ^ {(k)}, t; \lambda^ {(k)}) & \leftarrow - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - \\ \text {descent} & u ^ {(k + 1)} = & \underset {u \in R ^ {d}} {\arg \min} L (u, t ^ {(k + 1)}; \lambda^ {(k)}) & \leftarrow - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - \\ (\text {alternating}) & \lambda^ {(k + 1)} = & \lambda^ {(k)} - \beta (t ^ {(k + 1)} - D u ^ {(k + 1)}) & \leftarrow -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- \\ \text {Dual} & \lambda^ {(k + 1)} = & \lambda^ {(k)} - \beta (t ^ {(k + 1)} - D u ^ {(k + 1)}) & \leftarrow -- -- -- -- -- -- -- -- -- -- -- -- -- -- \\ \text {ascent} & \end{array}
$$

Augmented Lagrangian function: 

$$
L (u, t; \lambda) = \frac {\mu}{2} \left\| A u - b \right\| _ {2} ^ {2} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , t - D u \right\rangle + \frac {\beta}{2} \left\| t - D u \right\| _ {2} ^ {2}
$$

Subproblem for primal variable t : 

$$
\begin{array}{r l} & t ^ {(k + 1)} = \underset {t \in \mathbb {R} ^ {2 d}} {\arg \min} L (u ^ {(k)}, t; \lambda^ {(k)}) \dots \text {drop the terms which do not depend on} t \dots \\ & = \underset {t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{\sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda^ {(k)}, t - D u ^ {(k)} \right\rangle + \frac {\beta}{2} \left\| t - D u ^ {(k)} \right\| _ {2} ^ {2} \right\} \\ & = \underset {t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{\sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} + \frac {\beta}{2} \left\| t - q ^ {(k)} \right\| _ {2} ^ {2} \right\}, q ^ {(k)} = D u ^ {(k)} + \frac {1}{\beta} \lambda^ {(k)} \in \mathbb {R} ^ {2 d} \end{array}
$$

... the cost function is the sum of $d$ "separate" bivariate functions, in fact: 

$$
t ^ {(k + 1)} = \underset {t \in \mathbb {R} ^ {2 d}} {\arg \min} \sum_ {i = 1} ^ {d} \left\{\left\| t _ {i} \right\| _ {2} + \frac {\beta}{2} \left\| t _ {i} - q _ {i} ^ {(k)} \right\| _ {2} ^ {2} \right\}, \quad q _ {i} ^ {(k)} = \binom{\left(D u ^ {(k)}\right) _ {i} + \frac {1}{\beta} \lambda_ {i} ^ {(k)}}{i = 1, \ldots , d} \in \mathbb {R} ^ {2},
$$

... the problem thus reduces to d independent bivariate minimizations: 

$$
t _ {i} ^ {(k + 1)} = \underset {t _ {i} \in \mathbb {R} ^ {2}} {\arg \min} \left\{\left\| t _ {i} \right\| _ {2} + \frac {\beta}{2} \left\| t _ {i} - q _ {i} ^ {(k)} \right\| _ {2} ^ {2} \right\}, \quad i = 1, \dots , d
$$

... which all admit closed-form solution (see Proposition 1 in the next slide): 

$$
t _ {i} ^ {(k + 1)} = \max \left\{\left\| q _ {i} ^ {(k)} \right\| _ {2} - \frac {1}{\beta}, 0 \right\} \frac {q _ {i} ^ {(k)}}{\left\| q _ {i} ^ {(k)} \right\| _ {2}}, \quad i = 1, \ldots , d
$$

linear computational complexity O(d) 

## Proposition 1 ( proximal map of ||x||₂ )

Let $\alpha \in \mathbb{R}_{++}$ , $v \in \mathbb{R}^2$ be given constants. Then, the optimization problem 

$$
x ^ {*} = \operatorname{prox} _ {\| x \| _ {2}} ^ {\alpha} (v) = \arg \min _ {x \in \mathbb {R} ^ {2}} \left\{\theta (\mathrm{x}; v) = \left\| x \right\| _ {2} + \frac {\alpha}{2} \left\| x - v \right\| _ {2} ^ {2} \right\}
$$

is strongly convex and admits the unique solution given by the following “shrinkage” (or soft-thresholding) operator: 

$$
x ^ {*} = \max \left\{\left\| v \right\| _ {2} - \frac {1}{\alpha}, 0 \right\} \frac {v}{\left\| v \right\| _ {2}}
$$

(where $0 \cdot 0 / 0 = 0$ is assumed) 

Augmented Lagrangian function: 

$$
L (u, t; \lambda) = \frac {\mu}{2} \left\| A u - b \right\| _ {2} ^ {2} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , t - D u \right\rangle + \frac {\beta}{2} \left\| t - D u \right\| _ {2} ^ {2}
$$

Subproblem for primal variable u: 

$$
\begin{array}{l} u ^ {(k + 1)} = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} L (u, t ^ {(k + 1)}; \lambda^ {(k)}) \dots \text {drop the terms which do not depend on} u. \\ = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{Z (u) = \frac {\mu}{2} \big \| A u - b \big \| _ {2} ^ {2} + \left\langle \lambda^ {(k)}, D u \right\rangle + \frac {\beta}{2} \big \| t ^ {(k + 1)} - D u \big \| _ {2} ^ {2} \right\} \end{array}
$$

The function Z is quadratic in the optimization variable u, hence its global minimizers (if there exists one) are to be sought among its stationary points: 

$\boldsymbol{u}^{(k + 1)}\in \mathrm{set~of~solutions~of:}\quad \nabla Z(u) = 0_{d}$ 

... after some simple algebraic manipulations: 

$$
\nabla Z (u) = 0 _ {d} \iff \left(D ^ {T} D + \frac {\mu}{\beta} A ^ {T} A\right) u = D ^ {T} \left(t ^ {(k + 1)} - \frac {1}{\beta} \lambda^ {(k)}\right) + \frac {\mu}{\beta} A ^ {T} b
$$

The coefficient matrix is almost identical to the one previously obtained for the $\mathrm{TIK - L_2}$ model (see...): it is symmetric, positive definite, and has full rank, hence the linear system admits a unique solution giving the new iterate $u^{(k + 1)}$ 

## RESTORATION:

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/2c86b2547f5566f068db422902788fe6d36f363509afb90a13ff4bd7b599e19d.jpg)


Assuming periodic/reflective/anti-reflective boundary conditions for $u$ , the linear system can be solved (like for $\mathrm{TIK - L_2}$ case) by 2D DFT/DCT/DST. By using 2D FFT/FCT/FST implementations $\longrightarrow$ computational complexity O(d log d) 

... after some simple algebraic manipulations: 

$$
\nabla Z (u) = 0 _ {d} \iff \left(D ^ {T} D + \frac {\mu}{\beta} A ^ {T} A\right) u = D ^ {T} \left(t ^ {(k + 1)} - \frac {1}{\beta} \lambda^ {(k)}\right) + \frac {\mu}{\beta} A ^ {T} b
$$

The coefficient matrix is almost identical to the one previously obtained for the $\mathrm{TIK - L_2}$ model (see...): it is symmetric, positive definite, and has full rank, hence the linear system admits a unique solution giving the new iterate $u^{(k + 1)}$ 

## INPAINTING:

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/97b638d22e13cb8728fdffbf1c177c0fa64441ef17009a69c5fa030d6656b11b.jpg)


the linear system can be solved (like for $\mathrm{TIK - L_2}$ case) by iterative (P)CG 

## ADMM (two-blocks)

# for the numerical solution of the (unconstrained)

TIK-L $_{1}$ model 

The unconstrained convex non-smooth model: 

$$
u ^ {*} = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{J (u) = \mu \| A u - b \| _ {1} + \frac {1}{2} \| D u \| _ {2} ^ {2} \right\}
$$

Variables splitting: $r = Au - b \in R^{d}$ … residue image (noise image estimate) 

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, r ^ {*} \right\} = \underset {u \in R ^ {d}, r \in R ^ {d}} {\arg \min} \left\{G (u, r) = \mu \left\| r \right\| _ {1} + \frac {1}{2} \left\| D u \right\| _ {2} ^ {2} \right\}
$$

s.t. 

$$
r = A u - b
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, r ^ {*} \right\} = \underset {u \in R ^ {d}, r \in R ^ {d}} {\arg \min} \left\{G (u, r) = \mu \left\| r \right\| _ {1} + \frac {1}{2} \left\| D u \right\| _ {2} ^ {2} \right\}
$$

s.t. 

$$
r = A u - b
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, r ^ {*} \right\} = \underset {u \in R ^ {d}, r \in R ^ {d}} {\arg \min} \left\{G (u, r) = \mu \| r \| _ {1} + \frac {1}{2} \| D u \| _ {2} ^ {2} \right\} \quad \text {s.t.}
$$

$$
r = A u - b
$$

It can be equivalently rewritten as: 

$$
\begin{array}{r l} & {\left\{u ^ {*}, r ^ {*} \right\} = \underset {u \in R ^ {d}, r \in R ^ {d}} {\arg \min} \left\{G (u, r) = f (u) + g (r) \right\} \text {s.t.} A u + (- I _ {d} r) = b} \\ & {\qquad \mathrm{with} f (u) = \frac {1}{2} \left\| D u \right\| _ {2} ^ {2}, g (r) = \mu \left\| r \right\| _ {1}, \text {both closed, proper, convex}} \end{array}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, r ^ {*} \right\} = \underset {u \in R ^ {d}, r \in R ^ {d}} {\arg \min} \left\{G (u, r) = \mu \left\| r \right\| _ {1} + \frac {1}{2} \left\| D u \right\| _ {2} ^ {2} \right\}
$$

s.t. 

$$
r = A u - b
$$

It can be equivalently rewritten as: 

s.t. 

$$
A u + (- I _ {d} r) = b
$$

with $f(u)=\frac{1}{2}\left\|Du\right\|_{2}^{2},\quad g(r)=\mu\left\|r\right\|_{1},\quad$ both closed, proper, convex 

Is it a standard "two-blocks" problem? 

minimize $f(x) + g(y)$ subject to $Bx + Cy = c$ 

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, r ^ {*} \right\} = \underset {u \in R ^ {d}, r \in R ^ {d}} {\arg \min} \left\{G (u, r) = \mu \left\| r \right\| _ {1} + \frac {1}{2} \left\| D u \right\| _ {2} ^ {2} \right\} \quad \text {s.t.} \quad r = A u - b
$$

It can be equivalently rewritten as: 

$$
\left\{u ^ {*}, r ^ {*} \right\} = \underset {u \in R ^ {d}, r \in R ^ {d}} {\arg \min} \left\{G (u, r) = f (u) + g (r) \right\} \mathrm{s.t.} A u + (- I _ {d} r) = b
$$

with $f(u)=\frac{1}{2}\left\|Du\right\|_{2}^{2},\quad g(r)=\mu\left\|r\right\|_{1},\quad$ both closed, proper, convex 

Is it a standard "two-blocks" problem? YES! 

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/c1139f9c35f6acc9c9a87167e895a07da460bd8dac194bdb5b53274bad9a9b43.jpg)


$$
f (x) + g (y)
$$

subject to 

$$
x = u, y = r, B = A, C = - I _ {d}, c = b
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, r ^ {*} \right\} = \underset {u \in R ^ {d}, r \in R ^ {d}} {\arg \min} \left\{G (u, r) = \mu \left\| r \right\| _ {1} + \frac {1}{2} \left\| D u \right\| _ {2} ^ {2} \right\}
$$

s.t. 

$$
r = A u - b
$$

vector of Lagrange 

$$
\lambda \in \mathbb {R} ^ {d}
$$

The augmented Lagrangian function: 

multipliers 

$$
L (u, r; \lambda) = \mu \left\| r \right\| _ {1} + \frac {1}{2} \left\| D u \right\| _ {2} ^ {2} - \left\langle \lambda , r - (A u - b) \right\rangle + \frac {\beta}{2} \left\| r - (A u - b) \right\| _ {2} ^ {2}
$$

penalty parameter $\beta > 0$ 

Solution image as the saddle point of the above function, that is: 

find 

$$
\left\{u ^ {*}, r ^ {*}, \lambda^ {*} \right\}
$$

s.t. 

$$
L \left(u ^ {*}, r ^ {*}, \lambda\right) \leq L \left(u ^ {*}, r ^ {*}, \lambda^ {*}\right) \leq L \left(u, r, \lambda^ {*}\right)
$$

$$
\forall (u, r, \lambda) \in \mathbb {R} ^ {d} \times \mathbb {R} ^ {d} \times \mathbb {R} ^ {d}
$$

Augmented Lagrangian function: ... saddle point ?? 

$$
L (u, r; \lambda) = \mu \left\| r \right\| _ {1} + \frac {1}{2} \left\| D u \right\| _ {2} ^ {2} - \left\langle \lambda , r - (A u - b) \right\rangle + \frac {\beta}{2} \left\| r - (A u - b) \right\| _ {2} ^ {2}
$$

ADMM-based iterative algorithm (split optimization sub-problems): 

Primal
descent 

(alternating) 

Dual
ascent 

$$
\begin{array}{r c l} r ^ {(k + 1)} & = & \underset {r \in R ^ {d}} {\arg \min} L (u ^ {(k)}, r; \lambda^ {(k)}) \xleftarrow {} \\ u ^ {(k + 1)} & = & \underset {u \in R ^ {d}} {\arg \min} L (u, r ^ {(k + 1)}; \lambda^ {(k)}) \xleftarrow {} \\ \lambda^ {(k + 1)} & = & \lambda^ {(k)} - \beta   (r ^ {(k + 1)} - (A u ^ {(k + 1)} - b)) \end{array}
$$

closed-form
proximal map 

s.p.d. linear system 

closed-form 

Augmented Lagrangian function: 

$$
L (u, r; \lambda) = \mu \| r \| _ {1} + \frac {1}{2} \| D u \| _ {2} ^ {2} - \left\langle \lambda , r - (A u - b) \right\rangle + \frac {\beta}{2} \| r - (A u - b) \| _ {2} ^ {2}
$$

Subproblem for primal variable r : 

$$
\begin{array}{r l} & r ^ {(k + 1)} = \underset {r \in \mathbb {R} ^ {d}} {\arg \min} L (u ^ {(k)}, r; \lambda^ {(k)}) \dots \text {drop the terms which do not depend on} r \dots \\ & \qquad = \underset {r \in \mathbb {R} ^ {d}} {\arg \min} \left\{\mu \left\| r \right\| _ {1} - \left\langle \lambda^ {(k)}, r - (A u ^ {(k)} - b) \right\rangle + \frac {\beta}{2} \left\| r - (A u ^ {(k)} - b) \right\| _ {2} ^ {2} \right\} \\ & \qquad = \underset {r \in \mathbb {R} ^ {d}} {\arg \min} \left\{\left\| r \right\| _ {1} + \frac {\gamma}{2} \left\| r - q ^ {(k)} \right\| _ {2} ^ {2} \right\}, \quad q ^ {(k)} = A u ^ {(k)} - b + \frac {1}{\beta} \lambda^ {(k)} \in \mathbb {R} ^ {d} \\ & \qquad \qquad \gamma = \beta / \mu \in \mathbb {R} _ {+ +} \end{array}
$$

... the cost function is the sum of $d$ "separate" univariate functions, in fact: 

$$
r ^ {(k + 1)} = \underset {r \in \mathbb {R} ^ {d}} {\arg \min} \sum_ {i = 1} ^ {d} \left\{\left| r _ {i} \right| + \frac {\gamma}{2} \left(r _ {i} - q _ {i} ^ {(k)}\right) ^ {2} \right\}, \quad q _ {i} ^ {(k)} = \left(\left(A u ^ {(k)} - b\right) _ {i} + \frac {1}{\beta} \lambda_ {i} ^ {(k)}\right) \in \mathbb {R}, i = 1, \ldots , d
$$

... the problem thus reduces to d independent univariate minimizations: 

$$
r _ {i} ^ {(k + 1)} = \underset {r _ {i} \in \mathbb {R}} {\arg \min} \left\{\left| r _ {i} \right| + \frac {\gamma}{2} \Big (r _ {i} - q _ {i} ^ {(k)} \Big) ^ {2} \right\}, \quad i = 1, \dots , d
$$

... which all admit closed-form solution (see Proposition 2 in the next slide): 

$$
r _ {i} ^ {(k + 1)} = \mathrm{sign} \left(q _ {i} ^ {(k)}\right) \max \left\{\left| q _ {i} ^ {(k)} \right| - \frac {1}{\gamma}, 0 \right\}, \quad i = 1, \ldots , d
$$

linear computational complexity O(d) 

## Proposition 2 ( proximal map of |x| )

Let $\alpha \in \mathbb{R}_{++}$ , $v \in \mathbb{R}$ be given constants. Then, the optimization problem 

$$
x ^ {*} = \operatorname{prox} _ {| x |} ^ {\alpha} (v) = \arg \min _ {x \in \mathbb {R}} \left\{\theta (\mathrm{x}; v) = | x | + \frac {\alpha}{2} (x - v) ^ {2} \right\}
$$

is strongly convex and admits the unique solution given by the following “shrinkage” (or soft-thresholding) operator: 

$$
x ^ {*} = \operatorname{sign} (v) \cdot \max \left\{\left| v \right| - \frac {1}{\alpha}, 0 \right\}
$$

where $\operatorname{sign}(v) = -1$ for v < 0, +1 for v > 0, 0 for v = 0 

Augmented Lagrangian function: 

$$
L (u, r; \lambda) = \mu \left\| r \right\| _ {1} + \frac {1}{2} \left\| D u \right\| _ {2} ^ {2} - \left\langle \lambda , r - (A u - b) \right\rangle + \frac {\beta}{2} \left\| r - (A u - b) \right\| _ {2} ^ {2}
$$

Subproblem for primal variable u: 

$$
\begin{array}{l} u ^ {(k + 1)} = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} L (u, r ^ {(k + 1)}; \lambda^ {(k)}) \dots \text {drop the terms which do not depend on} u. \\ = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{Z (u) = \frac {1}{2} \left\| D u \right\| _ {2} ^ {2} + \left\langle \lambda^ {(k)}, A u \right\rangle + \frac {\beta}{2} \left\| r ^ {(k + 1)} - (A u - b) \right\| _ {2} ^ {2} \right\} \end{array}
$$

The function Z is quadratic in the optimization variable u, hence its global minimizers (if there exists one) are to be sought among its stationary points: 

$u^{(k + 1)}\in$ set of solutions of: $\nabla Z(u) = 0_{d}$ 

... after some simple algebraic manipulations: 

$$
\nabla Z (u) = 0 _ {d} \iff \left(\frac {1}{\beta} D ^ {T} D + A ^ {T} A\right) u = A ^ {T} \left(r ^ {(k + 1)} - \frac {1}{\beta} \lambda^ {(k)} + b\right)
$$

The coefficient matrix is almost identical to the one previously obtained for the $\mathrm{TIK - L_2}$ and $\mathrm{TV - L_2}$ models (see...): it is s. p. d. and has full rank, hence the linear system admits a unique solution giving the new iterate $u^{(k + 1)}$ 

## RESTORATION:

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/52daad2f5b5d2dda0f6a93c3c3a21c4e4d654b5a53b040b2d72b6e2043f1f7b0.jpg)


Assuming periodic/reflective/anti-reflective boundary conditions for u, the linear system can be solved (like for $TIK-L_{2}$ case) by 2D DFT/DCT/DST. By using 2D FFT/FCT/FST implementations $\longrightarrow$ computational complexity O(d log d) 

... after some simple algebraic manipulations: 

$$
\nabla Z (u) = 0 _ {d} \iff \left(\frac {1}{\beta} D ^ {T} D + A ^ {T} A\right) u = A ^ {T} \left(r ^ {(k + 1)} - \frac {1}{\beta} \lambda^ {(k)} + b\right)
$$

The coefficient matrix is almost identical to the one previously obtained for the $\mathrm{TIK - L_2}$ and $\mathrm{TV - L_2}$ models (see...): it is s. p. d. and has full rank, hence the linear system admits a unique solution giving the new iterate $u^{(k + 1)}$ 

## INPAINTING:

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/5af2bde4b6e583ccb6f8283cb2579e2a9e1c9729800ec0ee142533ab930786c7.jpg)


the linear system can be solved (like for $\mathrm{TIK - L_2}$ case) by iterative (P)CG 

ADMM (three-blocks → two-blocks) 

# for the numerical solution of the (unconstrained)

TV-L₁ model 

The unconstrained convex non-smooth model: 

$$
u ^ {*} \in \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{J (u) = \mu \| A u - b \| _ {1} + \sum_ {i = 1} ^ {d} \sqrt {\left(D ^ {(h)} u\right) _ {i} ^ {2} + \left(D ^ {(v)} u\right) _ {i} ^ {2}} \right\}
$$

Variables splitting: 

$$
\begin{array}{r l} & t = D u \iff \binom {t _ {h}} {t _ {v}} = \binom {D _ {h} u} {D _ {v} u}, t _ {i} \triangleq \binom {t _ {h, i}} {t _ {v, i}} = \binom {\left(D _ {h} u\right) _ {i}} {\left(D _ {v} u\right) _ {i}} \in \mathbb {R} ^ {2} \\ & r = A u - b \in \mathbb {R} ^ {d} \end{array}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \text {s.t.} t = D u, r = A u - b
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \mathrm{s.t.} t = D u, r = A u - b
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \text {s.t.} t = D u, r = A u - b
$$

It can be equivalently rewritten as: 

$$
\begin{array}{l} \left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = f (u) + g (t) + h (r) \right\} \\ \text {s.t.} \binom {D} {A} u + \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t + \binom {0 _ {2 d \times d}} {- I _ {d}} r = \binom {0 _ {2 d}} {b} \text {with} \begin{array}{c} f (u) = 0, g (t) = \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2}, h (r) = \mu \left\| r \right\| _ {1}, \\ \text {all closed, proper, convex} \end{array} \end{array}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \text {s.t.} t = D u, r = A u - b
$$

It can be equivalently rewritten as: 

$$
\begin{array}{l} \left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = f (u) + g (t) + h (r) \right\} \\ \text {s.t.} \binom {D} {A} u + \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t + \binom {0 _ {2 d \times d}} {- I _ {d}} r = \binom {0 _ {2 d}} {b} \text {with} \begin{array}{c} f (u) = 0, g (t) = \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2}, h (r) = \mu \left\| r \right\| _ {1}, \\ \text {all closed, proper, convex} \end{array} \end{array}
$$

Is it a standard "three-blocks" problem? 

minimize $f(x) + g(y) + h(z)$ 

subject to $Bx + Cy + Ez = c$ 

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \mathrm{s.t.} t = D u, r = A u - b
$$

It can be equivalently rewritten as: 

$$
\begin{array}{l} \left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = f (u) + g (t) + h (r) \right\} \\ \text {s.t.} \binom {D} {A} u + \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t + \binom {0 _ {2 d \times d}} {- I _ {d}} r = \binom {0 _ {2 d}} {b} \text {with} \begin{array}{c} f (u) = 0, g (t) = \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2}, h (r) = \iota_ {B (\delta)} (r), \\ \text {all closed, proper, convex} \end{array} \end{array}
$$

Is it a standard "three-blocks" problem? 

subject to 

YES! 

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/f59d26494bf406a9b38997743644169e9662ebcb74dc3a8689f2993d662a4892.jpg)


Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \mathrm{s.t.} t = D u, r = A u - b
$$

It can be equivalently rewritten as: 

$$
\begin{array}{l} \left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = f (u) + g (t) + h (r) \right\} \\ \text {s.t.} \binom {D} {A} u + \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t + \binom {0 _ {2 d \times d}} {- I _ {d}} r = \binom {0 _ {2 d}} {b} \text {with} \begin{array}{c} f (u) = 0, g (t) = \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2}, h (r) = \iota_ {B (\delta)} (r), \\ \text {all closed, proper, convex} \end{array} \end{array}
$$

## Can it also be seen as a standard “two-blocks” YES! problem?

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/077b75710e706a2d1a459b2013e8a881a75496b259b56c7b307ba02b04921b20.jpg)


$$
\begin{array}{c} \text {problem?} \\ \boxed {\text {minimize} f (x) + g (y)} \\ \text {subject to} B x + C y = c \end{array} \quad \leftarrow \quad \begin{array}{c} x = u, y = (t; r), \\ g (y) = \dots , B = \dots , C = \dots , c = \dots \end{array}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \text {s.t.} t = D u, r = A u - b
$$

The augmented Lagrangian function: 

$$
\begin{array}{l} L (u, t, r; \lambda) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , c - B u - C t - E r \right\rangle + \frac {\beta}{2} \left\| c - B u - C t - E r \right\| _ {2} ^ {2} \\ = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , \binom {0 _ {2 d}} {b} - \binom {D} {A} u - \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t - \binom {0 _ {2 d \times d}} {- I _ {d}} r \right\rangle + \frac {\beta}{2} \left\| \binom {0 _ {2 d}} {b} - \binom {D} {A} u - \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t - \binom {0 _ {2 d \times d}} {- I _ {d}} r \right\| _ {2} ^ {2} \end{array}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \text {s.t.} t = D u, r = A u - b
$$

The augmented Lagrangian function: 

$$
\begin{array}{l} L (u, t, r; \lambda) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , c - B u - C t - E r \right\rangle + \frac {\beta}{2} \left\| c - B u - C t - E r \right\| _ {2} ^ {2} \\ = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , \binom {0 _ {2 d}} {b} - \binom {D} {A} u - \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t - \binom {0 _ {2 d \times d}} {- I _ {d}} r \right\rangle + \frac {\beta}{2} \left\| \binom {0 _ {2 d}} {b} - \binom {D} {A} u - \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t - \binom {0 _ {2 d \times d}} {- I _ {d}} r \right\| _ {2} ^ {2} \\ \lambda \triangleq \binom {\lambda_ {t}} {\lambda_ {r}}, \lambda_ {t} \in \mathbb {R} ^ {2 d}, \lambda_ {r} \in \mathbb {R} ^ {d}, \lambda \in \mathbb {R} ^ {3 d} \end{array}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \text {s.t.} t = D u, r = A u - b
$$

The augmented Lagrangian function: 

$$
\begin{array}{l} L (u, t, r; \lambda) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , c - B u - C t - E r \right\rangle + \frac {\beta}{2} \left\| c - B u - C t - E r \right\| _ {2} ^ {2} \\ = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , \binom {0 _ {2 d}} {b} - \binom {D} {A} u - \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t - \binom {0 _ {2 d \times d}} {- I _ {d}} r \right\rangle + \frac {\beta}{2} \left\| \binom {0 _ {2 d}} {b} - \binom {D} {A} u - \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t - \binom {0 _ {2 d \times d}} {- I _ {d}} r \right\| _ {2} ^ {2} \\ L (u, t, r; \lambda_ {t}, \lambda_ {r}) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda_ {t}, t - D u \right\rangle + \frac {\beta}{2} \left\| t - D u \right\| _ {2} ^ {2} - \left\langle \lambda_ {r}, r - (A u - b) \right\rangle + \frac {\beta}{2} \left\| r - (A u - b) \right\| _ {2} ^ {2} \end{array}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \text {s.t.} t = D u, r = A u - b
$$

The augmented Lagrangian function: 

$$
\begin{array}{l} L (u, t, r; \lambda) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , c - B u - C t - E r \right\rangle + \frac {\beta}{2} \left\| c - B u - C t - E r \right\| _ {2} ^ {2} \\ = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , \binom {0 _ {2 d}} {b} - \binom {D} {A} u - \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t - \binom {0 _ {2 d \times d}} {- I _ {d}} r \right\rangle + \frac {\beta}{2} \left\| \binom {0 _ {2 d}} {b} - \binom {D} {A} u - \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t - \binom {0 _ {2 d \times d}} {- I _ {d}} r \right\| _ {2} ^ {2} \\ L (u, t, r; \lambda_ {t}, \lambda_ {r}) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda_ {t}, t - D u \right\rangle + \frac {\beta}{2} \left\| t - D u \right\| _ {2} ^ {2} - \left\langle \lambda_ {r}, r - (A u - b) \right\rangle + \frac {\beta}{2} \left\| r - (A u - b) \right\| _ {2} ^ {2} \end{array}
$$

- It is like if we “Lagrange-augment the two constraints t = Du, r = Au-b separately” 

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \text {s.t.} t = D u, r = A u - b
$$

The augmented Lagrangian function: 

$$
\begin{array}{l} L (u, t, r; \lambda) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , c - B u - C t - E r \right\rangle + \frac {\beta}{2} \left\| c - B u - C t - E r \right\| _ {2} ^ {2} \\ = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , \binom {0 _ {2 d}} {b} - \binom {D} {A} u - \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t - \binom {0 _ {2 d \times d}} {- I _ {d}} r \right\rangle + \frac {\beta}{2} \left\| \binom {0 _ {2 d}} {b} - \binom {D} {A} u - \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t - \binom {0 _ {2 d \times d}} {- I _ {d}} r \right\| _ {2} ^ {2} \\ L (u, t, r; \lambda_ {t}, \lambda_ {r}) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda_ {t}, t - D u \right\rangle + \frac {\beta_ {t}}{2} \left\| t - D u \right\| _ {2} ^ {2} - \left\langle \lambda_ {r}, r - (A u - b) \right\rangle + \frac {\beta_ {r}}{2} \left\| r - (A u - b) \right\| _ {2} ^ {2} \end{array}
$$

- It is like if we “Lagrange-augment the two constraints t = Du, r = Au-b separately” 

- It is possible (and we do it) to use two different $\beta$ values for the two constraints 

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \text {s.t.} t = D u, r = A u - b
$$

The augmented Lagrangian function: 

$$
\begin{array}{l} L (u, t, r; \lambda) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , c - B u - C t - E r \right\rangle + \frac {\beta}{2} \left\| c - B u - C t - E r \right\| _ {2} ^ {2} \\ = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda , \binom {0 _ {2 d}} {b} - \binom {D} {A} u - \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t - \binom {0 _ {2 d \times d}} {- I _ {d}} r \right\rangle + \frac {\beta}{2} \left\| \binom {0 _ {2 d}} {b} - \binom {D} {A} u - \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t - \binom {0 _ {2 d \times d}} {- I _ {d}} r \right\| _ {2} ^ {2} \\ L (u, t, r; \lambda_ {t}, \lambda_ {r}) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda_ {t}, t - D u \right\rangle + \frac {\beta_ {t}}{2} \left\| t - D u \right\| _ {2} ^ {2} - \left\langle \lambda_ {r}, r - (A u - b) \right\rangle + \frac {\beta_ {r}}{2} \left\| r - (A u - b) \right\| _ {2} ^ {2} \end{array}
$$

- It is like if we “Lagrange-augment the two constraints t = Du, r = Au-b separately” 

- It is possible (and we do it) to use two different $\beta$ values for the two constraints 

- Convergence of this “three-blocks ADMM” with different $\beta$ values has been proved in 

C. Wu et al., Augmented Lagrangian Method for Total Variation Restoration with Non-quadratic Fidelity, Inverse Problems and Imaging, 2011 (file “02_TV_NQ_FIDELITY_ADMM” in REPOSITORY) 

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \text {s.t.} t = D u, r = A u - b
$$

like for TV-L $_{2}$ 

The augmented Lagrangian function: 

like for $\mathsf{TIK - L_1}$ 

$$
\begin{array}{c} L (u, t, r; \lambda_ {t}, \lambda_ {r}) = \mu \| r \| _ {1} + \sum_ {i = 1} ^ {d} \| t _ {i} \| _ {2} - \left\langle \lambda_ {t}, t - D u \right\rangle + \frac {\beta_ {t}}{2} \| t - D u \| _ {2} ^ {2} \\ - \left\langle \lambda_ {r}, r - (A u - b) \right\rangle + \frac {\beta_ {r}}{2} \| r - (A u - b) \| _ {2} ^ {2} \end{array}
$$

Solution image as the saddle point of the above function, that is: 

$$
\begin{array}{r l} \text {find} \left\{u ^ {*}, t ^ {*}, r ^ {*}, \lambda_ {t} ^ {*}, \lambda_ {r} ^ {*} \right\} & \text {s.t.} L \big (u ^ {*}, t ^ {*}, r ^ {*}, \lambda_ {t}, \lambda_ {r} \big) \leq L \big (u ^ {*}, t ^ {*}, r ^ {*}, \lambda_ {t} ^ {*}, \lambda_ {r} ^ {*} \big) \leq L \big (u, t, r, \lambda_ {t} ^ {*}, \lambda_ {r} ^ {*} \big) \\ & \forall (u, t, r, \lambda_ {t}, \lambda_ {r}) \in \mathbb {R} ^ {d} \times \mathbb {R} ^ {2 d} \times \mathbb {R} ^ {d} \times \mathbb {R} ^ {2 d} \times \mathbb {R} ^ {d} \end{array}
$$

Saddle point of the Augmented Lagrangian function? 

ADMM-based iterative algorithm (split optimization sub-problems): 

$$
\begin{array}{l l l l} \text {Primal descent (alternating)} & t ^ {(k + 1)} & = & \underset {t \in \mathbb {R} ^ {2 d}} {\arg \min} L (u ^ {(k)}, t, r ^ {(k)}; \lambda_ {t} ^ {(k)}, \lambda_ {r} ^ {(k)}) \\ & r ^ {(k + 1)} & = & \underset {r \in \mathbb {R} ^ {d}} {\arg \min} L (u ^ {(k)}, t ^ {(k + 1)}, r; \lambda_ {t} ^ {(k)}, \lambda_ {r} ^ {(k)}) \\ & u ^ {(k + 1)} & = & \underset {u \in \mathbb {R} ^ {d}} {\arg \min} L (u, t ^ {(k + 1)}, r ^ {(k + 1)}; \lambda_ {t} ^ {(k)}, \lambda_ {r} ^ {(k)}) \\ \text {Dual ascent} & \lambda_ {t} ^ {(k + 1)} & = & \lambda_ {t} ^ {(k)} - \beta_ {t} (t ^ {(k + 1)} - D u ^ {(k + 1)}) \\ & \lambda_ {r} ^ {(k + 1)} & = & \lambda_ {r} ^ {(k)} - \beta_ {r} (r ^ {(k + 1)} - (A u ^ {(k + 1)} - b)) \end{array}
$$

Augmented Lagrangian function: 

$$
\begin{array}{r l} & L (u, t, r; \lambda_ {t}, \lambda_ {r}) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda_ {t}, t - D u \right\rangle + \frac {\beta_ {t}}{2} \left\| t - D u \right\| _ {2} ^ {2} \\ & \text {   problem   for   primal   variable   } t: \end{array}
$$

$t^{(k + 1)} = \underset {t\in \mathbb{R}^{2d}}{\arg \min}L(u^{(k)},t,r^{(k)};\lambda_{t}^{(k)},\lambda_{r}^{(k)})$ 

... exactly the same as for TV- $L_{2}$ ... 

... closed-form solution (see Proposition 1) of d independent 2D problems: 

$$
\begin{array}{l} t _ {i} ^ {(k + 1)} = \underset {t _ {i} \in \mathbb {R} ^ {2}} {\arg \min} \left\{\left\| t _ {i} \right\| _ {2} + \frac {\beta_ {t}}{2} \left\| t _ {i} - q _ {i} ^ {(k)} \right\| _ {2} ^ {2} \right\}, \quad q _ {i} ^ {(k)} = \left(\left(D u ^ {(k)}\right) _ {i} + \frac {1}{\beta_ {t}} \lambda_ {t, i} ^ {(k)}\right) \in \mathbb {R} ^ {2}, \\ = \max \left\{\left\| q _ {i} ^ {(k)} \right\| _ {2} - \frac {1}{\beta_ {t}}, 0 \right\} \frac {q _ {i} ^ {(k)}}{\left\| q _ {i} ^ {(k)} \right\| _ {2}}, \quad i = 1, \ldots , d \quad \text {linear computational complexity O(d)} \end{array}
$$

Augmented Lagrangian function: 

$$
\begin{array}{r l} & L (u, t, r; \lambda_ {t}, \lambda_ {r}) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda_ {t}, t - D u \right\rangle + \frac {\beta_ {t}}{2} \left\| t - D u \right\| _ {2} ^ {2} \\ & \text {problem for primal variable r:} \end{array}
$$

$$
r ^ {(k + 1)} = \underset {r \in \mathbb {R} ^ {d}} {\arg \min} L (u ^ {(k)}, t ^ {(k + 1)}, r; \lambda_ {t} ^ {(k)}, \lambda_ {r} ^ {(k)})
$$

... exactly the same as for $\mathrm{TIK - L_1}$ ... 

... closed-form solution (see Proposition 2) of d independent 1D problems: 

$$
\begin{array}{l} r _ {i} ^ {(k + 1)} = \underset {r _ {i} \in \mathbb {R}} {\arg \min} \Bigg \{\big | r _ {i} \big | + \frac {\gamma}{2} \Big (r _ {i} - w _ {i} ^ {(k)} \Big) ^ {2} \Bigg \}, \quad w _ {i} ^ {(k)} = \left(\left(A u ^ {(k)} - b\right) _ {i} + \frac {1}{\beta_ {r}} \lambda_ {r, i} ^ {(k)}\right) \in \mathbb {R}, \\ = \operatorname{sign} \left(w _ {i} ^ {(k)}\right) \operatorname * {m a x} \Bigg \{\left| w _ {i} ^ {(k)} \right| - \frac {1}{\gamma}, 0 \Bigg \}, \quad i = 1, \ldots , d \quad \text {linear computational complexity O(d)} \end{array}
$$

Augmented Lagrangian function: 

$$
\begin{array}{r l} & L (u, t, r; \lambda_ {t}, \lambda_ {r}) = \mu \left\| r \right\| _ {1} + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda_ {t}, t - D u \right\rangle + \frac {\beta_ {t}}{2} \left\| t - D u \right\| _ {2} ^ {2} \\ & - \left\langle \lambda_ {r}, r - (A u - b) \right\rangle + \frac {\beta_ {r}}{2} \left\| r - (A u - b) \right\| _ {2} ^ {2} \\ & \text {Subproblem for primal variable u:} \end{array}
$$

$u^{(k + 1)} = \arg \min_{u\in \mathbb{R}^d}L(u,t^{(k + 1)},r^{(k + 1)};\lambda_t^{(k)},\lambda_r^{(k)})$ ... drop the terms independent of $u$ ... 

$$
= \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{Z (u) = \left\langle \lambda_ {t} ^ {(k)}, D u \right\rangle + \frac {\beta_ {t}}{2} \left\| t ^ {(k + 1)} - D u \right\| _ {2} ^ {2} + \left\langle \lambda_ {r} ^ {(k)}, A u \right\rangle + \frac {\beta_ {r}}{2} \left\| r ^ {(k + 1)} - (A u - b) \right\| _ {2} ^ {2} \right\}
$$

The function Z is quadratic in the optimization variable u, hence its global minimizers (if there exists one) are to be sought among its stationary points: 

$u^{(k + 1)}\in$ set of solutions of: $\nabla Z(u) = 0_{d}$ 

... after some simple algebraic manipulations: 

$$
\nabla Z (u) = 0 _ {d} \quad \Leftrightarrow \quad \left(D ^ {T} D + \frac {\beta_ {r}}{\beta_ {t}} A ^ {T} A\right) u = D ^ {T} \left(t ^ {(k + 1)} - \frac {1}{\beta_ {t}} \lambda_ {t} ^ {(k)}\right) + \frac {\beta_ {r}}{\beta_ {t}} A ^ {T} \left(r ^ {(k + 1)} - \frac {1}{\beta_ {r}} \lambda_ {r} ^ {(k)} + b\right)
$$

The coefficient matrix is almost identical to the one previously obtained for the $\mathrm{TIK - L_2}$ , TV- $\mathrm{L}_2$ and $\mathrm{TIK - L_1}$ models (see...): it is s. p. d. and has full rank, hence the linear system admits a unique solution giving the new iterate $u^{(k + 1)}$ 

## RESTORATION:

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/fff51601c79e01741167b51edc01de8217d7abbb01f60927f04de74af51d395d.jpg)


Assuming periodic/reflective/anti-reflective boundary conditions for $u$ , the linear system can be solved (like for $\mathrm{TIK - L_2}$ case) by 2D DFT/DCT/DST. By using 2D FFT/FCT/FST implementations $\longrightarrow$ computational complexity O(d log d) 

... after some simple algebraic manipulations: 

$$
\nabla Z (u) = 0 _ {d} \quad \Leftrightarrow \quad \left(D ^ {T} D + \frac {\beta_ {r}}{\beta_ {t}} A ^ {T} A\right) u = D ^ {T} \left(t ^ {(k + 1)} - \frac {1}{\beta_ {t}} \lambda_ {t} ^ {(k)}\right) + \frac {\beta_ {r}}{\beta_ {t}} A ^ {T} \left(r ^ {(k + 1)} - \frac {1}{\beta_ {r}} \lambda_ {r} ^ {(k)} + b\right)
$$

The coefficient matrix is almost identical to the one previously obtained for the $\mathrm{TIK - L_2}$ , TV- $\mathrm{L}_2$ and $\mathrm{TIK - L_1}$ models (see...): it is s. p. d. and has full rank, hence the linear system admits a unique solution giving the new iterate $u^{(k + 1)}$ 

## INPAINTING:

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/796fe56e628685b03806752af200b5f983bece21cfceb4a3e41184df546dd034.jpg)


the linear system can be solved (like for $\mathrm{TIK - L_2}$ case) by iterative (P)CG 

ADMM (three-blocks → two-blocks) 

# for the numerical solution of the ("discrepancy"-constrained)

TV-L₂ model 

- Discrepancy Principle (DP) definition: for any variational model of the form: $u^{*}(\mu) = \arg\min_{u \in \mathbb{R}^{d}} \left\{ J(u; \mu) = R(u) + \frac{\mu}{2} \left\| A u - b \right\|_{2}^{2} \right\}$ , with $R$ any regularizer,
and with: 

$$
A = K \in \mathbb {R} ^ {d \times d}
$$

blurring matrix (for image restoration) 

$$
A = S \in \mathbb {R} ^ {d \times d}
$$

inpainting matrix (for image inpainting) 

- Discrepancy Principle (DP) definition: for any variational model of the form: 

$u^{*}(\mu) = \arg \min_{u\in \mathbb{R}^{d}}\bigg\{J(u;\mu) = R(u) + \frac{\mu}{2}\left\| Au - b\right\|_{2}^{2}\bigg\},$ with $R$ any regularizer, 

choose $\mu$ such that the solution $u^{*}(\mu)$ satisfies the discrepancy constraint: 

$u^{*}(\mu)\in \mathcal{D}(\delta)\triangleq \left\{u\in \mathbb{R}^{d}\colon \left\| Au - b\right\|_{2}^{2}\leq \delta^{2}\right\} ,\delta = \delta (\tau) = \tau \sqrt{d}\hat{\sigma}_{n},\tau \simeq 1,\hat{\sigma}_{n}\mathrm{est.of noise}\mathrm{stdv}$ 

- DP rationale: we aim at solutions $u^{*}(\mu)$ which are as near as possible to the sought clean image, $u_{true}$ , which, according to the linear degradation model, satisfies: 

$$
b = A u _ {t r u e} + n \Rightarrow A u _ {t r u e} - b = n \Rightarrow \left\| A u _ {t r u e} - b \right\| _ {2} ^ {2} = \left\| n \right\| _ {2} ^ {2} \cong d \sigma_ {n} ^ {2} = \delta^ {2} (\tau) \mathrm{with} \tau = 1
$$

Hence, by DP we impose that the residual of $u^{*}(\mu)$ has the same variance of noise 

- DP usefulness: tuning (by hand) the best (in terms of obtained restoration results) regularization parameter $\mu$ of unconstrained models can be a long and tedious task. If we are able to compute a good estimate $\hat{\sigma}_n$ of noise standard deviation, then imposing directly the discrepancy constraint – i.e., using discrepancy-constrained variational models – allows to obtain in one shot a good restoration! 

The Unconstrained (U) and (discrepancy) Constrained (C) TV- $L_{2}$ models: 

$$
\begin{array}{l} u _ {U} ^ {*} (\mu) = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{J _ {U} (u; \mu) = \mathrm{TV} (u) + \frac {\mu}{2} \left\| A u - b \right\| _ {2} ^ {2} \right\} \dots \text {seen in previous slides} \\ u _ {C} ^ {*} (\delta) = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{J _ {C} (u; \delta) = \mathrm{TV} (u) + \iota_ {\mathcal {D} (\delta)} (u) \right\}, \mathcal {D} (\delta) = \left\{u \in \mathbb {R} ^ {d}: \left\| A u - b \right\| _ {2} ^ {2} \leq \delta^ {2} \right\} \\ \quad \text {Discrepancy} \\ \text {set / constraint} \\ \quad \text {hyper - ellipsoidal} \\ \text {shape} \end{array}
$$

The Unconstrained (U) and (discrepancy) Constrained (C) TV- $L_{2}$ models: 

$u_{U}^{*}\left(\mu\right)=\arg\min_{u\in\mathbb{R}^{d}}\left\{J_{U}(u;\mu)=\mathrm{TV}(u)+\frac{\mu}{2}\left\|\boldsymbol{A}\boldsymbol{u}-\boldsymbol{b}\right\|_{2}^{2}\right\}\quad\ldots$ seen in previous slides $u_{C}^{*}\left(\delta\right)=\arg\min_{u\in\mathbb{R}^{d}}\left\{J_{C}(u;\delta)=\mathrm{TV}(u)+\iota_{\mathcal{D}(\delta)}(u)\right\},\quad\mathcal{D}(\delta)=\left\{u\in\mathbb{R}^{d}\colon\left\|\boldsymbol{A}\boldsymbol{u}-\boldsymbol{b}\right\|_{2}^{2}\leq\delta^{2}\right\}$ where the indicator function $\iota_{S}(x)$ of a set S is defined as $\iota_{S}(x)=\begin{cases}0&if x\in S\\+\infty&if x\notin S\end{cases}$ ,

such that the constrained model above can also be equivalently written as $u_{C}^{*}\left(\delta\right)=\arg\min_{u\in\mathcal{D}(\delta)}\mathrm{TV}(u),\quad\mathcal{D}(\delta)=\left\{u\in\mathbb{R}^{d}\colon\left\|\boldsymbol{A}\boldsymbol{u}-\boldsymbol{b}\right\|_{2}^{2}\leq\delta^{2}\right\}\quad←$ discrepancy set

discrepancy constraint

For our purposes, it is convenient to consider the form with the indicator function! 

The Unconstrained (U) and (discrepancy) Constrained (C) TV- $L_{2}$ models: 

$$
\begin{array}{l} u _ {U} ^ {*} (\mu) = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{J _ {U} (u; \mu) = \mathrm{TV} (u) + \frac {\mu}{2} \left\| A u - b \right\| _ {2} ^ {2} \right\} \dots \text {seen in previous slides} \\ u _ {C} ^ {*} (\delta) = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{J _ {C} (u; \delta) = \mathrm{TV} (u) + \iota_ {\mathcal {D} (\delta)} (u) \right\}, \mathcal {D} (\delta) = \left\{u \in \mathbb {R} ^ {d}: \left\| A u - b \right\| _ {2} ^ {2} \leq \delta^ {2} \right\} \end{array}
$$

The constrained model can be equivalently and usefully rewritten (by rewriting in an equivalent form the discrepancy constraint) as follows: 

$$
u _ {C} ^ {*} (\delta) = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{J _ {C} (u; \delta) = \operatorname{TV} (u) + \iota_ {B (\delta)} (A u - b) \right\}, B (\delta) = \left\{x \in \mathbb {R} ^ {d}: \| x \| _ {2} ^ {2} \leq \delta^ {2} \right\}
$$

where $B(\delta)$ is the $l_{2}$ ball of $R^{d}$ with center the origin and radius $\delta$ 

The Unconstrained (U) and (discrepancy) Constrained (C) TV- $L_{2}$ models: 

$$
\begin{array}{l} u _ {U} ^ {*} (\mu) = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{J _ {U} (u; \mu) = \mathrm{TV} (u) + \frac {\mu}{2} \| A u - b \| _ {2} ^ {2} \right\} \dots \text {seen in previous slides} \\ u _ {C} ^ {*} (\delta) = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{J _ {C} (u; \delta) = \mathrm{TV} (u) + \iota_ {B (\delta)} (A u - b) \right\}, B (\delta) = \left\{x \in \mathbb {R} ^ {d}: \| x \| _ {2} ^ {2} \leq \delta^ {2} \right\} \end{array}
$$

Motivation for the constrained model (numerically more challenging): 

If we know (or we are able to estimate) the noise standard deviation $\sigma_{n}$ , then the above constrained model (unlike the unconstrained one) allows us to automatically obtain a good-quality solution $u_{C}^{*}(\delta)$ by selecting $\delta = \tau \sqrt{d}\sigma_{n}$ , with $\tau \simeq 1$ , as in this way we are imposing that the standard deviation of the solution residual image $r_{C}^{*}(\delta) = Ku_{C}^{*}(\delta) - b$ is approximately equal to the noise standard deviation $\sigma_{n}$ ... 

... this is called the DISCREPANCY PRINCIPLE 

The Unconstrained (U) and (discrepancy) Constrained (C) TV- $L_{2}$ models: 

$$
\begin{array}{l} u _ {U} ^ {*} (\mu) = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{J _ {U} (u; \mu) = \mathrm{TV} (u) + \frac {\mu}{2} \left\| A u - b \right\| _ {2} ^ {2} \right\} \dots \text {seen in previous slides} \\ u _ {C} ^ {*} (\delta) = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{J _ {C} (u; \delta) = \mathrm{TV} (u) + \iota_ {B (\delta)} (A u - b) \right\}, B (\delta) = \left\{x \in \mathbb {R} ^ {d}: \left\| x \right\| _ {2} ^ {2} \leq \delta^ {2} \right\} \end{array}
$$

“Equivalence” of the unconstrained and constrained models 

The (discrepancy) constrained convex non-smooth model: 

$$
u ^ {*} = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{J (u; \delta) = \iota_ {B (\delta)} (A u - b) + \sum_ {i = 1} ^ {d} \sqrt {(D _ {h} u) _ {i} ^ {2} + (D _ {v} u) _ {i} ^ {2}} \right\},
$$

with : 

$B(\delta) = \left\{x \in \mathbb{R}^d : \| x \|_2^2 \leq \delta^2\right\}, l^2$ ball in $\mathbb{R}^d$ with center the origin and radius $\delta$ 

Variables splitting: 

$$
\begin{array}{r l} & t = D u \iff \binom {t _ {h}} {t _ {v}} = \binom {D _ {h} u} {D _ {v} u}, t _ {i} \triangleq \binom {t _ {h, i}} {t _ {v, i}} = \binom {\left(D _ {h} u\right) _ {i}} {\left(D _ {v} u\right) _ {i}} \in \mathbb {R} ^ {2} \\ & r = A u - b \in \mathbb {R} ^ {d} \end{array}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \iota_ {B (\delta)} (r) + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \mathrm{s.t.} t = D u, r = A u - b
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \iota_ {B (\delta)} (r) + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \text {s.t.} t = D u, r = A u - b
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \iota_ {B (\delta)} (r) + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \mathrm{s.t.} t = D u, r = A u - b
$$

It can be equivalently rewritten as: 

$$
\begin{array}{l} \left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = f (u) + g (t) + h (r) \right\} \\ \text {s.t.} \binom {D} {A} u + \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t + \binom {0 _ {2 d \times d}} {- I _ {d}} r = \binom {0 _ {2 d}} {b} \text {with} \begin{array}{c} f (u) = 0, g (t) = \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2}, h (r) = \iota_ {B (\delta)} (r), \\ \text {all closed, proper, convex} \end{array} \end{array}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \iota_ {B (\delta)} (r) + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \mathrm{s.t.} t = D u, r = A u - b
$$

It can be equivalently rewritten as: 

$$
\begin{array}{l} \left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = f (u) + g (t) + h (r) \right\} \\ \text {s.t.} \binom {D} {A} u + \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t + \binom {0 _ {2 d \times d}} {- I _ {d}} r = \binom {0 _ {2 d}} {b} \text {with} \begin{array}{c} f (u) = 0, g (t) = \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2}, h (r) = \iota_ {B (\delta)} (r), \\ \text {all closed, proper, convex} \end{array} \end{array}
$$

Is it a standard "three-blocks" problem? 

minimize $f(x) + g(y) + h(z)$ 

subject to $Bx + Cy + Ez = c$ 

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \iota_ {B (\delta)} (r) + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \text {s.t.} t = D u, r = A u - b
$$

It can be equivalently rewritten as: 

$$
\begin{array}{l} \left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = f (u) + g (t) + h (r) \right\} \\ \text {s.t.} \binom {D} {A} u + \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t + \binom {0 _ {2 d \times d}} {- I _ {d}} r = \binom {0 _ {2 d}} {b} \text {with} \begin{array}{c} f (u) = 0, g (t) = \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2}, h (r) = \iota_ {B (\delta)} (r), \\ \text {all closed, proper, convex} \end{array} \end{array}
$$

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/7fe3cd9933f00b48ac7a5e3490e47045d0cff44e11125578ef3f92cc95ef6c9a.jpg)


$$
\begin{array}{l} \text {Is it a standard} \\ \text {``three - blocks'' problem?} \\ \boxed {\text {minimize} f (x) + g (y) + h (z)} \\ \text {subject to} B x + C y + E z = c \end{array} \quad \begin{array}{l} \text {YES!} \\ \boxed {x = u, y = t, z = r,} \\ B = \binom {D} {A}, C = \binom {- I _ {2 d}} {0 _ {d \times 2 d}}, E = \binom {0 _ {2 d \times d}} {- I _ {d}}, c = \binom {0 _ {2 d}} {b} \end{array}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \iota_ {B (\delta)} (r) + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \text {s.t.} t = D u, r = A u - b
$$

It can be equivalently rewritten as: 

$$
\begin{array}{l} \left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = f (u) + g (t) + h (r) \right\} \\ \text {s.t.} \binom {D} {A} u + \binom {- I _ {2 d}} {0 _ {d \times 2 d}} t + \binom {0 _ {2 d \times d}} {- I _ {d}} r = \binom {0 _ {2 d}} {b} \text {with} \begin{array}{c} f (u) = 0, g (t) = \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2}, h (r) = \iota_ {B (\delta)} (r), \\ \text {all closed, proper, convex} \end{array} \end{array}
$$

## Can it also be seen as a standard “two-blocks” YES! problem?

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/6c4105af3d9b276ed71894147c84ef8f3680892936863a5fe096844fcf04548a.jpg)


$$
\begin{array}{c} \text {problem?} \\ \boxed {\text {minimize} f (x) + g (y)} \\ \text {subject to} B x + C y = c \end{array} \quad \leftarrow \quad \begin{array}{c} x = u, y = (t; r), \\ g (y) = \dots , B = \dots , C = \dots , c = \dots \end{array}
$$

Split (linearly constrained) model: 

$$
\left\{u ^ {*}, t ^ {*}, r ^ {*} \right\} \in \underset {u, r \in \mathbb {R} ^ {d}, t \in \mathbb {R} ^ {2 d}} {\arg \min} \left\{G (u, t, r) = \iota_ {B (\delta)} (r) + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} \right\} \mathrm{s.t.} t = D u, r = A u - b
$$

The augmented Lagrangian function: 

like for TV-L₂ like for TV-L₁ 

like for $\mathsf{TIK - L_1}$ 

$$
\begin{array}{r} L (u, t, r; \lambda_ {t}, \lambda_ {r}) = \iota_ {B (\delta)} (r) + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda_ {t}, t - D u \right\rangle + \frac {\beta_ {t}}{2} \left\| t - D u \right\| _ {2} ^ {2} \\ - \left\langle \lambda_ {r}, r - (A u - b) \right\rangle + \frac {\beta_ {r}}{2} \left\| r - (A u - b) \right\| _ {2} ^ {2} \end{array}
$$

Restored image as the saddle point of the above function, that is: 

$$
\begin{array}{r l} \text {find} \left\{u ^ {*}, t ^ {*}, r ^ {*}, \lambda^ {*}, \lambda_ {r} ^ {*} \right\} & \text {s.t.} L \big (u ^ {*}, t ^ {*}, r ^ {*}, \lambda_ {t}, \lambda_ {r} \big) \leq L \big (u ^ {*}, t ^ {*}, r ^ {*}, \lambda^ {*}, \lambda_ {r} ^ {*} \big) \leq L \big (u, t, r, \lambda^ {*}, \lambda_ {r} ^ {*} \big) \\ & \forall (u, t, r, \lambda_ {t}, \lambda_ {r}) \in \mathbb {R} ^ {d} \times \mathbb {R} ^ {2 d} \times \mathbb {R} ^ {d} \times \mathbb {R} ^ {2 d} \times \mathbb {R} ^ {d} \end{array}
$$

Saddle point of the Augmented Lagrangian function? 

ADMM-based iterative algorithm (split optimization sub-problems): 

$$
\begin{array}{l l l} \text {Primal descent alternating)} & t ^ {(k + 1)} = \underset {t \in \mathbb {R} ^ {2 d}} {\arg \min} L (u ^ {(k)}, t, r ^ {(k)}; \lambda_ {t} ^ {(k)}, \lambda_ {r} ^ {(k)}) \\ & r ^ {(k + 1)} = \underset {r \in \mathbb {R} ^ {d}} {\arg \min} L (u ^ {(k)}, t ^ {(k + 1)}, r; \lambda_ {t} ^ {(k)}, \lambda_ {r} ^ {(k)}) \\ & u ^ {(k + 1)} = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} L (u, t ^ {(k + 1)}, r ^ {(k + 1)}; \lambda_ {t} ^ {(k)}, \lambda_ {r} ^ {(k)}) \\ & \lambda_ {t} ^ {(k + 1)} = \lambda_ {t} ^ {(k)} - \beta_ {t} (t ^ {(k + 1)} - D u ^ {(k + 1)}) \\ & \lambda_ {r} ^ {(k + 1)} = \lambda_ {r} ^ {(k)} - \beta_ {r} (r ^ {(k + 1)} - (A u ^ {(k + 1)} - b)) \end{array}
$$

Augmented Lagrangian function: 

$$
\begin{array}{r l} & L (u, t, r; \lambda_ {t}, \lambda_ {r}) = \iota_ {B (\delta)} (r) + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda_ {t}, t - D u \right\rangle + \frac {\beta_ {t}}{2} \left\| t - D u \right\| _ {2} ^ {2} \\ & \text {ubproblem for primal variable t:} \end{array}
$$

$$
t ^ {(k + 1)} = \underset {t \in \mathbb {R} ^ {2 d}} {\arg \min} L (u ^ {(k)}, t, r ^ {(k)}; \lambda_ {t} ^ {(k)}, \lambda_ {r} ^ {(k)})
$$

... exactly the same as for TV- $L_{1,2}$ ... 

... closed-form solution (see Proposition 1) of d independent 2D problems: 

$$
\begin{array}{l} t _ {i} ^ {(k + 1)} = \underset {t _ {i} \in \mathbb {R} ^ {2}} {\arg \min} \left\{\left\| t _ {i} \right\| _ {2} + \frac {\beta_ {t}}{2} \left\| t _ {i} - q _ {i} ^ {(k)} \right\| _ {2} ^ {2} \right\}, \quad q _ {i} ^ {(k)} = \left(\left(D u ^ {(k)}\right) _ {i} + \frac {1}{\beta_ {t}} \lambda_ {t, i} ^ {(k)}\right) \in \mathbb {R} ^ {2}, \\ = \max \left\{\left\| q _ {i} ^ {(k)} \right\| _ {2} - \frac {1}{\beta_ {t}}, 0 \right\} \frac {q _ {i} ^ {(k)}}{\left\| q _ {i} ^ {(k)} \right\| _ {2}}, \quad i = 1, \ldots , d \quad \text {linear computational complexity O(d)} \end{array}
$$

Augmented Lagrangian function: 

$$
\begin{array}{r l} & L (u, t, r; \lambda_ {t}, \lambda_ {r}) = \iota_ {B (\delta)} (r) + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda_ {t}, t - D u \right\rangle + \frac {\beta_ {t}}{2} \left\| t - D u \right\| _ {2} ^ {2} \\ & \text {ubproblem for primal variable r:} \end{array}
$$

$$
\begin{array}{r l} r ^ {(k + 1)} & = \underset {r \in \mathbb {R} ^ {d}} {\arg \min} L (u ^ {(k)}, t ^ {(k + 1)}, r; \lambda_ {t} ^ {(k)}, \lambda_ {r} ^ {(k)}) \dots \text {drop the terms independent of} r \dots \\ & = \underset {r \in \mathbb {R} ^ {d}} {\arg \min} \left\{\iota_ {B (\delta)} (r) - \left\langle \lambda_ {r} ^ {(k)}, r - (A u ^ {(k)} - b) \right\rangle + \frac {\beta_ {r}}{2} \| r - (A u ^ {(k)} - b) \| _ {2} ^ {2} \right\} \\ & = \underset {r \in \mathbb {R} ^ {d}} {\arg \min} \left\{\iota_ {B (\delta)} (r) + \frac {\beta_ {r}}{2} \| r - w ^ {(k)} \| _ {2} ^ {2} \right\}, w ^ {(k)} = K A u ^ {(k)} - b + \frac {1}{\beta_ {r}} \lambda_ {r} ^ {(k)} \in \mathbb {R} ^ {d} \\ & = \underset {r \in B (\delta)} {\arg \min} \| r - w ^ {(k)} \| _ {2} = \prod_ {B (\delta)} (w ^ {(k)}) \quad \text {Euclidean (orthogonal) projection of vector w^{(k)} onto the l_{2} ball of radius \delta} \end{array}
$$

Euclidean (orthogonal) projection of vector $w^{(k)}$ onto the $l_2$ ball of radius $\delta$ : 

$$
\boxed {r ^ {(k + 1)}} = \underset {r \in B (\delta)} {\arg \min} \left\| r - w ^ {(k)} \right\| _ {2} = \prod_ {B (\delta)} \left(w ^ {(k)}\right),
$$

with 

$$
B (\delta) = \left\{x \in \mathbb {R} ^ {d}: \| x \| _ {2} \leq \delta \right\}
$$

The $I_{2}$ ball is a convex (compact) set, hence the projection $r^{(k+1)}$ exists and is unique, 

and admits a simple closed-form expression : 

$$
\boxed {r ^ {(k + 1)}} = \left\{ \begin{array}{c c c} & w ^ {(k)} & \text {if} \quad \left\| w ^ {(k)} \right\| _ {2} \leq \delta \\ \frac {\delta}{\left\| w ^ {(k)} \right\| _ {2}} & w ^ {(k)} & \text {if} \quad \left\| w ^ {(k)} \right\| _ {2} > \delta \end{array} \right.\tag{1}
$$

$$
r ^ {(k + 1)} = \min \left\{\frac {\delta}{\left\| w ^ {(k)} \right\| _ {2}}, 1 \right\} w ^ {(k)}\tag{2}
$$

linear computational complexity O(d) 

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/39b0068ff7b3b616513de44e9a2e4452c20943b08836a73ad923af78a140ef49.jpg)


Augmented Lagrangian function: 

$$
\begin{array}{r l} & L (u, t, r; \lambda_ {t}, \lambda_ {r}) = \iota_ {B (\delta)} (r) + \sum_ {i = 1} ^ {d} \left\| t _ {i} \right\| _ {2} - \left\langle \lambda_ {t}, t - D u \right\rangle + \frac {\beta_ {t}}{2} \left\| t - D u \right\| _ {2} ^ {2} \\ & \text {Subproblem for primal variable u:} \quad - \left\langle \lambda_ {r}, r - (A u - b) \right\rangle + \frac {\beta_ {r}}{2} \left\| r - (A u - b) \right\| _ {2} ^ {2} \end{array}
$$

$$
\begin{array}{l} u ^ {(k + 1)} = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} L (u, t ^ {(k + 1)}, r ^ {(k + 1)}; \lambda_ {t} ^ {(k)}, \lambda_ {r} ^ {(k)}) \quad \dots \text {exactly the same as for TV - L_{1}} \dots \\ = \underset {u \in \mathbb {R} ^ {d}} {\arg \min} \left\{Z (u) = \left\langle \lambda_ {t} ^ {(k)}, D u \right\rangle + \frac {\beta_ {t}}{2} \left\| t ^ {(k + 1)} - D u \right\| _ {2} ^ {2} + \left\langle \lambda_ {r} ^ {(k)}, A u \right\rangle + \frac {\beta_ {r}}{2} \left\| r ^ {(k + 1)} - (A u - b) \right\| _ {2} ^ {2} \right\} \end{array}
$$

The function Z is quadratic in the optimization variable u, hence its global minimizers (if there exists one) are to be sought among its stationary points: 

$\boldsymbol{u}^{(k + 1)}\in \mathrm{set~of~solutions~of:}\nabla Z(u) = 0_{d}$ 

... after some simple algebraic manipulations: 

$$
\nabla Z (u) = 0 _ {d} \quad \Leftrightarrow \quad \left(D ^ {T} D + \frac {\beta_ {r}}{\beta_ {t}} A ^ {T} A\right) u = D ^ {T} \left(t ^ {(k + 1)} - \frac {1}{\beta_ {t}} \lambda_ {t} ^ {(k)}\right) + \frac {\beta_ {r}}{\beta_ {t}} A ^ {T} \left(r ^ {(k + 1)} - \frac {1}{\beta_ {r}} \lambda_ {r} ^ {(k)} + b\right)
$$

The linear system is exactly the same as the one in the subproblem for u for model TV- $L_{1}$ : it admits a unique solution giving the new iterate $u^{(k+1)}$ 

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/4f183d52eaa9ad5bceed75b8b102a432f60b8a3045a809fec6437b18a6008979.jpg)


## RESTORATION:

Assuming periodic/reflective/anti-reflective boundary conditions for $u$ , the linear system can be solved (like for $\mathrm{TIK - L_2}$ case) by 2D DFT/DCT/DST. By using 2D FFT/FCT/FST implementations $\longrightarrow$ computational complexity O(d log d) 

... after some simple algebraic manipulations: 

$$
\nabla Z (u) = 0 _ {d} \quad \Leftrightarrow \quad \left(D ^ {T} D + \frac {\beta_ {r}}{\beta_ {t}} A ^ {T} A\right) u = D ^ {T} \left(t ^ {(k + 1)} - \frac {1}{\beta_ {t}} \lambda_ {t} ^ {(k)}\right) + \frac {\beta_ {r}}{\beta_ {t}} A ^ {T} \left(r ^ {(k + 1)} - \frac {1}{\beta_ {r}} \lambda_ {r} ^ {(k)} + b\right)
$$

The linear system is exactly the same as the one in the subproblem for u for model TV- $L_{1}$ : it admits a unique solution giving the new iterate $u^{(k+1)}$ 

![image](https://cdn-mineru.openxlab.org.cn/result/2026-07-23/58c6b038-6490-42e4-88fb-96eb4ff298b0/e708afdcdb38a8283a7cb53935172927e9bdbddeb17662acc3c96f55e477e36f.jpg)


## INPAINTING:

the linear system can be solved (like for $\mathrm{TIK - L_2}$ case) by iterative (P)CG 