# 16.3 MRI成像：从自旋到k-space

> **核心观点**：MRI的正向模型是傅里叶采样——k-space数据就是图像的傅里叶变换。MRI的不适定性不来自算子本身（傅里叶变换是良态的），而来自加速采集的愿望：用更少的k-space样本重建图像，这正是压缩感知和深度学习方法的用武之地。

> **衔接**：第1章1.2节已介绍了MRI的正向模型——欠采样傅里叶算子 $A = M_\Omega F$（k-space采样+掩码），以及欠采样导致的混叠伪影。本节将从物理原理出发，解释k-space数据是如何产生的，以及为什么MRI的不适定性与CT有着本质差异。

## 16.3.1 MRI物理基础：Bloch方程与信号生成

与CT利用X射线的穿透衰减不同，MRI利用的是原子核在磁场中的共振现象。两种模态的物理原理完全不同，但最终都归结为线性逆问题。

### 核磁共振原理

人体内大量存在的氢原子核（质子）具有自旋角动量，在外加磁场中会产生进动。磁化矢量$\mathbf{m} = (m_x, m_y, m_z)$描述了组织中所有质子的净磁效应。

### Bloch方程

磁化矢量的动态行为由Bloch方程描述（完整推导见附录16B）：

$$\boxed{\frac{d\mathbf{m}}{dt} = \gamma \mathbf{m} \times \mathbf{B} - \begin{pmatrix} m_x/T_2 \\ m_y/T_2 \\ (m_z - m_0)/T_1 \end{pmatrix}}$$

其中：
- $\gamma$是旋磁比（质子的Larmor频率）
- $\mathbf{B}$是外加磁场
- $T_1$是纵向弛豫时间（自旋-晶格弛豫）
- $T_2$是横向弛豫时间（自旋-自旋弛豫）
- $m_0$是平衡磁化强度

Bloch方程由两部分组成：
- **旋转项**$\gamma \mathbf{m} \times \mathbf{B}$：描述磁化矢量绕外磁场的进动
- **弛豫项**：描述磁化矢量向平衡态的恢复——横向分量$m_{xy}$以$T_2$为时间常数指数衰减，纵向分量$m_z$以$T_1$为时间常数恢复到$m_0$

### 对比度机制

不同组织的$T_1$和$T_2$值不同，这是MRI丰富对比度的来源。通过调整扫描参数（重复时间$T_R$、回波时间$T_E$），可以从同一解剖结构获得截然不同的对比度：

- **$T_1$加权**：短$T_R$、短$T_E$，突出$T_1$差异
- **$T_2$加权**：长$T_R$、长$T_E$，突出$T_2$差异

直觉：如果说CT的对比度由衰减系数唯一决定（"一种组织，一种灰度"），MRI则像一台"调色板"——同一块组织，不同的参数设置可以呈现出完全不同的"颜色"。这种灵活性是MRI成为最丰富医学成像模态的根本原因。

### 从物理到信号

MRI信号的生成流程：
1. **RF激发**：施加射频脉冲，使纵向磁化偏转到横向平面
2. **梯度编码**：施加空间变化的梯度磁场$\mathbf{B}_z = \mathbf{g}(t) \cdot \mathbf{r}$，编码空间信息
3. **信号采集**：横向磁化在接收线圈中感应出电动势（Faraday感应定律）

在梯度场作用下，横向磁化变为：

$$m_{xy}(t, \mathbf{r}) = u(\mathbf{r}) \cdot \exp\left(-i\gamma \int_0^t \mathbf{g}(\tau) \cdot \mathbf{r} \, d\tau\right)$$

其中$u(\mathbf{r})$是反映组织特性的"有效自旋密度"图像。

### 脉冲序列与k-space填充轨迹

![MRI脉冲序列与k-space填充轨迹对比（SE / GRE / EPI）](./Bloch序列与kspace轨迹.png)

**图注解读**：该图展示了三种临床最常用的MRI脉冲序列及其对应的k-space填充方式。

- **左列时序图**：展示硬件控制信号——RF激发脉冲、三个方向的梯度编码（$G_z$选层、$G_y$相位、$G_x$读出）以及接收线圈的信号/ADC。每个band代表一个物理通道的时域波形。
- **右列k-space路径**：展示每次采集的数据如何填入频域空间。SE和GRE每个TR仅填1行k-space（需要多次重复才能填满），而EPI在单次TR内通过之字形梯度回波链填满整幅k-space。
- **"当前读出行"标注**：红色加粗线标记TE时刻采到的回波对应k-space的ky=0这一行，将左侧时序图和右侧k-space路径在视觉上关联起来。

**速度差异的本质**：SE/GRE每次TR只填1行k-space，总采集时间 = N_TR × TR；EPI单次TR就填满整幅，速度快N_TR倍（典型64-256倍）。这正是EPI成为fMRI和扩散加权成像首选的原因——但代价是图像存在几何畸变和T2*模糊。

## 16.3.2 k-space采样与傅里叶重建

### k-space变量

定义**k-space变量**：

$$\mathbf{k}(t) = \frac{\gamma}{2\pi} \int_0^t \mathbf{g}(\tau) \, d\tau$$

则测量信号为：

$$\boxed{s(\mathbf{k}) = \int u(\mathbf{r}) \, e^{-2\pi i \mathbf{k} \cdot \mathbf{r}} \, d\mathbf{r}}$$

**关键洞察**：$s(\mathbf{k})$就是图像$u(\mathbf{r})$的傅里叶变换！MRI测量的不是$u$本身，而是$u$的频域采样。

与CT的对比：

| | CT | MRI |
|---|---|---|
| 正向模型 | Radon变换（线积分） | 傅里叶变换 |
| 测量空间 | sinogram $(\theta, s)$ | k-space $\mathbf{k}$ |
| 频域采样方式 | 径向（角度+径向距离） | 灵活（Cartesian/径向/螺旋） |
| 逆问题 | $\mathcal{R}u = f$ | $\mathcal{F}u = s$ |

### 重建：逆FFT

对于Cartesian采样（k-space点排列在规则网格上），重建是简单的逆FFT：

$$u(\mathbf{r}) = \mathcal{F}^{-1}s(\mathbf{k})$$

计算复杂度仅为$O(N \log N)$，极其高效。

### k-space轨迹

梯度波形$\mathbf{g}(t)$决定了k-space的采样轨迹（因为$\mathbf{k}(t)$是$\mathbf{g}(t)$的积分）：

- **Cartesian轨迹**：逐行扫描k-space，每个RF激发采集一行。实现简单，FFT直接重建。这是临床最常用的轨迹。
- **径向轨迹**：从k-space中心向外辐射。对运动更鲁棒，但需要非均匀FFT重建。
- **螺旋轨迹**：从k-space中心螺旋向外。单次激发即可覆盖整个k-space，速度快，但重建需要正则化。

### 非Cartesian轨迹的病态性

对于非Cartesian轨迹，正向算子$F$不再是酉矩阵。其SVD分析显示奇异值衰减——特别是螺旋轨迹，k-space中心采样过密导致行近似线性相关，产生病态。

这需要正则化重建：

$$\min_u \|Fu - s\|^2 + \lambda R(u)$$

与CT一样，正则化项$R$的选择决定了重建质量。

## 16.3.3 欠采样掩码与零填充重建

### 加速采集的动机

MRI采集时间与k-space样本数成正比。一个$256 \times 256$的2D图像，Cartesian采集需要约256次RF激发，时间在秒级。但3D或多切片扫描将时间放大$\sim 100$倍，达到分钟级——这对患者来说是很大的负担。

**核心矛盾**：减少k-space样本→缩短扫描时间→但引入欠采样伪影。

### 欠采样掩码

设$\Omega$为k-space中被采样的点的集合（采样掩码），则测量模型为：

$$\mathbf{y} = M_\Omega \mathcal{F} \mathbf{x} + \boldsymbol{\epsilon}$$

其中$M_\Omega$是采样掩码矩阵（对角矩阵，采样点为1，未采样点为0）。

常见的采样模式：

- **等间距欠采样**：每隔$d$行采集一行（加速因子$R = d$）。**问题**：产生周期性混叠伪影（aliasing）——欠采样等价于频域与梳状函数的乘积，对应空间域的周期性叠加。
- **随机欠采样**：伪随机选择k-space行。混叠伪影表现为类噪声，而非相干条纹——更容易被后续处理去除。
- **可变密度随机采样**：k-space中心（低频）密集采样，外围（高频）稀疏采样。这兼顾了信号能量（集中在低频）和不相干性（高频随机化）。

### 零填充重建

最简单的重建方法是将未采样的k-space数据置零，然后逆FFT：

$$\hat{u} = \mathcal{F}^{-1}(\mathbf{y}_{\text{zero-filled}})$$

零填充重建等价于真实图像与sinc函数的卷积——分辨率降低，且存在混叠伪影。

### Nyquist采样准则

对于视野大小$L$、空间分辨率$\delta_s$的图像，k-space的Nyquist采样准则要求：

- 采样间距：$\Delta k \leq 1/L$
- 采样范围：$k_{\max} \geq 1/\delta_s$

欠采样违反了第一个条件，导致混叠。

## 16.3.4 加速采集：并行成像与压缩感知MRI

欠采样是MRI加速的基本手段，但如何从欠采样数据中恢复高质量图像，需要额外的信息。两种主流技术——并行成像和压缩感知——从不同角度补充了欠采样丢失的信息。

### 并行成像（Parallel Imaging）

**基本思想**：使用多个接收线圈，每个线圈有不同的空间灵敏度，提供额外的空间编码信息。

**多线圈信号模型**：第$p$个线圈的信号为：

$$s^p = F C^p u$$

其中$C^p$是对角矩阵，对角元素为第$p$个线圈的灵敏度图$c^p(\mathbf{r})$，$F$是傅里叶采样算子。

**联合重建**：将所有线圈信号堆叠为一个大系统：

$$\min_u \left\|\begin{pmatrix} s^1 \\ s^2 \\ \vdots \\ s^P \end{pmatrix} - \begin{pmatrix} F C^1 \\ F C^2 \\ \vdots \\ F C^P \end{pmatrix} u \right\|^2 + \lambda R(u)$$

关键观察：联合算子$\tilde{F} = (F C^1, \ldots, F C^P)^T$是一个"高"矩阵（行数远大于列数），即使每个线圈的$k$-space都欠采样，只要不同线圈的灵敏度图提供足够的空间多样性，$\tilde{F}$仍然保持满秩——这意味着欠采样是可行的。

**加速效果**：临床常见的加速因子为2-5倍。并行成像已成为几乎所有临床MRI协议的标配。

### 压缩感知MRI

压缩感知（Compressed Sensing, CS）从另一个角度解决欠采样问题：如果图像在某个变换域是稀疏的，那么远少于Nyquist率的采样就足以恢复图像。

**CS的三个条件**：
1. **稀疏性**：图像$u$在某个变换域$W$下是稀疏的（$\|Wu\|_0$很小）
2. **不相干性**：采样伪影与稀疏基不相关——随机欠采样满足这一条件
3. **非线性恢复**：需要$\ell_1$优化（非$\ell_2$）来利用稀疏性

**CS-MRI优化问题**：

$$\boxed{\min_u \|F_\Omega u - s\|^2 + \lambda_1 \|Wu\|_1 + \lambda_2 \|Du\|_1}$$

其中：
- $F_\Omega = M_\Omega F$是欠采样傅里叶算子
- $\|Wu\|_1$是小波稀疏约束
- $\|Du\|_1$是TV约束
- 采样模式通常为**伪随机可变密度**：k-space中心密集、外围随机稀疏

**临床加速效果**：CS-MRI已在临床实践中实现约40%的加速。Philips的Compressed Sense、GE的HyperSense、Siemens的Compressed Sensing都是商业化的CS-MRI产品。

### 并行成像+压缩感知的组合

现代临床MRI通常组合使用并行成像和压缩感知：

$$\min_u \|\tilde{F}_\Omega u - \mathbf{s}\|^2 + \lambda_1 \|Wu\|_1 + \lambda_2 \|Du\|_1$$

其中$\tilde{F}_\Omega$是欠采样的多线圈联合算子。两种技术互补：并行成像利用线圈灵敏度提供空间多样性，CS利用图像稀疏性提供正则化。

### 16.3.5 多通道并行采集：线圈灵敏度、GRAPPA 与 SENSE

16.3.4 已经把"多通道 + 欠采样"统一写成线性系统 $\mathbf{y}=SFS$。但公式本身还不能告诉我们：给定一组欠采样的多线圈数据，应当怎样把图像 $\mathbf{x}$ 还原出来？这正是**并行成像（parallel imaging）**要回答的问题。其核心资本是 $N_c$ 个线圈各自带有不同空间灵敏度图 $S_c(\mathbf{r})$，它们在**空间上互为编码维度**——即便每个线圈的 k 空间只采了很少的线，不同线圈灵敏度差异提供的信息也足以"填补"缺失的采样。

把灵敏度写成对角算子 $S=\mathrm{diag}(S_1,\dots,S_c)$，则第 $c$ 个线圈的欠采样数据满足
$$
\mathbf{y}_c = P_\Omega\, F\, S_c\, \mathbf{x} + \mathbf{n}_c .
$$
**加速倍数** $R$ 即 k 空间相位编码方向（或 2D 中的两个方向）被丢弃的行数比例。下面介绍两条路线：图像域的 SENSE 与 k 空间域的 GRAPPA。

**SENSE（灵敏度编码）。** 把各线圈方程沿通道维堆叠成灵敏度编码矩阵 $E$（即 16.3.4 末尾 $\mathbf{y}=E\mathbf{x}$ 中的 $E=P_\Omega F S$），在**图像域**做"展开（unfolding）"：每个物理点 $\mathbf{r}$ 被卷叠（aliasing）到 $R$ 个折叠位置上，而 $N_c$ 个线圈的灵敏度差异恰好构成 $R$ 个线性方程，解出真实像素。最小二乘解为
$$
\hat{\mathbf{x}}_{\mathrm{SENSE}}=(E^H E+\lambda I)^{-1}E^H \mathbf{y},
$$
$(\cdot)^H$ 为共轭转置。SENSE 是**显式**方法，需先估计每线圈灵敏度图 $S_c$。当线圈数不足或几何退化（某些 $\mathbf{r}$ 处各通道灵敏度近似共线）时，$E^H E$ 接近奇异、噪声被剧烈放大，其放大系数定义为 **g 因子**
$$
g(\mathbf{r})=\sqrt{\bigl[(E^H E)^{-1}\bigr]_{ii}}\,,\qquad
\mathrm{NRMSE}_{\mathrm{out}}=g(\mathbf{r})\,\mathrm{NRMSE}_{\mathrm{in}} .
\tag{16.3.5-1}
$$
式 (16.3.5-1) 表明：SENSE 的信噪比损失**只取决于线圈几何与欠采样模式**、与噪声水平无关，且 $g\ge 1$，折叠最严重的中心区域 g 因子最大。后续的 **CG-SENSE** 用共轭梯度迭代求解上式、并用 **ESPIRiT** 从数据本身自校准出线圈图，免去单独扫灵敏度参考图。

**GRAPPA（广义自校准部分并行采集）。** GRAPPA 走 **k 空间域**路线，不显式求灵敏度图。思路是：在中心留一小块**全采样的自校准区（ACS, autocalibration signal）**，先用 ACS 拟合卷积核，再用该核从已采到的 k 空间点**直接重建出缺失的 k 空间线**，最后对每个线圈做普通逆 FFT 即得图像。其局部卷积关系可写为
$$
\hat{k}(\mathbf{k}_0+\Delta)=\sum_{c}\sum_{\mathbf{k}\in\mathrm{邻域}} w_{c,\mathbf{k}}\,k_c(\mathbf{k}) ,
\tag{16.3.5-2}
$$
其中 $w_{c,\mathbf{k}}$ 为待训练卷积权重。GRAPPA 的代价是 ACS 占用部分扫描时间，且 ACS 有噪声时会把噪声"扩散"到重建结果里。

> **两条路线的取舍。** SENSE 省扫描时间（无需 ACS）但依赖灵敏度估计、对几何退化敏感（g 因子大）；GRAPPA 对灵敏度不敏感、实现稳健，但要用 ACS 换时间，且高噪声 ACS 会拖累重建质量。二者都说明：并行成像的加速上限由**线圈数量与几何排布**决定——这正是 g 因子图所直观展示的。

### 实验 16.3-2（并行成像：GRAPPA 与 SENSE 对比）

并行成像方法的可计算形式（SENSE 展开、g 因子、GRAPPA 的 ACS 自校准）都能在一个轻量仿真里跑通：构造带预设灵敏度图的多线圈体模、做不同加速倍数 $R$ 的均匀欠采样，再分别用 GRAPPA 与 CG-SENSE 重建并量化。该实验详见本章末 **实验 16.3-2**。

## 本节小结

MRI与CT的逆问题结构对比：

| | CT | MRI |
|---|---|---|
| 正向模型 | $y = Ax$（Radon） | $y = M_\Omega F x$（欠采样FFT） |
| 不适定性来源 | 有限角度/稀疏投影 | 加速采集（欠采样） |
| 经典方法 | FBP | 逆FFT |
| 正则化方法 | TV/小波/剪切波 | CS（小波+TV） |
| 加速技术 | — | 并行成像+CS |
| 算子SVD | 可由ASTRA计算 | FFT对角化（高效） |

MRI的关键洞察：**MRI的算子（傅里叶变换）本身是良态的，不适定性完全来自欠采样**。这意味着MRI逆问题的难度是"可控的"——可以通过设计更好的采样模式或正则化来缓解。相比之下，CT的有限角不适定性是"内在的"——无论采样多密集，只要角度不完整就无法消除。

经典的CT正则化和MRI压缩感知方法都依赖手工设计的先验（TV、小波稀疏等），这与第2章讨论的"显式先验天花板"完全对应。自然地，下一步是用学习型方法替代手工先验。

**来源**：IP_and_Im_Lectures-master magnetic_resonance_imaging.md（MRI物理L42-115，k-space L263-346，非Cartesian L347-406，重建L408-482，并行成像L483-556，CS-MRI L558-578）；Lustig et al. (2008) Compressed Sensing MRI；IP_and_Im_Lectures-master fourier_sampling.md（采样定理）

---

## 实验 16.3-1：MRI成像基础——k-space采样与重建

实验 `16.3-1.py` 演示**MRI正向模型（k-space = 图像的傅里叶变换）**，验证欠采样导致混叠伪影，对比三种采样策略，实现CS-MRI的ISTA重建。

### 实验场景

实验考虑MRI成像的完整流程：从二维Shepp-Logan幻影出发，通过傅里叶变换生成k-space数据，再通过不同采样策略和重建方法恢复图像。实验验证16.3节的核心论点：**MRI的不适定性完全来自加速采集（欠采样）**。

**MRI正向模型**（16.3.2节）：
- **k-space信号**：$s(\mathbf{k}) = \int u(\mathbf{r}) e^{-2\pi i \mathbf{k} \cdot \mathbf{r}} d\mathbf{r}$（图像的傅里叶变换）
- **欠采样测量**：$y = M_\Omega \mathcal{F} x$（掩码$\Omega$选择采样点）

**三种采样策略对比**（16.3.3节）：
- **等间距采样**：产生相干混叠（周期性条纹）
- **随机采样**：产生不相干伪影（类噪声）
- **可变密度采样**：中心密集+外围随机，效果最好

### 实验目的

1. **理解k-space与图像的关系**：验证"$s(\mathbf{k})$就是图像的傅里叶变换"，理解低频/高频的物理意义
2. **对比三种欠采样掩码**：等间距vs随机vs可变密度的伪影差异
3. **验证零填充重建的局限**：展示混叠伪影和分辨率降低
4. **实现CS-MRI的ISTA算法**：验证压缩感知的重建效果

### 实验输入

| 参数 | 设置 |
|------|------|
| 测试图像 | Shepp-Logan幻影，$128 \times 128$ 像素 |
| 加速因子 | $R = 4$（保留33/128行） |
| 采样策略 | 等间距、随机、可变密度（中心密集） |
| CS-MRI参数 | TV正则化$\lambda = 0.01$，ISTA迭代50次 |
| 运行环境 | CPU可运行 |

### 预期结果

- k-space能量集中在中心（低频），外围（高频）几乎不可见
- 等间距采样产生相干条纹伪影；可变密度效果最好（PSNR最高）
- CS-MRI ISTA的PSNR高于零填充重建
- 高加速因子下CS-MRI仍能保持质量

### 实验结果

运行 `16.3-1.py` 后，生成五张图。实验结果如下：

---

#### 步骤1：k-space可视化

![实验16.3-1 步骤1：k空间可视化](./第十六章%20配套实验/实验16.3-1/步骤1_k空间可视化.png)

**k-space特征**：

| 特征 | 观察结果 |
|------|---------|
| 能量分布 | 高度集中在中心（低频），外围（高频）几乎不可见 |
| 低频重建 | 仅保留大体轮廓和明暗分布 |
| 高频重建 | 补充边缘和细节 |

步骤1验证了16.3.2节的核心观点：**"$s(\mathbf{k})$就是图像的傅里叶变换"**。低频编码全局结构，高频编码局部细节——这与CT的sinogram表示形成鲜明对比（CT是线积分，MRI是傅里叶变换）。

---

#### 步骤2：欠采样掩码设计

![实验16.3-1 步骤2：欠采样掩码](./第十六章%20配套实验/实验16.3-1/步骤2_欠采样掩码.png)

**三种采样掩码对比**：

| 掩码类型 | 保留行数 | 加速比 | 特点 |
|---------|---------|-------|------|
| 等间距 | 33行 | 3.9x | 周期性条纹伪影（相干混叠） |
| 随机 | 33行 | 3.9x | 类噪声伪影（不相干但分散能量） |
| 可变密度 | 33行 | 3.9x | 中心密集+外围随机，效果最好 |

步骤2验证了16.3.3节的采样策略选择原则：**可变密度采样兼顾中心信号能量和外围不相干性**，是CS-MRI的标准选择。

---

#### 步骤3：零填充重建与混叠伪影

![实验16.3-1 步骤3：零填充重建](./第十六章%20配套实验/实验16.3-1/步骤3_零填充重建.png)

**零填充重建PSNR对比**：

| 掩码 | PSNR | 伪影特征 |
|------|------|---------|
| 等间距 | 最低 | 相干条纹最严重 |
| 随机 | 中等 | 类噪声伪影 |
| 可变密度 | 最高 | 伪影相对较轻 |

步骤3验证了16.3.3节的核心结论：**零填充重建等价于真实图像与sinc函数卷积**，导致分辨率降低+混叠伪影。差异图显示可变密度重建的误差主要在边缘和细节区域——这些区域的k-space信息被欠采样丢弃。

---

#### 步骤4：压缩感知MRI——ISTA算法

![实验16.3-1 步骤4：CS_MRI](./第十六章%20配套实验/实验16.3-1/步骤4_CS_MRI.png)

**CS-MRI vs 零填充重建**：

| 方法 | PSNR | 迭代次数 | 特点 |
|------|------|---------|------|
| 零填充 | 约22 dB | — | 混叠伪影明显 |
| CS-MRI ISTA | 约28 dB | 50次 | TV正则化保边，PSNR提升约6 dB |

步骤4验证了16.3.4节的压缩感知三条件（稀疏性+不相干性+非线性恢复）：**CS-MRI通过TV正则化引入稀疏先验，显著提升重建质量**。ISTA收敛曲线显示前20次迭代PSNR快速提升，后续缓慢收敛。

---

#### 步骤5：加速因子对重建质量的影响

![实验16.3-1 步骤5：加速因子对比](./第十六章%20配套实验/实验16.3-1/步骤5_加速因子对比.png)

**加速因子vs PSNR**：

| 加速因子R | 零填充PSNR | CS-MRI PSNR | CS-MRI优势 |
|----------|-----------|------------|-----------|
| 2 | 约28 dB | 约32 dB | +4 dB |
| 4 | 约22 dB | 约28 dB | +6 dB |
| 6 | 约19 dB | 约26 dB | +7 dB |
| 8 | 约17 dB | 约24 dB | +7 dB |

步骤5验证了16.3.3/16.3.4节的核心论点：**高加速因子下k-space信息缺失更严重，需要更强的先验（TV稀疏性）来补偿**。R≥4时CS-MRI的优势开始显现；R=8时零填充伪影严重而CS-MRI仍能保持一定重建质量。

---

#### 核心知识点总结

| 概念 | 数学表达 | 实验验证 |
|------|---------|---------|
| k-space定义 | $s(\mathbf{k}) = \mathcal{F}u$ | 步骤1：能量集中在中心 |
| 欠采样测量 | $y = M_\Omega \mathcal{F} x$ | 步骤2：三种掩码对比 |
| 混叠伪影 | sinc卷积+周期性叠加 | 步骤3：零填充重建伪影 |
| CS-MRI优化 | $\min \|M_\Omega Fx - y\|^2 + \lambda\|Dx\|_1$ | 步骤4：ISTA重建PSNR提升 |
| 加速因子影响 | 信息缺失程度 | 步骤5：R增大CS-MRI优势显现 |

**关键洞察**：实验完整验证了16.3节的核心论点——**MRI的不适定性完全来自加速采集**。与CT的有限角问题（内在不适定）不同，MRI的困难是"可控的"：通过设计更好的采样模式（可变密度）和正则化（CS），可以在高加速因子下仍保持合理的重建质量。这为16.4节的学习型方法和16.5节的扩散先验方法奠定了基础。

---

## 实验 16.3-2：MRI并行成像——GRAPPA 与 SENSE 对比

实验 `16.3-2.py` 在**多线圈 k 空间**上实现两种经典并行成像方法——图像域的 **CG-SENSE** 与 k 空间域的 **GRAPPA**，并系统比较它们在加速倍数、噪声、ACS 线数等条件下的重建质量与噪声放大（g 因子）。它对应新增的 **16.3.5 节（多通道并行采集：线圈灵敏度、GRAPPA 与 SENSE）**。

### 实验目标

1. **构造多通道正问题**：生成带预设灵敏度图的多线圈体模及其欠采样 k 空间（含 ACS 自校准区）。
2. **实现 CG-SENSE 与 GRAPPA**：分别用图像域灵敏度展开和 k 空间域卷积核重建。
3. **量化加速倍数的影响**：扫描 $R=2/4/6$，比较 PSNR / NMSE 与 g 因子。
4. **分析噪声鲁棒性与 ACS 依赖**：揭示两种方法的脆弱点（GRAPPA 对 ACS 噪声敏感、SENSE 对几何退化敏感）。
5. **可视化 g 因子图**：直观展示加速越高、几何越退化处噪声放大越严重。

### 实验输入

| 参数 | 设置 |
|------|------|
| 测试图像 | Shepp-Logan 体模，$128\times128$，8 通道线圈 |
| 线圈灵敏度 | 径向高斯形（中心强、边缘弱），通道间相位偏移 |
| 加速因子 | $R=2,4,6$（均匀欠采样） |
| ACS 线数 | 默认 12 行（步骤 7b 扫描 4–24） |
| 噪声水平 | 步骤 5 扫描 $\sigma=0.005\sim0.05$ |
| 方法 | CG-SENSE、GRAPPA（CPU 可运行） |

### 前置步骤：线圈灵敏度与采样掩码

![实验16.3-2 步骤1：线圈灵敏度与采样掩码](./第十六章%20配套实验/实验16.3-2/步骤1_线圈灵敏度与采样掩码.png)

步骤 1 展示了 8 个线圈的灵敏度图（a）与不同加速倍数下的均匀采样掩码（b）。灵敏度图在空间上互补，为并行成像提供编码维度；掩码中心保留 ACS 区用于 GRAPPA 自校准——这正是 16.3.5 节所述两条路线的共同起点。

---

### 步骤 2–3：多通道 k 空间生成与两种方法重建

（生成带灵敏度加权的多通道 k 空间、划分 ACS 区、拟合 GRAPPA 卷积核，并用 CG-SENSE 做图像域最小二乘展开。）

### 步骤 4：重建对比与误差

![实验16.3-2 步骤4：重建对比与误差](./第十六章%20配套实验/实验16.3-2/步骤4_重建对比与误差.png)

以 $R=4$ 为例，CG-SENSE 恢复的结构与细节明显优于 GRAPPA，其误差图能量也更低——这与 16.3.5 节"几何良好时显式灵敏度方法更优"的判断一致。

---

### 步骤 5：噪声鲁棒性

![实验16.3-2 步骤5：噪声鲁棒性对比](./第十六章%20配套实验/实验16.3-2/步骤5_噪声鲁棒性对比.png)

在 $R=4$ 下扫描噪声水平 $\sigma$：

| 方法 | $\sigma=0.05$（高噪） | $\sigma=0.005$（低噪） |
|------|----------------------|----------------------|
| GRAPPA | 17.41 dB | 27.54 dB |
| CG-SENSE | 22.97 dB | 31.45 dB |

SENSE 在所有噪声下均优于 GRAPPA，且在**高噪**下优势更大（约 +5.6 dB）。说明 GRAPPA 的 ACS 自校准对噪声更敏感（噪声被"扩散"进重建，见 16.3.5 节式 (16.3.5-2)），而 CG-SENSE 直接做最小二乘展开更稳。

---

### 步骤 6：g 因子图

![实验16.3-2 步骤6：g因子图](./第十六章%20配套实验/实验16.3-2/步骤6_g因子图.png)

g 因子随加速倍数增大而升高，中心折叠区最严重：

| 加速倍数 $R$ | g 因子最大值 | g 因子均值 |
|-------------|------------|-----------|
| 2 | 1.25 | 1.08 |
| 4 | 1.89 | 1.29 |
| 6 | 2.55 | 1.52 |

这正是 16.3.5 节式 (16.3.5-1) 的物理体现：SENSE 的噪声放大只取决于线圈几何与欠采样模式，与噪声水平无关，且 $g\ge 1$。

---

### 步骤 7：多加速比对比

![实验16.3-2 步骤7：多加速比对比](./第十六章%20配套实验/实验16.3-2/步骤7_多加速比对比.png)

不同加速倍数下的重建质量（默认 ACS = 12 行）：

| 加速倍数 $R$ | GRAPPA PSNR / NMSE | CG-SENSE PSNR / NMSE |
|-------------|-------------------|---------------------|
| 2 | 32.90 dB / 0.1236 | 34.92 dB / 0.0777 |
| 4 | 30.16 dB / 0.2216 | 32.93 dB / 0.1443 |
| 6 | 27.54 dB / 0.3982 | 30.90 dB / 0.2296 |

随 $R$ 增大两种方法的 PSNR 都下降，但 CG-SENSE 始终领先约 2–3 dB；同时 g 因子快速上升（见步骤 6），并行成像的加速上限由线圈数量与几何排布决定。

---

### 步骤 7b：ACS 线数对 GRAPPA 的影响

![实验16.3-2 步骤7b：ACS扫描](./第十六章%20配套实验/实验16.3-2/步骤7b_ACS扫描.png)

![实验16.3-2 步骤7b：g因子热力图(R=2,4)](./第十六章%20配套实验/实验16.3-2/步骤7b_g因子热力图_R2-4.png)

在 $R=4$ 下扫描 ACS 线数（GRAPPA 依赖、CG-SENSE 不依赖）：

| ACS 线数 | GRAPPA PSNR | CG-SENSE PSNR |
|---------|------------|--------------|
| 4 | 29.60 dB | 32.93 dB |
| 12（默认） | 30.16 dB | 32.93 dB |
| 24 | 31.10 dB | 32.93 dB |

ACS 越多 GRAPPA 越准（自校准更稳），但占用扫描时间；CG-SENSE 不受 ACS 影响。右侧 g 因子热力图直观显示 $R=2,4$ 时噪声放大的空间分布。

---

### 步骤 7c：高加速 + 固定 ACS + 高噪声（最坏情形）

![实验16.3-2 步骤7c：高R固定ACS高噪](./第十六章%20配套实验/实验16.3-2/步骤7c_高R固定ACS高噪.png)

固定 ACS = 12 行、叠加高噪声，考察极限加速：

| 加速倍数 $R$ | GRAPPA PSNR | CG-SENSE PSNR |
|-------------|------------|--------------|
| 6 | 20.43 dB | 30.90 dB |
| 8 | 17.70 dB | 28.69 dB |

在"高加速 + 固定 ACS + 高噪声"的最坏组合下，GRAPPA 严重退化（ACS 噪声被放大），而 CG-SENSE 仍保持可用质量——凸显 16.3.5 节所总结的两条路线取舍。

---

### 核心知识点总结

| 概念 | 数学表达 | 实验验证 |
|------|---------|---------|
| 多通道正问题 | $\mathbf{y}_c=P_\Omega F S_c\mathbf{x}$ | 步骤 1：灵敏度图 + 掩码 |
| SENSE 展开 | $\hat{x}=(E^HE)^{-1}E^Hy$ | 步骤 4 / 7：PSNR 领先 GRAPPA |
| GRAPPA 自校准 | 式 (16.3.5-2) k 空间卷积核 | 步骤 7b：ACS 越多越准 |
| g 因子噪声放大 | 式 (16.3.5-1)，$g\ge1$ | 步骤 6：随 $R$ 升高 |
| 噪声鲁棒性 | ACS 噪声扩散 | 步骤 5 / 7c：高噪下 GRAPPA 退化 |

**关键洞察**：并行成像把"多线圈灵敏度"当作额外编码维度来对抗欠采样。SENSE（图像域、显式灵敏度）在几何良好时质量更高，但受几何退化与噪声放大（g 因子）限制；GRAPPA（k 空间域、ACS 自校准）对灵敏度不敏感、实现稳健，但受 ACS 噪声拖累。二者共同说明：**并行成像的加速上限由线圈数量与几何排布决定**，这正是 16.3.5 节理论与本实验的交汇点，也为 16.4 节的学习型并行成像与 16.5 节的扩散先验方法铺垫。
