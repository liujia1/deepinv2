# 附录 2B TGV：TV 的改进

> 2.2 节指出，TV 先验假设图像分段常数，在渐变区域产生阶梯效应。本附录介绍 Total Generalized Variation（TGV），一种通过引入二阶信息缓解阶梯效应的改进方法，供对正则化理论有深入兴趣的读者参考。

## 阶梯效应的根源回顾

TV 先验的正则项为 $\text{TV}(x) = \|\nabla x\|_1$，惩罚图像梯度 $\ell^1$ 范数。函数空间中，TV 解属于 **BV（有界变差）空间**——BV 允许不连续函数（边缘），但 TV 最小化解倾向**分段常数**函数。

这并非偶然。考虑最简单去噪问题：区间 $[0,1]$ 上给定含噪一维信号 $y$，TV 去噪解 $x^*$ 满足：

$$x^* = \arg\min_x \frac{1}{2}\|x - y\|_2^2 + \lambda \int_0^1 |x'| \, dt$$

当 $\lambda$ 较大时，正则项 $\int |x'| \, dt$ 强烈偏好 $x' = 0$——即 $x$ 为常数。对真正渐变信号（如 $x(t) = t$，$x' = 1 \neq 0$），TV 只能用一系列分段常数近似线性函数，产生阶梯状人工结构。

阶梯效应**本质原因**在于：TV 只用一阶导数信息，将函数空间限制为分段常数。若引入二阶导数信息，就可允许分段仿射（一次函数）解——这正是 TGV 改进思路。

---

## TGV 的定义

Bredies, Kunisch 和 Pock（2010）提出的 **TGV**（Total Generalized Variation，广义全变差）是 TV 高阶推广。二阶 TGV 连续定义：

$$\text{TGV}_\alpha^2(x) = \sup\left\{\int_\Omega x \, \text{div}^2 p \, dx \;\Big|\; p \in C_c^\infty(\Omega, \text{Sym}_{2\times2}),\;\|p\|_\infty \leq \alpha_0,\;\|\text{div}\, p\|_\infty \leq \alpha_1\right\}$$

其中 $\alpha = (\alpha_0, \alpha_1)$ 是两个正参数，$\text{Sym}_{2\times2}$ 是 $2\times 2$ 对称矩阵空间，$\text{div}^2$ 是二阶散度算子。

定义较抽象。更直观理解来自 TGV **原始形式**（primal formulation）：

$$\text{TGV}_\alpha^2(x) = \min_w \;\alpha_1\|\nabla x - w\|_1 + \alpha_0\|\mathcal{E}(w)\|_1$$

其中：
- $w$ 是辅助向量场（可理解为对梯度 $\nabla x$ 的"仿射分量"逼近）
- $\mathcal{E}(w) = \frac{1}{2}(\nabla w + \nabla w^\top)$ 是 $w$ 的**对称梯度**（symmetrized gradient），即 $w$ 的二阶导数信息

### 逐项解读

**第一项** $\alpha_1\|\nabla x - w\|_1$：惩罚图像梯度 $\nabla x$ 与辅助场 $w$ 的偏差。当 $w = 0$ 时，第一项退化为 $\alpha_1\|\nabla x\|_1 = \alpha_1 \text{TV}(x)$——TGV 退化为加权 TV。

**第二项** $\alpha_0\|\mathcal{E}(w)\|_1$：惩罚 $w$ 的对称梯度 $\ell^1$ 范数。$\mathcal{E}(w)$ 度量 $w$ 的"非仿射性"——若 $w$ 本身是仿射场（对应 $x$ 是二次函数），则 $\mathcal{E}(w) = 0$，第二项为零。

**两者协同**：最小化在对 $w$ 取下确界后，TGV 允许图像梯度 $\nabla x$ 与一个"仿射允许"的辅助场 $w$ 偏离，同时惩罚 $w$ 非仿射性。当图像分段仿射（渐变区域 + 边缘），可选取 $w \approx \nabla x$ 使第一项小，且 $w$ 近似仿射使第二项也小——从而 TGV 值低，TGV 正则化不会惩罚分段仿射结构。

---

## TGV 与 TV 的关系

### TV 是 TGV 的特例

当 $\alpha_0 \to \infty$ 时，第二项 $\alpha_0\|\mathcal{E}(w)\|_1$ 权重趋无穷，迫使 $\mathcal{E}(w) = 0$，即 $w$ 必须是仿射场。若进一步限制 $w = 0$（仿射场最简情形），则 TGV 退化为 TV：

$$\text{TGV}_{(\alpha_0 \to \infty, \alpha_1)}^2(x) \to \alpha_1\|\nabla x\|_1 = \alpha_1 \text{TV}(x)$$

因此，**TV 是 TGV 在 $\alpha_0 \to \infty$ 时的特例**。TGV 比 TV 多一个自由度 $\alpha_0$，用于控制对二阶信息的容忍程度。

### 函数空间差异

| | TV | TGV$^2$ |
|---|---|---|
| **惩罚** | $\|\nabla x\|_1$（一阶） | $\alpha_1\|\nabla x - w\|_1 + \alpha_0\|\mathcal{E}(w)\|_1$（一阶+二阶） |
| **偏好解** | 分段常数 | 分段仿射 |
| **渐变区域** | 阶梯效应 | 自然渐变 |
| **边缘保持** | 能 | 能 |
| **参数** | $\alpha_1$（一个） | $(\alpha_0, \alpha_1)$（两个） |
| **函数空间** | BV | BV$^2$（有界二阶变差） |

关键差异：TV 解是分段常数，TGV 解是分段仿射。对渐变区域（如天空渐变），分段仿射近似远比分段常数自然——不需用一系列阶梯近似斜坡。

---

## 离散 TGV 模型

实际计算中需将连续 TGV 离散化。设图像定义在离散网格上，$D$ 为有限差分算子，$E$ 为对称梯度算子。离散 TGV：

$$\text{TGV}(x) = \min_w \;\alpha_1\|Dx - w\|_1 + \alpha_0\|Ew\|_1$$

其中 $Dx$ 给出图像水平和垂直有限差分，$Ew$ 给出辅助场 $w$ 的对称梯度（含 $w$ 二阶和混合差分）。

TGV 正则化去噪模型：

$$\min_{x, w} \;\frac{1}{2}\|x - y\|_2^2 + \alpha_1\|Dx - w\|_1 + \alpha_0\|Ew\|_1$$

这是凸优化问题，可用**原始-对偶方法**（primal-dual method）高效求解。与 TV 去噪比，TGV 优化变量增加 $w$，但算法框架相同，只是变量和约束更多。

参数 $(\alpha_0, \alpha_1)$ 选取通常经验调参或 L-曲线法确定。Bredies 等人建议 $\alpha_0 / \alpha_1 \approx 2$ 作为起点。

---

## 效果对比：TV vs TGV

### 一维直觉

考虑简单一维信号：常数段 → 线性渐变段 → 常数段。TV 去噪将线性渐变段近似为一系列阶梯（因 TV 偏好分段常数）。TGV 去噪可自然保留线性渐变（因 TGV 允许分段仿射）。

### 二维实验

Pock 在讲义中展示 TV 与 TGV 对比实验：

| 方法 | $\sigma = 25$ 去噪 PSNR |
|---|---|
| TV | 29.91 dB |
| TGV$^2$ | 约 30.2 dB |

TGV 在 PSNR 上提升看似不大，但视觉质量改善在渐变区域非常明显——阶梯效应基本消失，天空渐变、皮肤过渡等区域更自然。

Siltanen 在冬季学校讲义中也展示 CT 重建中 TV 与 TGV 对比：TV 重建渐变组织区域出现阶梯状伪影，TGV 重建可更自然恢复平滑过渡。

---

## 应用场景

TGV 在以下场景相比 TV 有显著优势：
1. **PET 重建**：正电子发射断层扫描中，放射性示踪剂浓度分布常含渐变区域（如肝脏、肺部组织边界），TGV 可避免 TV 引入阶梯伪影
2. **MRI 重建**：磁共振图像中软组织灰度渐变区域（如脑白质到脑灰质过渡），TGV 可更自然恢复
3. **图像修复**（inpainting）：待补全区域含渐变结构时，TV 假设分段常数会导致修复区阶梯，TGV 可生成平滑渐变填充

---

## 实验 2B-1：TGV 缓解阶梯效应——从分段常数到分段仿射

前面理论分析指出，TGV 通过引入二阶导数信息，将 TV 分段常数假设推广为分段仿射假设，从而缓解阶梯效应。但自然的问题是：**TGV 在实际图像上是否真能消除阶梯？辅助向量场 $w$ 如何工作？参数选择对结果有何影响？** 实验 `2.b-1.py` 通过 cameraman 图像去噪任务，系统验证附录 2B 所有核心结论。

### 实验场景

使用经典 cameraman 图像（128×128），含丰富边缘和渐变区域（如天空背景、人物衣物）。添加高斯白噪声，噪声水平 $\sigma = 0.1$。对比 TV 去噪与 TGV 去噪效果，并分析辅助向量场 $w$ 行为。

### 实验目的

1. **验证阶梯效应缓解**：通过自动检测平滑渐变区域放大对比，直观展示 TV 阶梯状结构与 TGV 自然过渡
2. **理解辅助向量场 $w$ 作用**：可视化 $w$ 幅值分布和方向性流动，揭示 TGV 如何识别仿射结构
3. **评估参数敏感性**：固定 $\alpha_1 = 0.15$，变化 $\alpha_0/\alpha_1$ 比值（从 0.5 到 8.0），观察全局指标与局部视觉质量差异
4. **认识算法实现细节**：掌握 Chambolle-Pock 原始-对偶方法收敛条件、离散算子伴随关系及数值稳定性要点
5. **对比 TV 与 TGV 计算开销**：TGV 需优化 $x$ 和 $w$ 两组变量，计算开销约为 TV 的 2-3 倍

### 实验结果

![实验2B-1 TV与TGV对比](./配套实验/实验2.b-1/步骤1_TV_vs_TGV对比.png)

**第一行**展示含噪图像、原始图像、TV去噪和TGV去噪直观对比：
- **含噪图像**（PSNR=20.37 dB, SSIM=0.353）：高斯噪声严重破坏图像细节，天空区域和人物轮廓都受明显干扰
- **原始图像**：cameraman 图像参考标准，含清晰边缘（如三脚架、相机镜头）和平滑渐变区域（如天空背景）
- **TV 去噪**（PSNR=25.92 dB, SSIM=0.766）：有效去除噪声，保留边缘锐度，但天空渐变区域出现明显阶梯状结构——灰度值在若干像素范围保持常数，然后跳跃到下一常数值
- **TGV 去噪**（PSNR=25.91 dB, SSIM=0.766）：同样有效去噪，边缘保持良好，且天空渐变区域呈自然平滑过渡，无人工阶梯结构

从全局指标看，TV 与 TGV 的 PSNR 和 SSIM 几乎相同——与附录正文引用 Pock 实验结果一致（TGV 的 PSNR 提升通常不超过 0.3 dB）。但**视觉质量差异在渐变区域非常明显**。

![实验2B-1 阶梯效应改善分析](./配套实验/实验2.b-1/步骤2_阶梯效应改善分析.png)

**左图**展示中心行剖面线对比：
- 黑色实线是真实信号灰度值变化
- 红色曲线是 TV 去噪结果，在渐变区域呈锯齿状波动——阶梯效应直接体现
- 绿色曲线是 TGV 去噪结果，更紧密跟随真实信号，渐变区域波动显著减小

**右图**放大自动检测平滑渐变区域（通过一阶梯度中等 + 二阶差分较小联合准则）：
- TV 去噪在该区域仍呈阶梯状结构，灰度值分段保持常数
- TGV 去噪则保持自然连续渐变，验证"分段仿射"假设有效性

![实验2B-1 辅助向量场可视化](./配套实验/实验2.b-1/步骤3_辅助向量场可视化.png)

**左图**展示辅助向量场 $|w|$ 热力图：
- 边缘区域（如人物轮廓、三脚架），$|w|$ 幅值较大，说明 $w$ 需显著偏离零以匹配强梯度
- 平坦区域，$|w| \approx 0$，TGV 退化为类似 TV 行为
- 渐变区域（如天空），$|w|$ 呈中等幅值，表明该区域被识别为仿射结构

**中图**将向量场 $w$ 叠加在 TGV 去噪图像上（箭头表示方向，颜色表示幅值）：
- 渐变区域，$w$ 形成一致方向性流动，揭示 TGV 如何"理解"该区域仿射性质
- 这种可解释性是 TV 不具备的——TV 没引入辅助变量，无法提供梯度结构化信息

**右图**是 $|w|$ 幅值分布直方图：
- 大部分像素 $|w|$ 接近零（平坦区域）
- 少数像素 $|w|$ 较大（边缘区域）
- 中等幅值像素对应渐变区域

### 参数敏感性分析

固定 $\alpha_1 = 0.15$，变化 $\alpha_0/\alpha_1$ 比值（从 0.5 到 8.0），观察去噪效果变化：

| $\alpha_0/\alpha_1$ | PSNR (dB) | SSIM |
|---|---|---|
| 0.5 | 25.88 | 0.764 |
| 1.0 | 25.90 | 0.765 |
| 2.0 | 25.91 | 0.766 |
| 4.0 | 25.89 | 0.764 |
| 8.0 | 25.87 | 0.763 |

![实验2B-1 参数敏感性分析](./配套实验/实验2.b-1/步骤4_参数敏感性分析.png)

**左图**展示不同 $\alpha_0/\alpha_1$ 比值下去噪结果：
- $\alpha_0/\alpha_1$ 较小时（如 0.5），二阶正则化较弱，TGV 接近 TV，阶梯效应较明显
- $\alpha_0/\alpha_1 \approx 2.0$ 时，一阶和二阶正则化达平衡，视觉效果最佳
- $\alpha_0/\alpha_1$ 过大（如 8.0），过度平滑可能导致边缘模糊

**右图**展示 PSNR 和 SSIM 随 $\alpha_0/\alpha_1$ 变化曲线：
- PSNR 在 $\alpha_0/\alpha_1 = 2.0$ 处达峰值（25.91 dB）
- SSIM 同样在 $\alpha_0/\alpha_1 = 2.0$ 处达峰值（0.766）
- 但整体变化幅度很小（PSNR 变化不超 0.05 dB，SSIM 变化不超 0.003）

### 关键观察

1. **全局指标对参数不敏感**：PSNR 和 SSIM 在 $\alpha_0/\alpha_1 \in [0.5, 8.0]$ 范围内变化不超 0.05 dB——因全局指标无法捕捉局部视觉质量差异。即使 PSNR/SSIM 几乎不变，渐变区域视觉效果仍有显著差异。
2. **视觉质量参数依赖性**：$\alpha_0/\alpha_1$ 较小时，二阶正则化较弱，TGV 接近 TV，阶梯效应较明显；$\alpha_0/\alpha_1$ 过大，过度平滑可能导致边缘模糊。Bredies 等人建议 $\alpha_0/\alpha_1 \approx 2.0$ 在视觉质量与数值指标间取得良好平衡。
3. **辅助向量场 $w$ 可解释性**：$w$ 提供"梯度仿射分量"结构化信息，帮助理解 TGV 为何改善阶梯效应。渐变区域 $w$ 形成一致方向性流动，表明该区域被识别为仿射结构——这是 TV 不具备的可解释性。
4. **计算开销权衡**：TGV 需优化 $x$ 和 $w$ 两组变量，计算开销约为 TV 的 2-3 倍。实时应用中需权衡视觉质量提升与计算成本增加。
5. **离散算子实现细节至关重要**：散度算子实现需避免中间列/行相互抵消（常见 bug：用 `div[:, :-1] -= p_h[:, :-1]` 代替 `div -= p_h` 会导致散度几乎为零）。对称梯度伴随算子中 $q_3$ 交叉项贡献方向需与数学定义一致。

### 实验启示

1. **全局指标局限性**：PSNR 和 SSIM 等全局指标无法反映局部视觉质量差异。评估 TGV 等改进方法时，应结合视觉检查和局部指标（如渐变区域 MSE）。
2. **参数调优依赖视觉检查**：因全局指标对参数不敏感，TGV 参数调优常依赖视觉检查而非自动指标。实际应用增加调参难度。
3. **可解释性价值**：辅助向量场 $w$ 提供 TGV 内部工作机制洞察，有助于理解算法为何有效及如何针对特定场景调参。
4. **显式先验精细化改良有极限**：TGV 代表显式先验改进的"局部最优"——框架内精细调整达极限，真正突破需跳出框架，走向数据驱动隐式先验（2.4节）。

> **运行实验**：`python 配套实验/实验2.b-1/2.b-1.py`
>
> **生成图片**：
> - `步骤1_TV_vs_TGV对比.png`：含噪图像、原始图像、TV去噪、TGV去噪直观对比
> - `步骤2_阶梯效应改善分析.png`：中心行剖面线对比及自动检测渐变区域放大对比
> - `步骤3_辅助向量场可视化.png`：$|w|$ 热力图、叠加在去噪图像上的向量场、幅值分布直方图
> - `步骤4_参数敏感性分析.png`：不同 $\alpha_0/\alpha_1$ 比值下去噪结果及 PSNR/SSIM 曲线
> - `步骤5_性能与特性对比.png`：PSNR/SSIM 独立柱状图及 TV vs TGV 特性对比表

---

## 为何本书不展开 TGV

TGV 是 TV 精细化改良，特定应用中确实有效。但它属于**显式先验内部改进**——仍假设某种参数化正则项形式（一阶+二阶导数的 $\ell^1$ 惩罚），只是比 TV 多引入一阶。TGV 没参与"贝叶斯→扩散"主线叙事：
- TGV 没改变"人为指定先验形式"本质——仍须手工选正则项结构和参数
- TGV 改进是量变（分段常数到分段仿射），非质变（显式到隐式）
- TGV 阶梯效应缓解只是延缓问题——分段仿射假设在更高阶渐变（如二次渐变）中仍可能不足

从本书叙事主线看，TGV 代表显式先验改进的"局部最优"——框架内精细调整达极限，真正突破需跳出框架，走向数据驱动隐式先验（2.4节）。

---

## 参考文献

Bredies K., Kunisch K., Pock T. "Total Generalized Variation." *SIAM Journal on Imaging Sciences*, 3(3):492-526, 2010.

> **附录2B的要点**：TGV 通过同时惩罚一阶和二阶导数，将 TV 分段常数假设推广为分段仿射假设，有效缓解阶梯效应。TGV 是 TV 推广（$\alpha_0 \to \infty$ 时 TGV 退化为 TV），在渐变区域显著改善重建质量。但 TGV 仍是显式先验精细化改良，不改变"人为指定先验形式"本质——本书主线走向从数据中学先验的隐式路径。
