# 2.2 经典先验族

> 你走进一间工具房（正则化工具箱），墙上挂满扳手：Tikhonov、LASSO、TV……每一把都贴着"适用什么场景"的标签。但你有没有想过：这些工具为什么长成这个样子？它们究竟在假设"图像长什么样"？

承接上一节，我们已经拿到那张"贝叶斯 ↔ 变分"对照表，核心结论只有一句：**数据项由噪声物理唯一决定，正则项由先验决定——先验是我们唯一能主动施加影响的地方。**

所以"选择先验"到底意味着什么？答案就藏在对照表里：**选择先验，就是选择关于 $x$ 的数学假设。** 每种先验编码了"图像长什么样"的一种结构化知识——或来自物理约束，或来自统计规律；取负对数之后，这种信念变成正则项，引导优化算法走向满足该信念的解。从更深层次看，先验不是人为偏见，而是系统已有经验的数学化表达；本节要做的，正是把每一种经典正则项"翻译"回它所代表的那条知识，再逐一检验：这条知识在什么场景下为真，在什么场景下会伤害解。

**本章目标**：
- 理清三种经典先验的递进：高斯说"值小" → Laplace 说"值稀疏" → TV 说"梯度稀疏"
- 看懂每种先验对应的正则项和解的形态，以及各自的"阿喀琉斯之踵"
- 理解"零点奇性"这个关键点——它才是稀疏性的概率根源

---

## 一、高斯先验：从"值小"到 Tikhonov 正则化

### 假设与推导

在写下任何公式之前，先问一个认知层面的问题：如果我们对图像几乎一无所知，只知道"它不可能具有任意大的能量"，那么最保守的信念是什么？答案是各分量围绕零集中、彼此互不相关的分布——高斯分布。它编码的结构知识极为克制：信号分量的幅值是有界的，偏离零越远越不可信，且偏离量以平方速率被否定。这正对应一条朴素的物理约束：成像系统的动态范围有限，信号能量不可能发散。

最简单的先验假设：**图像 $x$ 的各分量独立，且服从零均值高斯分布**。

$$p(x) = \prod_{i=1}^n \frac{1}{\sqrt{2\pi\sigma_x^2}} \exp\!\Big(-\frac{x_i^2}{2\sigma_x^2}\Big) = \frac{1}{(2\pi\sigma_x^2)^{n/2}} \exp\!\Big(-\frac{1}{2\sigma_x^2}\|x\|_2^2\Big)$$

其中 $\sigma_x^2$ 是先验对方差（即信号典型能量）的估计。取负对数：

$$-\ln p(x) = \frac{1}{2\sigma_x^2}\|x\|_2^2 + \text{const}$$

正则项 $\frac{1}{2\sigma_x^2}\|x\|_2^2$ 正是 $\ell^2$ 正则项。配合高斯噪声的 $\ell^2$ 数据项，对应 MAP 估计：

$$\hat{x}_{\text{MAP}} = \arg\min_x \|y - Ax\|_2^2 + \lambda\|x\|_2^2$$

其中 $\lambda = \sigma^2 / \sigma_x^2$ 是两个方差的比值：噪声越强、或先验认为信号越弱，正则化就越强——数据与信念的相对权重，在这里获得了清晰的概率诠释。这就是 **Tikhonov 正则化**（岭回归），应用最广。

### 解的形态与闭式解

高斯先验的一大独特优势：后验仍是高斯分布，MAP 估计有**闭式解**：

$$\hat{x} = (A^\top A + \lambda I)^{-1} A^\top y$$

更一般地，当 $L \neq I$（如 $L = \nabla$）时，广义 Tikhonov 正则化 $\|y - Ax\|_2^2 + \lambda\|Lx\|_2^2$ 的解为：

$$\hat{x} = (A^\top A + \lambda L^\top L)^{-1} A^\top y$$

闭式解意味着不用迭代优化，直接解线性方程组就行——这一转变把计算成本从"迭代收敛问题"降格为"一次线性求解"，是计算上的巨大优势。代价是，$\ell^2$ 的二次惩罚性质决定了解的整体形态：**所有分量被均匀压缩，但没有分量被推到精确的零。**

### 局限：偏好平滑，丢失边缘

高斯先验的假设——"$x$ 的值小（0 均值）"——隐含了对低能量解的偏好，这就是过度平滑的根源。具体来说，这个假设在图像上有两种表现形式：

**在图像空间上**，平坦、低幅值的区域恰好符合"值小"的预期，被正确平滑；但边缘和纹理处的像素值突变意味着大的局部偏差与大的 $|x_i - x_{i-1}|$，而 $\ell^2$ 对大值施加**二次惩罚**，强烈抑制突变——无论惩罚作用于 $x$ 还是 $\nabla x$，结果都是边缘被模糊、纹理被抹平。

**在解的结构上**，$\ell^2$ 无法产生精确的零分量。这在图像重建等逆问题中成为主要瓶颈：真实信号往往稀疏（边缘仅占少数像素、频谱仅有少数活跃频率），而 $\ell^2$ 的非稀疏性使其无法匹配这种结构。其后果是重建结果整体偏模糊——能量被泄漏到本应为零的分量上；同时丧失特征选择能力，且增大 $\lambda$ 只会将所有分量同步缩小，而非将无关分量精确置零。

归根结底：Tikhonov 正则化天然倾向过度平滑：边缘模糊、纹理丢失。

### 推广：梯度高斯先验 → Sobolev 平滑

对 $x$ 本身加高斯先验假设"像素值小"，在图像里不太合理——整体亮度是场景属性，本不该由先验裁决。更自然的假设是"相邻像素差值小"，即图像平滑。这一转变意味着先验编码的对象从"数值"换成了"空间结构"：图像是连续物理量的采样，相邻像素天然相关。把高斯先验加在梯度上：

$$\nabla x \sim \mathcal{N}(0, \sigma_\nabla^2 I)$$

取负对数得：

$$-\ln p(x) = \frac{1}{2\sigma_\nabla^2}\|\nabla x\|_2^2 + \text{const}$$

对应正则项 $\|\nabla x\|_2^2$，即 **Sobolev 平滑正则化**（也称 $H^1$ 正则化）：

$$\min_x \|y - Ax\|_2^2 + \lambda\|\nabla x\|_2^2$$

这比对 $x$ 直接加高斯更合理——它编码"相邻像素相关"的空间结构。但 $\ell^2$ 的二次惩罚没有变：大梯度（边缘处）仍被过度惩罚，过度平滑只是缓解，没有根治。

> **高斯先验的本质**：高斯分布没有"零点奇性"——密度在零点取到的是一处光滑的最大值，$p(x)$ 在零附近平缓变化，没有任何"偏好零"的特殊力量。这意味着 $\ell^2$ 正则项只会均匀缩小所有值，而不会把小值推向精确的零。高斯先验的解是"分散的小值"，不是"稀疏的少量非零值"。

---

## 实验 2.2-1：高斯先验的推广——从 Tikhonov 到 Sobolev 平滑

实验 `2.2-1.py` 演示**简单高斯先验（Tikhonov）与梯度高斯先验（Sobolev）的去噪效果对比**，揭示协方差结构对正则化效果的影响。

### 实验场景

实验考虑纯去噪问题：观测模型 $y = x + \varepsilon$，其中 $\varepsilon \sim \mathcal{N}(0, \sigma^2 I)$ 为加性高斯白噪声。对比两种高斯先验：

**简单高斯先验（Tikhonov）**：假设 $x \sim \mathcal{N}(0, \sigma_x^2 I)$，正则项为 $\|x\|_2^2$。MAP 估计有闭式解：

$$\hat{x} = \frac{y}{1 + \lambda}, \quad \lambda = \frac{\sigma^2}{\sigma_x^2}$$

**梯度高斯先验（Sobolev）**：假设 $\nabla x \sim \mathcal{N}(0, \sigma_\nabla^2 I)$，正则项为 $\|\nabla x\|_2^2$。MAP 估计可通过梯度下降迭代求解，也可通过频域闭式解直接计算。

### 实验目的

1. **验证 Tikhonov 参数的概率诠释**：对比从数据估计 $\sigma_x$ 与主观设定 $\sigma_x$ 两种参数选择方式的效果差异
2. **对比两种高斯先验的去噪效果**：Tikhonov（假设值小）vs Sobolev（假设相邻像素相似）
3. **验证 Sobolev 迭代解与频域闭式解的等价性**：理论上两者应给出相同结果
4. **展示超参数敏感性**：不同 $\lambda$ 对 Sobolev 去噪效果的影响

### 实验输入

| 参数 | 设置 |
|------|------|
| 测试图像 | camera，$64 \times 64$ 像素 |
| 噪声模型 | 加性高斯白噪声，$\sigma = 0.1$ |
| 图像实际标准差 | $\sigma_x(\text{数据}) = 0.2755$ |
| Tikhonov 参数 | 从数据估计：$\lambda = \sigma^2/\sigma_x^2 = 0.1317$；主观先验：$\sigma_x = 1.0$，$\lambda = 0.01$ |
| Sobolev 参数 | $\lambda \in \{0.05, 0.1, 0.2\}$ |
| 求解方法 | Tikhonov：闭式解；Sobolev：梯度下降迭代 + 频域闭式解验证 |

### 预期结果

- Tikhonov 去噪效果受参数选择影响显著：从数据估计的 $\sigma_x$ 更匹配数据特性
- Sobolev 去噪效果优于 Tikhonov，因为它编码了空间结构先验
- Sobolev 迭代解与频域闭式解数值差异应接近机器精度

### 实验结果

运行 `2.2-1.py` 后，生成四张图。实验结果如下：

---

#### 步骤1：去噪结果对比

![实验2.2-1 步骤1：去噪结果对比](<./第二章 配套实验/实验2.2-1/步骤1_去噪结果对比.png>)

**质量指标对比**：

| 方法 | 参数 | PSNR | SSIM |
|------|------|------|------|
| 含噪图像 | — | 20.03 dB | 0.427 |
| Tikhonov（数据估计 $\sigma_x$） | $\lambda = 0.1317$ | 19.17 dB | 0.445 |
| Tikhonov（主观先验 $\sigma_x = 1$） | $\lambda = 0.01$ | 20.10 dB | 0.429 |
| Sobolev | $\lambda = 0.05$ | 21.51 dB | 0.470 |
| Sobolev | $\lambda = 0.10$ | 22.60 dB | 0.504 |
| Sobolev | $\lambda = 0.20$ | 24.02 dB | 0.556 |

Tikhonov 两种参数选择方式展示了先验假设对结果的影响：
- 从数据估计 $\sigma_x = 0.276$：$\lambda = 0.1317$，正则化较强
- 主观先验 $\sigma_x = 1.0$：$\lambda = 0.01$，正则化较弱（因假设值域过大）

对比 PSNR（19.17 vs 20.10 dB）说明：先验假设需与数据特性匹配，不匹配的先验会引入偏差或不足。

---

#### 步骤2：剖面对比与性能

![实验2.2-1 步骤2：剖面对比与性能](<./第二章 配套实验/实验2.2-1/步骤2_剖面对比与性能.png>)

左图展示了 Sobolev 不同 $\lambda$ 下中心行的剖面对比，可见 $\lambda$ 越大，平滑程度越高。右图汇总了所有方法的 PSNR 和 SSIM 指标。

---

#### 步骤3：正则项形态与方差保持

![实验2.2-1 步骤3：正则项形态与方差保持](<./第二章 配套实验/实验2.2-1/步骤3_正则项形态与方差保持.png>)

左图对比了 L2（高斯先验）与 L1（Laplace/TV 先验）的正则项形态：L2 均匀惩罚所有值，L1 在零点附近惩罚更强，促稀疏。

右图展示了各方法在图像中心 $10 \times 10$ 区域的局部方差保持度：

| 方法 | 局部方差 |
|------|----------|
| 真实 | 0.0234 |
| Tikhonov（数据估计） | 0.0174 |
| Sobolev（$\lambda = 0.05$） | 0.0206 |
| Sobolev（$\lambda = 0.10$） | 0.0190 |
| Sobolev（$\lambda = 0.20$） | 0.0166 |

---

#### 步骤4：迭代解与频域闭式解对比

![实验2.2-1 步骤4：迭代解与闭式解对比](<./第二章 配套实验/实验2.2-1/步骤4_迭代解与闭式解对比.png>)

Sobolev 去噪可通过两种方法求解：
- **迭代法**：空域梯度下降，复杂度 $O(k \cdot n^2)$，$k$ 为迭代次数
- **闭式解**：频域 DFT 滤波，复杂度 $O(n^2 \log n)$

理论上两者等价。实验验证了数值差异：

| $\lambda$ | 最大差异 | 迭代解 PSNR | 闭式解 PSNR |
|-----------|----------|-------------|-------------|
| 0.05 | $2.69 \times 10^{-5}$ | 21.51 dB | 21.51 dB |
| 0.10 | $6.01 \times 10^{-5}$ | 22.60 dB | 22.60 dB |
| 0.20 | $1.88 \times 10^{-4}$ | 24.02 dB | 24.02 dB |

差异量级远小于像素值范围，验证了两种方法的等价性。

---

#### 核心知识点：MAP 与 MMSE 的等价性

在高斯-高斯模型下（高斯似然 + 高斯先验），后验分布为高斯分布。由于高斯分布对称，均值 = 众数，因此：

- MAP 估计（后验众数）= MMSE 估计（后验均值）
- Tikhonov 闭式解 $x = y/(1+\lambda)$ 既是 MAP 也是 MMSE

这是共轭先验的重要性质，详见第2章附录2A。

---

#### 高斯先验的特点总结

| 特性 | 简单高斯先验（Tikhonov） | 梯度高斯先验（Sobolev） |
|------|-------------------------|------------------------|
| 假设 | 图像值小 | 相邻像素相似 |
| 协方差 | $\sigma_x^2 I$（对角矩阵） | 涉及梯度算子 |
| 正则项 | $\|x\|_2^2$ | $\|\nabla x\|_2^2$ |
| 优点 | 有闭式解，计算高效 | 编码空间结构，比简单高斯更合理 |
| 缺点 | 过度平滑，丢失边缘 | 仍会过度平滑边缘 |
| 参数选择 | $\sigma_x$ 需与数据值域匹配 | $\lambda$ 影响平滑程度，需调参 |

---

## 二、Laplace 先验：从"值稀疏"到 L1 正则化

### 假设与推导

第二类结构化知识来自一条被反复验证的统计规律——**可压缩性**：自然信号总能在某个表示下，由少数显著分量承载绝大部分能量，其余分量接近于零。要把这条知识写进先验，所需的分布必须同时满足两点：强烈偏好零，又宽容少数大值。高斯分布做不到——它对大值的二次惩罚恰恰扼杀了显著分量。Laplace 分布正是满足这条要求的最简单选择。

如果图像在某个表示下是**稀疏**的——大多数分量为零或接近零，只有少数显著非零——更合适的先验是 Laplace 分布：

$$p(x) = \prod_{i=1}^n \frac{1}{2b} \exp\!\Big(-\frac{|x_i|}{b}\Big) = \frac{1}{(2b)^n} \exp\!\Big(-\frac{1}{b}\|x\|_1\Big)$$

其中 $b$ 控制分布的尺度：$b$ 越小，先验对零的偏好越尖锐。Laplace 与高斯的关键区别在于**零点处的概率集中**：Laplace 在 $x_i = 0$ 处有一个尖峰（不可导的峰），高斯在零处是光滑的。取负对数：

$$-\ln p(x) = \frac{1}{b}\|x\|_1 + \text{const}$$

正则项是 $\ell^1$ 正则项。配合高斯噪声数据项：

$$\hat{x}_{\text{MAP}} = \arg\min_x \|y - Ax\|_2^2 + \lambda\|x\|_1$$

这就是著名的 **LASSO**（最小绝对收缩与选择算子）或**稀疏正则化**。

### L1 促稀疏：为什么 $\ell^1$ 产生稀疏解？

$\ell^1$ 为什么能产生稀疏解？我们可以从三个不同的角度来建立直觉。

**概率角度**：Laplace 分布在零点有尖峰，概率质量高度集中在零附近。先验偏好 $x_i = 0$，MAP 自然会令许多分量精确为零。

**几何角度**：二维情形下，$\ell^1$ 的等高线是菱形，$\ell^2$ 的等高线是圆形。数据项 $\|y - Ax\|_2^2$ 的等高线是以最小二乘解为中心的椭圆。正则化解就是数据项椭圆与正则项等高线的切点：
- $\ell^2$（圆形）等高线与椭圆的切点通常在坐标轴之间——各分量均非零
- $\ell^1$（菱形）等高线的角点恰好在坐标轴上，椭圆更容易在角点处相切——某些分量精确为零

这个直觉在高维也成立：$\ell^1$ 球的角点在坐标轴上，优化更倾向在角点取到——这正是稀疏性的来源。

![图2-1：L1 vs L2 几何解释](<./第二章 配套实验/实验2.2-1/图2-1_L1_vs_L2几何解释.png>)

**优化角度**：$\ell^1$ 对 $x_i$ 的惩罚是线性的 $|x_i|$，其次梯度在 $x_i>0$ 时恒为 $+1$，在 $x_i<0$ 时恒为 $-1$。无论 $x_i$ 多小，把它推向零的力都一样大——小值被推到零，大值只受温和的线性惩罚。相比之下 $\ell^2$ 惩罚 $x_i^2$ 的梯度是 $2x_i$——$x_i$ 越小推力越弱，小值永远推不到精确零。

> **$\ell^0$ 与 $\ell^1$ 的关系**：最"理想"的稀疏度量是 $\ell^0$ 伪范数 $\|x\|_0 = \#\{i : x_i \neq 0\}$（非零分量个数），但最小化 $\|y-Ax\|_2^2 + \lambda\|x\|_0$ 是 NP 困难的（组合搜索）。$\ell^1$ 是 $\ell^0$ 的**最紧凸松弛**——在保持凸性（可优化）前提下，$\ell^1$ 是最接近 $\ell^0$ 的范数。这让稀疏正则化既理论可证收敛，又实践中真能促稀疏。

### 应用场景：稀疏表示框架

Laplace 先验的直接应用是假设图像本身稀疏——但自然图像的像素值通常并不稀疏。Laplace 真正发挥作用的是**稀疏表示框架**：

图像 $x$ 可在某个变换域（小波、字典学习）下被少量基函数逼近：

$$x = W\alpha, \quad \|\alpha\|_0 \ll n$$

其中 $W$ 是变换矩阵，$\alpha$ 是稀疏系数（例如 JPEG 中 $W$ 为分块 DCT 基，量化后绝大多数高频系数为零，仅保留极少数低频分量即可重建视觉可接受的图像）。对 $\alpha$ 施加 Laplace 先验（其负对数密度即 $\ell^1$ 范数）：

$$\min_\alpha \|y - AW\alpha\|_2^2 + \lambda\|\alpha\|_1$$

这是**合成形式**（synthesis form）稀疏正则化——在系数空间找稀疏解。当 $W$ 正交时与之严格等价的**分析形式**（analysis form）直接在图像空间操作：

$$\min_x \|y - Ax\|_2^2 + \lambda\|W^\top x\|_1$$

其中 $W^\top$ 是分析算子（如小波分解）。关键洞察：自然图像本身不稀疏，但在合适的变换域下可由少量系数有效表示。Laplace 先验 + 合适变换 = 稀疏性先验。该方法的重要性并不只在于提供了一个可解的优化问题，而在于它把"稀疏"从对信号本身苛刻的要求，转化为对表示域的一次选择——先验与表示共同构成知识，二者缺一不可。

### 局限：稀疏假设需要前提

Laplace 的核心假设是"稀疏"。如果图像在选定表示下确实稀疏（如小波稀疏），L1 效果优秀。但若表示选错——图像在该表示下并不稀疏——Laplace 先验会强制引入稀疏性，导致重建偏差。

### 从 ℓ¹ 到 ℓ⁰：迭代硬阈值与重加权逼近

上面把 $\ell^1$ 定位为 $\ell^0$ 的**最紧凸松弛**——理论最优凸近似。这里补充**实用地逼近 $\ell^0$** 的三类算法，都直接面向"非零分量很少"这一真实 $\ell^0$ 目标：

- **迭代硬阈值（Iterative Hard Thresholding, IHT）**：合成稀疏模型 $y=A\alpha$ 下，交替做"梯度步 + 硬阈值"：
  $$\alpha^{k+1} = H_\lambda\!\left(\alpha^k + A^\top(y - A\alpha^k)\right)$$
  其中 $H_\lambda$ 只保留绝对值大于 $\lambda$ 的分量、其余置零。这类"测量数远少于未知数、却靠稀疏性精确恢复"的问题统称**压缩感知**；IHT 正是它在约束等距（RIP）条件下最朴素的迭代求解器。
- **重加权 ℓ¹（reweighted ℓ¹ / IRL1）**：把固定权重 $\lambda\|\alpha\|_1$ 换成迭代权重 $\sum_i w_i^{(k)}|\alpha_i|$，其中 $w_i^{(k)} = 1/(|\alpha_i^{(k)}|+\varepsilon)$。权重随解更新，目标越来越逼近 $\ell^0$（小系数被更强惩罚）；实验上比固定 ℓ¹ 更稀疏、更接近真 ℓ⁰ 解。
- **贪婪法（匹配追踪 / OMP）**：每次迭代挑一个最能降低残差的原子（基函数），逐步搭起稀疏支撑集——与 IHT 的"全局阈值"互补。

**逆问题中的应用**：超分辨荧光显微（super-resolution microscopy）中，点光源在图像中天然高度稀疏，直接用 $\ell^{2}\text{-}\ell^{0}$ 模型即可恢复亚衍射极限的发光点位置。这类"对象本身稀疏，于是稀疏性直接当先验用"的思路，正是第 13、16 章要展开的主线——把稀疏先验用到 CT、MRI 等逆问题的重建中，此时并不需要 TV 或学习类先验。注意这和 2.2 节"ℓ¹ 是最紧凸松弛"并不矛盾，而是**在同一条稀疏先验主线上，从凸近似走向更贴近 ℓ⁰ 的实用算法**。

---

## 实验 2.2-2：Laplace 先验的稀疏机制——为什么它能把系数精确置零

上一节从几何、概率、优化三个视角论证了 Laplace 先验的零点奇性"应该"促稀疏。本实验用一个一维稀疏恢复问题，把抽象的零点尖峰变成肉眼可见的"置零"效果，并与同强度的高斯先验作直接对照；进而延伸到压缩感知设定，对比 IHT、重加权 ℓ¹、OMP 三种"从 ℓ¹ 到 ℓ⁰"的实用逼近算法。

### 实验场景

实验 `2.2-2.py` 分为两部分：

**Part A（软阈值）** 构造一条只有 5 个非零系数的 50 维稀疏信号 $x^*$，加高斯噪声得观测 $y$，在同一个 $\lambda$ 下分别用 Laplace 先验（LASSO，闭式软阈值）和高斯先验（Ridge，闭式缩放）做 MAP 估计，对比两者恢复出的系数。这一 Part 对应本章"Laplace 先验的零点奇性促稀疏"及"三视角统一"的核心论述。

**Part B（压缩感知）** 把视角搬到"观测维度 $M$ 远小于信号维度 $N$"的**欠定**情形——这正是压缩感知的典型设定：依靠稀疏性，用极少量观测恢复出信号。具体做法是把 Laplace 先验真正作用的"变换域稀疏系数 $\alpha$"搬进合成形式 $\min_\alpha\|y-A\alpha\|_2^2 + \lambda\|\alpha\|_1$，构造一个真正欠定（$A$ 为 $80\times256$，$M<N$）的 1D 压缩感知问题，对比 IHT、重加权 ℓ¹、OMP 三种算法如何把 $\ell^1$ 推向更贴近 $\ell^0$ 的解。这一 Part 对应上文"从 ℓ¹ 到 ℓ⁰：迭代硬阈值与重加权逼近"与"应用场景：稀疏表示框架"两部分的内容。

### 实验目的

1. **验证零点奇性的"硬置零"机制**：看 L1 软阈值在死区 $[-\lambda, \lambda]$ 内把小系数精确压成 0，而 L2 只均匀缩小、不产生任何精确零。
2. **公平对比同 $\lambda$ 下的恢复质量**：用 MSE 与支撑恢复率量化 Laplace 先验 vs 高斯先验的差距，理解为何 $\ell^1$ 促稀疏而 $\ell^2$ 不促稀疏。
3. **把三视角统一到一次实验上**：先验密度图（概率视角）、系数估计对比（恢复效果）、软阈值函数（优化视角）三者互相印证。
4. **验证"从 ℓ¹ 到 ℓ⁰"的三类实用算法**：在欠定压缩感知条件下对比 IHT（硬阈值）、重加权 ℓ¹（迭代权重）、OMP（贪心匹配）对 $\ell^0$ 的不同逼近策略。
5. **展示不同逼近策略的代价与权衡**：三种策略在恢复精度与非零个数上的不同表现，直观说明"逼近 ℓ⁰"不是唯一答案，而是有方向的工程选择。

### 实验输入

| 参数 | Part A | Part B |
|------|--------|--------|
| 信号维度 $N$ | 50 | 256 |
| 真实非零系数个数 $K$ | 5 | 10 |
| 观测数 $M$ | —（$A=I$，纯去噪） | 80（$M<N$，欠定） |
| 正则强度 $\lambda$ | 0.6 | —（算法自适应） |
| 观测 SNR | 20 dB | 30 dB |
| 随机种子 | 42 | 7 |
| 测量矩阵 | — | 高斯随机矩阵（列归一化） |

### 预期结果

- **Part A**：Ridge 的精确置零数应为 0——$\ell^2$ 只均匀缩小、永不让分量精确为零；LASSO 应产生大量精确零（软阈值死区 $[-\lambda,\lambda]$ 内的观测值全部被压到零），MSE 应低于 Ridge。但 LASSO 支撑恢复率可能反低于 Ridge——弱信号被噪声淹没后，软阈值会将其和噪声一起"误杀"，这是 L1 促稀疏的固有代价。
- **Part B**：IHT 与 OMP 应恢复出恰好 $K=10$ 个非零分量（硬阈值和贪心都"猜对"支撑集规模）；重加权 ℓ¹ 相对误差可能最低，但非零个数可能少于 $K$（比固定 ℓ¹ 更贴近 ℓ⁰，代价是漏掉被噪声压弱的支撑）；三者相对误差预计在 ~0.13–0.21 区间，均远优于随机猜测——验证压缩感知中"稀疏先验 + 欠定测量"的可恢复性。

### 实验结果

运行 `2.2-2.py` 后，脚本打印对比并生成五张图。实验结果如下：

---

#### Part A：软阈值——Laplace 先验的"硬置零"机制

**质量指标对比**：

| 方法 | 先验 | MSE | 支撑恢复率 | 精确置零数 |
|------|------|-----|-----------|-----------|
| Ridge (L2) | 高斯 $\mathcal{N}(0, 1/\lambda)$ | 0.0985 | 100.00% | 0 |
| LASSO (L1) | Laplace $(0, b)$ | 0.0427 | 80.00% | 41 |

**关键观察**：

- **Ridge 的精确置零数为 0**——$\ell^2$ 二次惩罚把每个小系数都按比例缩小，但从不让任何分量真正等于 0。
- **LASSO 精确置零了 41 个分量**（50 个分量中，观测值落在死区内的全部被压成精确零），MSE（0.0427）反而低于 Ridge（0.0985），因为"该压成零的确实压成了零"。这正是一维情形下零点奇性 → 稀疏性的直接证据。
- **反直觉细节**：LASSO 的支撑恢复率（80%）反而低于 Ridge（100%）。原因是软阈值在 $|y_i| < \lambda$ 时，会把**真支撑里被噪声压到死区内的小峰**一起误杀。这恰好说明 L1 促稀疏是有代价的——$\lambda$ 越大、噪声越强，越容易"错杀"弱信号。实践中这就是 LASSO 需要交叉验证调 $\lambda$、且常在小波/DCT 变换域使用的原因（变换后信号更稀疏，弱峰更少被淹没）。

---

##### 图1：先验密度对比（概率视角）

![实验2.2-2 图1：先验密度对比](<./第二章 配套实验/实验2.2-2/图1_先验密度对比.png>)

高斯与 Laplace 的先验密度画在同一坐标系。高斯在原点光滑、平缓；Laplace 在原点出现不可导的尖峰（cusp）。这个"尖"就是先验强烈偏好零的概率表述——它是一切稀疏性的根源，也是下一节 TV 先验（把尖峰搬到梯度空间）的同一套机制。

---

##### 图2：系数估计对比（恢复效果）

![实验2.2-2 图2：系数估计对比](<./第二章 配套实验/实验2.2-2/图2_系数估计对比.png>)

三张茎叶图从左到右依次是真实信号、Ridge 估计、LASSO 估计。Ridge 将每个非零峰按比例压低，背景中仍残留一片未被清零的细小分量；LASSO 不仅峰高更准，背景也被彻底清空为零。同 $\lambda$ 下，LASSO 的 MSE（0.043）明显低于 Ridge（0.099）。

---

##### 图3：软阈值函数（优化视角）

![实验2.2-2 图3：软阈值函数](<./第二章 配套实验/实验2.2-2/图3_软阈值函数.png>)

图中画出三种映射：恒等线 $y=x$（无正则）、Ridge 的缩放直线 $x = y/(1+\lambda)$、LASSO 的软阈值曲线。Ridge 是过原点的一条斜率小于 1 的直线——永远不产生零；软阈值在死区 $[-\lambda, \lambda]$ 内被**压平到 0**（图中红色阴影区），两侧斜率恢复为 1。这个"压平"动作就是 $\ell^1$ 不可导尖点带来的直接后果，也是几何、概率、优化三视角在一条曲线上的汇合。

---

#### Part B：从 ℓ¹ 到 ℓ⁰ —— 压缩感知下的三种逼近

**算法对比**：

| 算法 | 策略 | 相对误差 | 恢复非零个数 |
|------|------|---------|------------|
| IHT（硬阈值 → ℓ⁰） | 梯度步 + top-K 截断 | 0.1405 | 10 |
| Reweighted ℓ¹ | 外层更新权重 + 内层 ISTA | 0.1336 | 7 |
| OMP（贪心） | 逐原子匹配 + 最小二乘 | 0.2062 | 10 |

**关键观察**：

- **IHT 与 OMP 都恢复出恰好 10 个非零分量**（= 真支撑大小 $K$）。硬阈值的"只留 top-K"和贪心的"逐原子搭建"都成功猜对了支撑集规模；相对误差约 0.14~0.21，主要来自噪声对弱系数的淹没。
- **Reweighted ℓ¹ 相对误差最低（0.1336），却只恢复出 7 个非零**——它把被噪声压到接近零的弱系数**进一步压成精确零**，比固定 ℓ¹ 更稀疏，正是上文"重加权 ℓ¹ 比固定 ℓ¹ 更接近真 ℓ⁰ 解"论断的实证，代价是漏掉了 3 个弱支撑。
- 三者共同印证上文关于"从 ℓ¹ 到 ℓ⁰"的结论："在同一条稀疏先验主线上，从凸近似（ℓ¹）走向更贴近 ℓ⁰ 的实用算法"——ℓ¹ 不是终点，而是逼近 ℓ⁰ 的一个可优化的起点。

---

##### 图4：1D 恢复对比（真值 vs 三算法）

![实验2.2-2 图4：1D 恢复对比](<./第二章 配套实验/实验2.2-2/图4_1D恢复对比.png>)

四张茎叶图从左到右：真实稀疏系数、IHT 恢复、重加权 ℓ¹ 恢复、OMP 恢复。在 $M\ll N$ 的欠定条件下，三算法都重建出了 $K=10$ 个尖峰；与 Part A 中"纯加噪、$A=I$"的情形不同，这里没有逐分量的闭式软阈值解，必须靠迭代/贪心才能在测量矩阵 $A$ 的列空间里找回真支撑——这也是压缩感知比"直接置零"更难也更有意思的地方。

---

##### 图5：收敛曲线（IHT vs 重加权 ℓ¹）

![实验2.2-2 图5：收敛曲线](<./第二章 配套实验/实验2.2-2/图5_收敛曲线.png>)

半对数坐标下，IHT（每步硬阈值）与重加权 ℓ¹（外层迭代）的相对误差都随迭代下降并趋于平稳。IHT 收敛快而直接（硬阈值 = 显式 ℓ⁰ 投影）；重加权 ℓ¹ 初期像普通 ℓ¹，随权重更新逐步"收紧"到更稀疏的解。两条曲线共同说明：ℓ¹ 不是终点，而是逼近 ℓ⁰ 的一个可优化的起点。

---

> 若把真值 $\alpha^*$ 改成"大量中小幅度系数、只有极少数真正为零"（即表示选错、并不稀疏），IHT/OMP 强制保留 $K=10$ 个支撑会发生什么？——这正说明稀疏先验是把"双刃剑"：表示选对则威力巨大，表示选错则引入系统性偏差。后续第 13、16 章会看到，逆问题里常把稀疏性直接作为先验（如超分辨荧光显微），前提是对象在该表示下确实稀疏。

---

## 三、TV 先验：从"梯度稀疏"到全变差正则化

### 假设与推导

第三类结构化知识不再关注数值或系数本身，而是关注几何：自然场景由物体构成，物体内部亮度近似恒定，物体之间以锐利边界分隔。因此梯度在绝大多数位置为零、在极少数位置显著——这就是"梯度稀疏"。注意，这一假设既不是"值小"也不是"值稀疏"：自然图像的像素值本身既不小也不稀疏，真正稀疏的是它的**差分**。

自然图像有个重要统计特性：**梯度稀疏**——大部分区域平滑（梯度接近零），只有少数位置有边缘（梯度较大）。TV（Total Variation，全变差）先验直接编码了这一假设。

> **梯度稀疏的实证依据：**
> "梯度稀疏"并非凭空设定的先验信念，而是自然图像的**实测统计**。对约 1500 万（$15$ million）个 $2\times2$ 图像块的梯度 $\nabla u$ 做直方图，其对数统计（log-statistics）形状像一个**"倒扣的冰淇淋蛋筒"（upside-down ice-cream cone）**——即梯度分布是**重尾（heavy-tailed）**的：绝大多数梯度接近零，但拖出一条延伸到很大值的长尾（对应边缘）。下图展示了真实自然图像在多角度各向同性采样下的 3D 对数联合梯度分布：
>
> ![图2-2：图像梯度的3D对数联合分布](<图2-2_图像梯度3D对数联合分布.png>)
>
> **图2-2** 水平轴为水平梯度 $\nabla_x$ 和垂直梯度 $\nabla_y$，纵轴为对数概率密度 $\ln P(\nabla_x,\nabla_y)$。尖峰高耸于原点（绝大多数像素对的梯度接近零），向四周拖出厚重的长尾（边缘处的大梯度）——这就是"倒扣冰淇淋蛋筒"的直观形态。
>
> 对这条负对数统计曲线做最简单的最小二乘拟合，得到的正是 $\ell_2$ 范数，于是自然导出全变差
> $$\operatorname{TV}(u)=\|Du\|_{2,1}=\sum_{i,j}\sqrt{(u_{i+1,j}-u_{i,j})^2+(u_{i,j+1}-u_{i,j})^2}$$
> 这正是 Rudin–Osher–Fatemi（1992）的 ROF 模型。换言之，TV 不是人为选择的正则项，而是对自然图像梯度**重尾统计的最小二乘拟合结果**；它也解释了为何 TV 解允许边缘（不连续）：统计上它正确区分了"平滑区域（梯度≈0）"与"边缘（梯度大但概率非零）"，而不是像 $\ell^2$ 那样对大梯度施加二次惩罚。

TV 先验假设图像的梯度服从 Laplace 分布：

$$\nabla x \sim \text{Laplace}(0, bI)$$

取负对数：

$$-\ln p(x) = \frac{1}{b}\|\nabla x\|_1 + \text{const}$$

其中 $\|\nabla x\|_1$ 就是全变差 $\text{TV}(x)$。配合高斯噪声数据项，得到 **Rudin-Osher-Fatemi（ROF）模型**（1992）：

$$\min_x \|y - Ax\|_2^2 + \lambda\,\text{TV}(x)$$

离散情形下，各向同性 TV 定义为：

$$\text{TV}(x) = \sum_i \sqrt{(D_h x)_i^2 + (D_v x)_i^2}$$

其中 $D_h$、$D_v$ 分别是水平和垂直有限差分算子。也可用各向异性 TV：$\|\nabla x\|_1 = \|D_h x\|_1 + \|D_v x\|_1$。还应注意：这一先验只作用于梯度，常数分量不受任何惩罚——这恰好对应一条物理事实：先验陈述的是图像的结构（边缘在哪里），而非整体亮度（图像有多亮）。

### TV 与 Laplace 先验的关系

仔细看：**TV 先验 = 对梯度施加 Laplace 先验**。这和"对 $x$ 本身施加 Laplace 先验"有本质区别：
- Laplace 先验（$\|x\|_1$）：假设像素值本身稀疏——适用于稀疏表示框架
- TV 先验（$\|\nabla x\|_1$）：假设像素梯度稀疏——直接适用于自然图像

从 Laplace 到 TV，假设从"$x$ 本身稀疏"变为"$\nabla x$ 稀疏"——这一转变意味着先验的作用对象从信号本身移到了信号的结构（边缘）上，从而更贴近自然图像的真实统计。

### 核心优势：同时平滑与保边

TV 正则化的核心优势是**同时平滑与保边**：
- **平坦区域**：梯度接近零，$\|\nabla x\|_1$ 惩罚很轻，平滑去噪类似高斯先验
- **边缘处**：梯度较大，但 $\ell^1$ 只产生线性代价（不像 $\ell^2$ 二次代价过度惩罚大梯度），边缘得以保留

从认知角度看，TV 的软阈值发生在梯度域：小梯度被判定为噪声并压为零，大梯度被判定为边缘并保留——这与像素域的软阈值是同一条"证据裁决"规则在不同空间中的再现。

这种"平滑但不模糊边缘"使 TV 成为最成功的显式先验之一。函数空间上，TV 解属于 **BV（有界变差）空间**而非 Sobolev 空间——BV 允许不连续函数（边缘），Sobolev 要求函数连续可微。

### 已知局限：阶梯效应

TV 假设"梯度稀疏"等价于假设图像是**分段常数**的。这对卡通类图像（大平坦区域 + 锐利边缘）很合适，但对渐变区域就有问题：
- **天空渐变**：真实亮度从地平线到天顶平滑渐变，但 TV 假设分段常数，把渐变近似成一系列阶梯——**阶梯效应**（staircase effect）
- **皮肤过渡**：面部光影柔和过渡被近似成色块
- **斜坡区域**：任何缓慢变化区域都会被"楼梯化"

从更深层次看，阶梯效应是信念强制的结果：当现实包含渐变，而先验坚持"梯度要么为零、要么显著"时，优化器只能以有限次跃变去逼近连续变化。这不是算法的失败，而是先验与真实结构错位的直接后果——先验编码什么，解就只能呈现什么。

阶梯效应的根源是 TV 只用了一阶信息（梯度），把函数空间限制为分段常数。改进思路是引入二阶信息——附录 2B 的 **TGV**（Total Generalized Variation）通过同时惩罚一阶和二阶导数，允许分段仿射解，有效缓解阶梯效应。

> **TV 先验的启示**：TV 的成功和局限都来自同一个假设——"梯度稀疏"。假设与真实一致（卡通图像），TV 优秀；假设过于简化（渐变区域），TV 引入系统性偏差。这印证了 2.1 节的结论：**好先验编码正确知识，差先验引入系统性偏差。**

---

## 实验 2.2-3：TV 先验的局限——阶梯效应

实验 `2.2-3.py` 演示 **Sobolev 平滑先验与 TV 全变差先验的去噪效果对比**，揭示两种先验在保边性和平滑性上的本质差异，以及 TV 先验的阶梯效应局限。

### 实验场景

实验考虑纯去噪问题：观测模型 $y = x + \varepsilon$，其中 $\varepsilon \sim \mathcal{N}(0, \sigma^2 I)$ 为加性高斯白噪声。对比两种先验：

**Sobolev 平滑先验**：假设 $\nabla x \sim \mathcal{N}(0, \sigma_\nabla^2 I)$，正则项为 $\|\nabla x\|_2^2$。MAP 估计通过梯度下降迭代求解。

**TV 全变差先验**：假设 $\nabla x \sim \text{Laplace}(0, bI)$，正则项为 $\|\nabla x\|_1$。MAP 估计通过近端梯度下降（PGD）求解，其中近端算子为软阈值操作。

### 实验目的

1. **对比 Sobolev 与 TV 先验的去噪效果**：展示 $\ell^2$ 梯度惩罚与 $\ell^1$ 梯度惩罚的定性差异
2. **揭示 TV 先验的保边优势**：TV 能保留锐利边缘，而 Sobolev 会过度平滑
3. **展示 TV 先验的阶梯效应局限**：在渐变区域出现阶梯状伪影
4. **理解两种先验的适用场景**：Sobolev 适合平滑区域，TV 适合分段常数区域

### 实验输入

| 参数 | 设置 |
|------|------|
| 测试图像 | camera，$128 \times 128$ 像素 |
| 噪声模型 | 加性高斯白噪声，$\sigma = 0.1$ |
| Sobolev 参数 | $\lambda = 0.15$，控制梯度平滑强度 |
| TV 参数 | $\alpha = 0.15$，控制全变差正则化强度 |
| 求解方法 | Sobolev：梯度下降；TV：近端梯度下降（PGD） |

> **参数选择说明**：Sobolev 的 $\lambda$ 与 TV 的 $\alpha$ 量纲不同——$\lambda$ 惩罚 $\|\nabla x\|_2^2$（二次惩罚），$\alpha$ 惩罚 $\|\nabla x\|_1$（线性惩罚）。本实验取相近数值以展示定性差异，非严格最优对比。

### 预期结果

- TV 先验在边缘处表现优于 Sobolev：边缘更锐利，PSNR 和 SSIM 更高
- Sobolev 先验在平滑区域表现自然，但边缘模糊
- TV 先验在渐变区域会出现阶梯状伪影（staircase effect）
- 两种先验各有优劣，适用于不同类型的图像区域

### 实验结果

运行 `2.2-3.py` 后，生成三张图。实验结果如下：

---

#### 步骤1：去噪结果对比

![实验2.2-3 步骤1：TV去噪对比](<./第二章 配套实验/实验2.2-3/步骤1_TV去噪对比.png>)

**质量指标对比**：

| 方法 | PSNR | SSIM |
|------|------|------|
| 含噪图像 | 20.38 dB | 0.354 |
| Sobolev（smoothness prior） | 23.64 dB | 0.459 |
| TV 先验 | 26.99 dB | 0.762 |

TV 先验在整体质量指标上显著优于 Sobolev 先验：
- PSNR 提升 3.35 dB（23.64 → 26.99 dB）
- SSIM 提升 0.303（0.459 → 0.762）

这主要归功于 TV 的保边能力：边缘锐利度对 SSIM 贡献显著。Sobolev 的 $\ell^2$ 梯度惩罚对大梯度（边缘处）施加二次代价，导致边缘过度平滑；而 TV 的 $\ell^1$ 惩罚只产生线性代价，边缘得以保留。

---

#### 步骤2：阶梯效应分析

![实验2.2-3 步骤2：阶梯效应分析](<./第二章 配套实验/实验2.2-3/步骤2_阶梯效应分析.png>)

左图展示了图像中心行的剖面对比，可以清晰看到：
- **Sobolev 剖面**：平滑连续，但边缘处过渡缓慢（过度平滑）
- **TV 剖面**：边缘处跃变陡峭（保边），但在渐变区域出现明显的阶梯状跳跃

右图展示了梯度幅值分布：
- **Sobolev**：梯度分布连续，边缘处梯度平缓过渡
- **TV**：梯度分布稀疏，大部分区域梯度接近零，边缘处梯度集中

**阶梯效应的根源**：TV 先验假设梯度稀疏，等价于假设图像是**分段常数**的。对于真实图像中的渐变区域（如天空渐变、光影过渡），TV 会将连续变化近似为一系列阶梯——这就是阶梯效应（staircase effect）。

---

#### 步骤3：正则项形态与性能对比

![实验2.2-3 步骤3：正则项与性能对比](<./第二章 配套实验/实验2.2-3/步骤3_正则项与性能对比.png>)

**左图：正则项形态对比**

对比了两种正则项对梯度的惩罚形态：
- **Sobolev（$\|\nabla x\|_2^2$）**：二次惩罚，大梯度受到强烈抑制
- **TV（$\|\nabla x\|_1$）**：线性惩罚，大梯度只受到温和抑制

**中图：梯度幅值直方图**

展示了三种图像（原始、Sobolev 去噪、TV 去噪）的梯度幅值分布：
- **原始图像**：梯度分布连续，边缘处梯度平缓过渡
- **Sobolev 去噪**：梯度分布与原始相似，但整体幅值降低（过度平滑）
- **TV 去噪**：产生大量零梯度（图中 TV 零梯度比例约 37.6%，Sobolev 约 28.1%），验证了 TV 偏好分段常数函数的特性

**右图：去噪性能对比**

汇总了 PSNR 和 SSIM 指标：
- TV 先验在两项指标上均显著优于 Sobolev 先验
- 但需注意：PSNR 高不代表视觉自然——TV 的高分主要来自边缘锐利度，阶梯效应在 PSNR 中不易体现

两种先验的特点汇总：

| 特性 | Sobolev 先验 | TV 先验 |
|------|-------------|---------|
| 梯度惩罚 | 二次（$\ell^2$） | 线性（$\ell^1$） |
| 边缘保持 | 不能（过度平滑） | 能（保边） |
| 渐变区域 | 自然平滑 | 阶梯状伪影 |
| 函数空间 | Sobolev $H^1$ | BV（有界变差） |
| 适用场景 | 平滑图像 | 卡通/分段常数图像 |

---

#### TV 先验的特点总结

**优势**：
1. **保边性**：$\ell^1$ 惩罚对大梯度的代价是线性的，边缘不会被过度平滑
2. **稀疏性**：梯度稀疏假设对自然图像合理——大部分区域平滑，少数位置有边缘
3. **凸性**：优化问题可高效求解（近端方法、原始-对偶方法）

**局限**：
1. **阶梯效应**：渐变区域出现阶梯状伪影，因为 TV 偏好分段常数函数
2. **分段常数假设**：无法自然表示渐变、斜坡等缓慢变化的区域
3. **参数敏感**：$\alpha$ 需要仔细调节，过大导致过度阶梯化，过小导致去噪不足

**改进方向**：
- **TGV**（Total Generalized Variation，见附录2B）：引入二阶导数信息，允许分段仿射解，有效缓解阶梯效应
- **隐式先验**（见2.4节）：从数据中学习先验，而非手工设计正则项

---

## 四、三种先验的对比：从简到繁的递进

三种经典先验构成一个清晰的递进结构，每一层都是对上一层的修正：

| | 高斯先验 | Laplace 先验 | TV 先验 |
|---|---|---|---|
| **假设** | $x$ 的值小 | $x$ 的值稀疏 | $\nabla x$ 稀疏 |
| **先验分布** | $x \sim \mathcal{N}(0, \sigma_x^2 I)$ | $x \sim \text{Laplace}(0, bI)$ | $\nabla x \sim \text{Laplace}(0, bI)$ |
| **正则项** | $\|x\|_2^2$ | $\|x\|_1$ | $\|\nabla x\|_1$ |
| **正则化方法** | Tikhonov / 岭回归 | LASSO / 稀疏正则化 | ROF / 全变差正则化 |
| **解的形态** | 平滑（所有值被均匀压缩） | 稀疏（小值被推到零） | 分段平滑（平坦+保边） |
| **函数空间** | $L^2$ / Sobolev $H^1$ | $\ell^1$ 稀疏空间 | BV（有界变差） |
| **边缘保持** | 不能（过度平滑） | 间接（需稀疏表示） | 直接保边 |
| **局限** | 过度平滑，丢边缘 | 需稀疏表示前提 | 阶梯效应 |

### 递进逻辑

```
高斯：假设"值小" → 平滑但丢边缘
  │
  │ 修正：从"值小"到"值稀疏"
  ▼
Laplace：假设"值稀疏" → 保稀疏但需稀疏表示前提
  │
  │ 修正：从"值稀疏"到"梯度稀疏"
  ▼
TV：假设"梯度稀疏" → 保边但有阶梯效应
```

每次修正都让假设更贴近真实图像的统计特性，但也让数学结构更复杂：Tikhonov 有闭式解，LASSO 需迭代优化（近端方法），TV 优化更复杂（原始-对偶方法）。同时每种先验都保留了前一种的某些局限——TV 解决了过度平滑，但引入新问题（阶梯效应）。从更深层次看，这条递进线索并非偶然的算法演化，而是"先验即结构化知识"这一思想的逐步展开：每前进一步，我们就把一条更真实的统计规律写进负对数密度，让解的形态被知识本身而非被计算便利所决定。

### 零点奇性的关键作用

高斯与 Laplace 的本质差异在于**零点处的概率集中**：
- **高斯分布**：$p(x)$ 在零点光滑可导，概率质量从零向两侧平缓扩散——没有"偏好零"的特殊力量。$\ell^2$ 正则项只能均匀缩小值，无法产生精确零。
- **Laplace 分布**：密度在零点不可导（尖峰/cusp），$p(x)$ 从两侧以指数速率衰减——零点处的"尖"意味着先验强烈偏好零。$\ell^1$ 正则项的软阈值效应（soft thresholding）将小值推到精确零。

这个差异解释了为什么 $\ell^1$ 促稀疏而 $\ell^2$ 不促稀疏——**零点奇性是稀疏性的概率根源**。这里所谓"奇性"，指的不是密度值无穷大（Laplace 在零点的密度 $1/(2b)$ 是有限值），而是密度及其负对数在零点不可导所形成的尖点：正是这个尖点，使 MAP 的最优性条件允许解精确落在零上。软阈值规则因此可以读作一条证据裁决准则：观测证据不足阈值时，先验对零的偏好压过数据，分量被判定为噪声而精确归零；观测超过阈值时，只有超出证据的部分被保留。TV 先验把这种零点奇性从像素空间转移到了梯度空间：梯度在零点有尖峰偏好，意味着大部分区域梯度被推到零（平滑），少数位置梯度非零（边缘）。

---

## 实验 2.2-4：经典先验族全景对比——高斯、Laplace 与 TV

实验 `2.2-4.py` 演示**三种经典先验（高斯、Laplace、TV）在同一去噪任务下的重建效果对比**，通过可视化重建图像、PSNR-lambda 扫描曲线和重建误差柱状图，全景展示"选先验就是选假设"的核心思想。

### 实验场景

实验考虑纯去噪问题：观测模型 $y = x + \varepsilon$，其中 $\varepsilon \sim \mathcal{N}(0, \sigma^2 I)$ 为加性高斯白噪声。在同一噪声水平下，分别施加三种先验并求解 MAP 估计：

**高斯先验（Tikhonov）**：假设 $x \sim \mathcal{N}(0, \sigma_x^2 I)$，正则项为 $\|x\|_2^2$。MAP 估计有闭式解：

$$\hat{x} = \frac{y}{1 + \lambda}, \quad \lambda = \frac{\sigma^2}{\sigma_x^2}$$

**Laplace 先验（LASSO）**：假设 $x \sim \text{Laplace}(0, bI)$，正则项为 $\|x\|_1$。MAP 估计为软阈值操作：

$$\hat{x}_i = \text{sign}(y_i) \cdot \max(|y_i| - \lambda, 0), \quad \lambda = \frac{\sigma^2}{b}$$

**TV 先验（ROF 模型）**：假设 $\nabla x \sim \text{Laplace}(0, bI)$，正则项为 $\|\nabla x\|_1$。MAP 估计通过 Chambolle 对偶投影算法迭代求解：

$$\min_x \frac{1}{2}\|x - y\|_2^2 + \lambda\,\text{TV}(x)$$

### 实验目的

1. **验证"选先验就是选假设"**：三种先验在同一任务下产生截然不同的重建结果，假设越贴近真实图像统计特性，效果越好
2. **展示 LASSO 在像素域的局限性**：自然图像的像素值本身不稀疏，直接在像素域施加 $\ell^1$ 正则化效果有限
3. **对比三种方法的 PSNR-lambda 曲线**：展示不同先验对正则化参数的敏感度和最优工作区间
4. **理解参数量纲差异**：$\|x\|_2^2$、$\|x\|_1$、$\|\nabla x\|_1$ 作用于不同域（像素值 vs 梯度值），$\lambda$ 的数值大小不可直接对比

### 实验输入

| 参数 | 设置 |
|------|------|
| 测试图像 | camera，$128 \times 128$ 像素 |
| 噪声模型 | 加性高斯白噪声，$\sigma = 0.1$ |
| 高斯先验参数 | $\sigma_x = 1.0$，$\lambda = \sigma^2/\sigma_x^2 = 0.01$ |
| Laplace 先验参数 | $b = 0.5$，$\lambda = \sigma^2/b = 0.02$ |
| TV 先验参数 | $\lambda = 0.15$（手动调参），Chambolle 迭代 200 次 |
| PSNR 扫描 | $\lambda \in [10^{-4}, 10^1]$，30 个对数均匀采样点 |

> **参数量纲说明**：Tikhonov 和 LASSO 的 $\lambda$ 作用于像素域，TV 的 $\lambda$ 作用于梯度域。三者量纲不同，$\lambda$ 数值大小不可直接对比。PSNR 数值仅在**同一方法内**不同 $\lambda$ 之间可比，跨方法对比 PSNR 需谨慎。

### 预期结果

- TV 先验 PSNR 显著高于高斯和 Laplace 先验，因为"梯度稀疏"假设最贴近自然图像
- 高斯和 Laplace 先验 PSNR 接近，均显著低于 TV——两者假设（值小/值稀疏）对自然图像不够准确
- LASSO 在像素域的直接应用效果有限，验证了稀疏表示框架的必要性
- PSNR-lambda 曲线展示三种方法的不同敏感度：TV 曲线峰值更尖锐，对 $\lambda$ 更敏感

### 实验结果

运行 `2.2-4.py` 后，生成两张图。实验结果如下：

---

#### 步骤1：三种先验重建对比

![实验2.2-4 步骤1：三种先验对比](<./第二章 配套实验/实验2.2-4/步骤1_三种先验对比.png>)

**质量指标对比**：

| 方法 | 假设 | 正则项 | $\lambda$ | PSNR |
|------|------|--------|-----------|------|
| 含噪图像 | — | — | — | 20.03 dB |
| Tikhonov（高斯先验） | 值小 | $\|x\|_2^2$ | 0.0100 | 20.05 dB |
| LASSO（Laplace 先验） | 值稀疏 | $\|x\|_1$ | 0.0200 | 20.05 dB |
| TV（TV 先验） | 梯度稀疏 | $\|\nabla x\|_1$ | 0.1500 | 26.91 dB |

结果分析：
- **TV 先验** PSNR 达到 26.91 dB，显著优于其他两种方法（+6.86 dB），验证了"梯度稀疏"假设对自然图像的适用性
- **高斯先验**和**Laplace 先验** PSNR 接近（20.05 dB），仅比含噪图像略好——两者的假设（值小/值稀疏）对自然图像像素值本身不够准确
- **LASSO 的局限性**：直接在像素域施加 $\ell^1$ 正则化，将大量像素值推到零，导致重建图像出现大量黑色区域。这正是 LASSO 需要在变换域（小波、DCT）中使用的原因——自然图像在变换域下才是稀疏的
- 右下角剖面图清晰展示：TV 重建（红色）最贴近真实信号（黑色虚线），Tikhonov（蓝色）和 LASSO（绿色）偏离较大

---

#### 步骤2：正则项形态与性能对比

![实验2.2-4 步骤2：正则项形态与性能对比](<./第二章 配套实验/实验2.2-4/步骤2_正则项形态与性能对比.png>)

**左图：正则项形态对比**

对比了 L2（高斯先验）和 L1（Laplace/TV 先验）的惩罚函数形态：
- **L2（$t^2$）**：二次惩罚，对小值惩罚轻、对大值惩罚重——均匀压缩所有值
- **L1（$|t|$）**：线性惩罚，对所有非零值施加相同的单位梯度——小值被推向零，大值只受温和抑制

**中图：PSNR-lambda 曲线**

展示了三种方法在不同 $\lambda$ 下的 PSNR 变化：
- **Tikhonov（蓝色）**：曲线较平缓，对 $\lambda$ 不太敏感，最优 PSNR 约 20 dB
- **LASSO（绿色）**：曲线在 $\lambda$ 较小时快速下降——$\ell^1$ 软阈值对 $\lambda$ 极度敏感，稍大就会将过多像素推到零
- **TV（红色）**：曲线有明显峰值，最优 $\lambda$ 附近 PSNR 可达 27 dB，但偏离最优值后性能急剧下降
- **含噪基线（黑色虚线）**：PSNR 约 20 dB，Tikhonov 和 LASSO 仅略高于此线

**右图：重建误差（MSE）对比**

柱状图展示三种方法的均方误差：
- TV 的 MSE 最低，约为 Tikhonov 和 LASSO 的 1/4
- Tikhonov 和 LASSO 的 MSE 接近，印证了两者在像素域的直接应用效果有限

---

#### 三种先验的特点总结

| 特性 | 高斯先验（Tikhonov） | Laplace 先验（LASSO） | TV 先验（ROF） |
|------|---------------------|----------------------|----------------|
| 假设 | 图像值小 | 图像值稀疏 | 图像梯度稀疏 |
| 正则项 | $\|x\|_2^2$ | $\|x\|_1$ | $\|\nabla x\|_1$ |
| 求解 | 闭式解 | 软阈值（闭式） | Chambolle 迭代 |
| 优点 | 计算高效，有闭式解 | 促稀疏，保留显著特征 | 同时平滑与保边 |
| 缺点 | 过度平滑，丢失边缘 | 像素域直接应用效果有限 | 可能产生阶梯效应 |
| 适用场景 | 简单去噪，计算资源受限 | 变换域稀疏表示 | 自然图像去噪，保边需求高 |

> **核心洞察**：本实验直观验证了 2.2 节的递进逻辑——从"值小"到"值稀疏"再到"梯度稀疏"，每一次假设修正都使先验更贴近真实图像的统计特性。TV 先验的显著优势（+6.86 dB）证明：**先验假设的质量直接决定重建效果的上限**。同时，LASSO 在像素域的失败提醒我们：稀疏先验需要配合合适的稀疏表示框架才能发挥作用。

---
**选先验，就是选"你相信图像长什么样"——而选错了，不是数值算得不准，而是把错误的信念直接刻进了重建结果里。** 高斯信"平滑"、Laplace 信"稀疏"、TV 信"边缘"，每一种正则项背后都是一个关于图像形态的具体赌注；赌注越贴近真实图像统计，重建越好，反之则引入系统性偏差。这也意味着：手写先验的天花板，其实就是人类视觉直觉的编码能力——下一节我们会看到，这种能力终究是有限的。

---

```
2.1 先验=正则项（Why）
      │
2.2 经典先验族（What：高斯/Laplace/TV）
      │
      │  "同一后验，不同估计策略 → 不同结果"
      ▼
2.3 MMSE vs MAP 与贝叶斯去噪器（How）
```
